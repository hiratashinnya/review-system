# spec-author — 設計経緯・判断記録（非規範）

> これは規範ではない。現行契約は [`.ai/agents/spec-author.md`](../agents/spec-author.md) にある。

SPEC の親子関係は、親から子への `decomposes` ではなく子から親への同型依存辺で表す。これは graph の依存方向を一方向に統一し、親ノードに将来の子一覧を持たせる二重管理を避けるために DD-014 で決めた。本文では現在の辺規則と受け入れ条件だけを規定する。
