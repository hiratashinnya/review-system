# doc-system-config — 適用範囲の経緯（rationale・非規範）

> **これは規範ではない。** 正本は `.agents/skills/doc-system-config/SKILL.md` であり、本文はこの Codex repo-skill の範囲を決めた経緯を保管する。

この skill は doc-system-v2 の `config.yml` と対応ノードを扱うために整備した。issue #140 では doc-system 側の config 操作を対象とし、review_system 側の config 操作は issue #141 の別スコープとした。#127 / #128 以降の拡張を混ぜないのは、config の変更根拠と検証対象を一つの境界に保つためである。

したがって本文の「使わない場合」は現在のスコープ境界として残し、issue 番号・計画時点・別 issue との関係という履歴はここにだけ置く。

