# 機能要求

> **型**: FR ／ **必須上流**: SR（refines ✅）
> doc-system＝Markdown 埋め込みの型付き ID グラフ＋ドリフト/構造/カバレッジを検証するツール。
> 出典：`docs/doc-system/`（DD-001〜011・RULE-001〜021・接続マトリクス・config.yaml）。

---

## FR-1: ノードグラフの構造化表現

<details><summary>⬡ FR-1 · v0.3.0</summary>

```yaml
id: FR-1
type: FR
labels: []
scheduled: ""
edges:
  - to: SR-2
    ref_version: "0.1"
  - to: DD-1
    ref_version: "0.1"
  - to: FND-12
    ref_version: "0.1"
  - to: FND-18
    ref_version: "0.1"
  - to: FND-32
    ref_version: "0.1"
```
</details>

`<details><summary>⬡ PREFIX-N` トグルと YAML ブロックで、型・ID・labels・辺を持つノードを Markdown に埋め込み、検証ツールがパースできる構造化表現を提供する（DD-001・04 記法）。

---

## FR-2: ID 体系と階層分解

<details><summary>⬡ FR-2 · v0.2.0</summary>

```yaml
id: FR-2
type: FR
labels: []
scheduled: ""
edges:
  - to: SR-3
    ref_version: "0.1"
  - to: SR-5
    ref_version: "0.1"
```
</details>

`PREFIX-N[-N...]` の連番・永続 ID を採番し、階層分割（`I-1`→`I-1-1`）は **ID パターンから推論**する（`decomposes` 辺は廃止・DD-014）。ID は永続（リネームしても変えない、意味は heading が持つ）。上下双方向のグラフ走査を可能にする（DD-002/003・02§3）。

---

## FR-3: 構造的完結性検証

<details><summary>⬡ FR-3 · v0.2.0</summary>

```yaml
id: FR-3
type: FR
labels: []
scheduled: ""
edges:
  - to: SR-2
    ref_version: "0.1"
```
</details>

グラフの構造的健全性を検証する：存在しない ID 参照（RULE-007 always_error）、孤立ノード（RULE-005 always_error）、必須接続の欠如（RULE-006・config 駆動）、階層 ID 親ノードの不在（RULE-008）を検出する（05 段階②）。

---

## FR-4: バージョンドリフト検出

<details><summary>⬡ FR-4 · v0.2.0</summary>

```yaml
id: FR-4
type: FR
labels: []
scheduled: ""
edges:
  - to: SR-6
    ref_version: "0.1"
```
</details>

ファイル単位 version（`x.y.z`、z は不問）と辺の `ref_version` を管理し（status は廃止・DD-013）、参照先の x.y 上昇時に全依存辺の ref_version 不一致を検出する（RULE-004 ERROR・DD-004/015・05 段階①）。

---

## FR-5: 意思決定ドリフト検出

<details><summary>⬡ FR-5 · v0.2.0</summary>

```yaml
id: FR-5
type: FR
labels: []
scheduled: ""
edges:
  - to: SR-6
    ref_version: "0.1"
```
</details>

DD/Q/PEND の義務辺（`DD→X`）が存在する＝意思決定の反映漏れを検出する（型で判定し DD=RULE-001 ERROR・Q=RULE-002 WARNING・PEND=RULE-022 WARNING）。反映後は辺を削除し `X→DD` を追加（DD-016）。「腐敗源（決定したのに設計に反映し忘れる）」対策の核心（02§9 グループ A）。

---

## FR-6: 仕様カバレッジ検証

<details><summary>⬡ FR-6 · v0.2.0</summary>

```yaml
id: FR-6
type: FR
labels: []
scheduled: ""
edges:
  - to: SR-2
    ref_version: "0.1"
  - to: FND-5
    ref_version: "0.1"
```
</details>

SPEC×condition（normal/boundary/empty/failure/error）×TD のカバレッジを検証する：SPEC への TD 被依存辺欠如（`must_be_linked_from: SPEC←[TD]`・RULE-006・旧 RULE-015）、condition 属性・語彙（RULE-016）、FR の normal SPEC 必須（RULE-017）・負例（RULE-018）、TD-SPEC の condition 一致（RULE-019）。`--coverage` レポートを生成する（DD-008/010・05 段階③）。

---

## FR-7: 検証・指摘の完結性検証

<details><summary>⬡ FR-7 · v0.2.0</summary>

```yaml
id: FR-7
type: FR
labels: []
scheduled: ""
edges:
  - to: SR-2
    ref_version: "0.1"
```
</details>

検証層ノードの辺と結果属性の完結性を検証する：FND/NFR/TC/VERIFY の必須接続（config `must_link_to`/`must_be_linked_from`・RULE-006）と TR の result/log_ref（RULE-020/021 ERROR・DD-011・02§9 グループ G）。

---

## FR-8: 三軸の検査抑制機構

<details><summary>⬡ FR-8 · v0.2.0</summary>

```yaml
id: FR-8
type: FR
labels: []
scheduled: ""
edges:
  - to: SR-7
    ref_version: "0.1"
```
</details>

検査ノイズを 3 つの軸で抑制する：①`scheduled`（後フェーズ＝完全サイレント）、②ステージ発火（config の `activate_stage`/`rule_activation` が current_stage 未達なら沈黙・旧 stage_scope.disable を代替）、③`suppress`（ルール単位・理由必須・ノード単位サイレント）。`always_error`（RULE-005/007）はいずれでも抑制不可（DD-007/019・02§8）。

---

## FR-9: トレース対象集合の宣言

<details><summary>⬡ FR-9 · v0.3.0</summary>

```yaml
id: FR-9
type: FR
labels: []
scheduled: ""
suppress: [RULE-018]  # 異常系なし: 走査範囲は静的な include/exclude 宣言で fail パスを持たない
edges:
  - to: SR-2
    ref_version: "0.1"
  - to: DD-1
    ref_version: "0.1"
```
</details>

`config.yaml` の `trace_scope.include`/`exclude`（glob）で検証対象（in-graph）ファイル集合を宣言し、doc-system 自身・methods・README 等を out-of-graph として除外する（exclude が include より優先）（06）。

---

## FR-10: 検証ツールの実行形態

<details><summary>⬡ FR-10 · v0.2.0</summary>

```yaml
id: FR-10
type: FR
labels: []
scheduled: ""
suppress: [RULE-018]  # 異常系なし: 出力契約（終了コード・整列）は正常系のみ。検出された違反は ERROR ではなく出力内容
edges:
  - to: SR-2
    ref_version: "0.1"
```
</details>

検証は Python CLI（標準ライブラリのみ）として実行し、ERROR があれば終了コード 1・なければ 0 を返す。出力は深刻度順（ERROR→WARNING→INFO）に整列する。段階①②を先行実装、③を次、④は sub-issue（05§6）。

---

## FR-11: テンプレートによる著作品質保証

<details><summary>⬡ FR-11 · v0.4.0</summary>

```yaml
id: FR-11
type: FR
labels: []
scheduled: ""
suppress: []
edges:
  - to: SR-1
    ref_version: "0.1"
  - to: FND-15
    ref_version: "0.1"
```
</details>

型別テンプレート（`templates/<layer>/<type>.md`）を提供し、著者がテンプレートを複製した時点で id・type・labels・scheduled・edges（ref_version 付き辺エントリ）と型別本文フォーマット（前提条件・入力/トリガ・期待動作・例）の必須フィールドが揃った状態を保証する。テンプレートを複製するだけで RULE-025（id 欠如）・RULE-026（type 欠如）・RULE-027（ref_version 欠如）が発火しない初期状態を提供する。

---

## FR-12: コード⇔設計の構造比較（段階④・post-MVP）

<details><summary>⬡ FR-12 · v0.2.0</summary>

```yaml
id: FR-12
type: FR
labels: [post-mvp]
scheduled: "post-mvp"
edges:
  - to: SR-3
    ref_version: "0.1"
```
</details>

ソースの `@id` アノテーションが DM/PORT/ORC を正しく `realizes` しているかを検証し、実装ありで設計ノードなし＝設計漏れ、設計ありで realizes 辺なし＝未実装/紐づけ漏れを検出する。実装着手後の sub-issue（05 段階④）。

---

## FR-13: 著作エージェントと層ワークフロー

<details><summary>⬡ FR-13 · v0.1.0</summary>

```yaml
id: FR-13
type: FR
labels: []
scheduled: ""
suppress: []
edges:
  - to: SR-1
    ref_version: "0.1"
```
</details>

型別著作エージェント（`requirements-author`・`spec-author`・`analysis-author`・`design-author`・`verification-author`）に著作規約（型・ID PREFIX・辺方向・本文フォーマット・RULE チェックリスト）を内包させ、外部ファイル参照なしに正しくノードを著作できるようにする。また各層の著作ワークフロー（著作エージェント呼び出し → `tmp/<sprint>/<id>.md` に出力 → reconciliation → 本ファイル反映）を定義し、著作フロー全体の整合性を確保する。

---

## FR-14: 伝搬編集支援（post-MVP）

<details><summary>⬡ FR-14 · v0.1.0</summary>

```yaml
id: FR-14
type: FR
labels: [post-mvp]
scheduled: "post-mvp"
suppress: [RULE-018]  # 異常系なし: post-MVP の支援表示機能であり、ドリフト検出（FR-4）の失敗系は FR-4 側で担保される
edges:
  - to: SR-6
    ref_version: "0.1"
```
</details>

上流ノードが更新されてドリフトが検出されたとき（FR-4・RULE-004）、下流ノードが持つ ref_version 更新が必要な辺の一覧を表示し、著者が反映漏れを効率的に解消できる支援機能を提供する（post-MVP）。

---

## FR-15: 依存グラフレポート出力

<details><summary>⬡ FR-15 · v0.1.0</summary>

```yaml
id: FR-15
type: FR
labels: [post-mvp]
scheduled: ""
suppress: [RULE-018]  # 異常系なし: post-MVP のエクスポート機能であり、出力フォーマット異常はフォーマット選択の入力検証（FR-10 側）で担保される
edges:
  - to: SR-2
    ref_version: "0.1"
  - to: DD-6
    ref_version: "0.1"
```
</details>

spec-inspector がノードグラフ全体の依存関係を Graphviz dot 形式・JSON 隣接リストの構造化フォーマットでエクスポートし、外部ツールによる可視化や分析ツールとの連携を可能にする機能を持つ（post-MVP）。

---

## FR-16: 参照関係複雑度算出

<details><summary>⬡ FR-16 · v0.1.0</summary>

```yaml
id: FR-16
type: FR
labels: [post-mvp]
scheduled: ""
suppress: [RULE-018]  # 異常系なし: post-MVP のメトリクス算出機能であり、グラフ構造の異常（孤立・不在参照）は FR-3/FR-4 側で担保される
edges:
  - to: SR-2
    ref_version: "0.1"
  - to: DD-6
    ref_version: "0.1"
```
</details>

spec-inspector が各ノードの参照関係複雑度（in-degree・out-degree・ハブノード・孤立クラスタ）を算出してメトリクスレポートとして出力し、リファクタリング優先度や高結合ノードの特定による開発意思決定支援を可能にする機能を持つ（post-MVP）。
