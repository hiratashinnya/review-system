# Codex repo skills

Codex discovers repository-scoped skills from `.agents/skills` when launched in
this repository. The directories here are migrated from the repository source skills so the
same project workflows are available to Codex CLI through the documented Agent
Skills mechanism.

When updating a shared workflow, keep the source and Codex copies in sync until
the project has a single source of truth for both clients.
