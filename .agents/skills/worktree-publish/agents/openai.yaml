interface:
  display_name: "Worktree Publish"
  short_description: "検証済み worktree を安全に draft PR として公開する"
  default_prompt: "Use $worktree-publish to publish the validated current worktree as a draft PR."
policy:
  allow_implicit_invocation: false
