**もの**: Markdown＋YAMLフロントマター形式のノードファイル群
**発生源**: 仕様著者（ACTOR-1）が作成
**形式**: `.md` ファイル、YAMLフロントマター＋Markdown本文
**タイミング**: spec-inspector 起動時にファイルシステムから読み込む

---

> **I-1-1 は退役**（旧 suppress 設定。系外から独立して届く入力ではなく、I-1（ノードファイル群）に
> 埋め込まれていた旧フィールド値であり、独立 I として持つのは PR1（もの＋発生源で分ける）・
> PR9（階層スキップ禁止）違反。退役 ID は再利用しない（DD-7）。issue #118 で suppress 機構は
> 廃止済みのため、D-4 内部表現および D-18 属性検査ビューにも suppress スライスは保持しない。
> FND-6・PEND-1 の `to: I-1-1` 辺は D-18 へ repoint 済み。）

---

> **I-1-2 は退役**（scheduled 設定。I-1 の中の scheduled フィールドの値であり P-1 がパースして初めて
> 構造化される。独立した系外入力ではないため PR1/PR9 違反（I-1-1 と同理由）。退役 ID は再利用しない
> （DD-7）。scheduled 値は D-4 内部表現に含まれ、**D-18（属性検査ビュー）の scheduled 属性へ吸収**され、
> P-2-5 と D-9（フェーズ・ステージ状態）との突合で発火判定に使用する。DD-12 (c) で決定。）

---

> **I-1-3 は退役**（ref_version 値。I-1 の辺定義に埋め込まれた ref_version フィールドの値であり P-1 が
> パースして初めて構造化される。独立した系外入力ではないため PR1/PR9 違反（I-1-1 と同理由）。退役 ID は
> 再利用しない（DD-7）。ref_version は D-4 内部表現の edges スライスに含まれ、**D-19（決定辺ビュー）へ吸収**
> され P-2-1-1（ref_version ドリフト検出）・P-2-1-2（決定スパイン義務辺残存検出）が消費する。DD-12 (c) で決定。）
