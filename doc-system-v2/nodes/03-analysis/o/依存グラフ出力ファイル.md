**もの**: `--export-graph` 実行時に指定ファイルパスへ書き出される依存グラフファイル（Graphviz dot 形式または JSON 隣接リスト形式）
**生成元**: P-8（依存グラフ出力処理）に依存（O→P）
**受け手**: レビュアー（ACTOR-2）に依存（O→ACTOR）——外部ツール（Graphviz 等）を通じてグラフを可視化・分析する
**形式**: dot 形式（`digraph { ... }` 構文）または JSON 形式（隣接リスト）。フォーマットは `--export-graph` のオプション引数で指定
