**もの**: `--complexity` 実行時に stdout へ出力される参照関係複雑度メトリクスレポート（各ノードの in-degree・out-degree・ハブ判定を id 昇順の行形式で並べたレポート）
**生成元**: P-9（参照関係複雑度計算処理）に依存（O→P）
**受け手**: レビュアー（ACTOR-2）に依存（O→ACTOR）——高結合ノード・ハブの特定と設計意思決定支援に使用する
**形式**: テキスト行形式（`{node-id} | in={N} | out={N} | hub={yes/no}`）、id 昇順、stdout 出力
