> **改訂理由（z バンプ v0.1.0→v0.1.1・FND-101 辺逆転コホート backref）**: FND-6（resolved）の辺逆転（DD-16）完了に伴い、処置対象である D-18 に `→FND-6`（ref_version "0.2"＝FND-6 現バッジ x.y）バックリファレンス辺を付与（DD-3）。バックリファレンス辺追加は内容のみ変更のため z バンプ（DD-8 §4・ドリフト非誘発）。なお当初 FND-6 を MINOR バンプし ref を "0.3" としていたが、Q-5/DD-21（辺逆転は z）により FND-6 が v0.2.1 へ訂正されたため ref も "0.2" に修正。

**もの**: D-4 から射影した属性検査用スライス（各ノードの id・type・condition・result・log_ref・scheduled・辺メタデータ）
**種別**: 内部データ（系外へ出ない）
**生成元**: P-1-6（検査ビュー射影）に依存（D→P）
**消費先**: P-2-3-1〜4（カバレッジ属性検査リーフ）・P-2-4-1〜3（検証層完結性検査リーフ）・P-2-5（発火・重症度確定プロセス・scheduled スライスとして）が P→D-18 で消費
**含むフィールド**: id・type・condition（SPEC/TD）・result（TR）・log_ref（TR）・scheduled（フェーズ指定）・edges（辺メタデータ全体）

> **注（G9・scheduled 同名語の出自）**: D-18 の `scheduled` は各**ノードのフロントマター `scheduled` 属性（フェーズ限定発火指定＝個々のノードの指定値）**由来である。D-9（フェーズ・ステージ状態）の `scheduled` 関連が config.yaml の phases リスト（許容値集合＝scheduled 値ドメイン）由来であるのとは別概念。発火判定では D-18 の各ノード scheduled（指定値）を D-9 の phases（ドメイン）と突合する。

**スタンプ結合解消**: I-1-2（scheduled）の退役により、scheduled を独立 I として配線していた疑似スタンプ結合を解消する。scheduled は D-18 の属性として一元保持し P-2-5 へ渡す。I-1-1（suppress）は issue #118 で機構ごと廃止済みのため D-18 には含めない。
