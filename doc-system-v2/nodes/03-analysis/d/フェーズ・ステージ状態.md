**もの**: config.yaml の current_phase・current_stage・phases・stages の 4 フィールドを束にした状態オブジェクト
**種別**: 内部データ（系外へ出ない）
**生成元**: P-5（設定読込・検証）の子 P-5-3（設定スライス組立）に依存（D→P）
**消費先**: P-1-4（スキーマ検証・scheduled 値ドメイン照合）・P-2-5（抑制・発火フィルタ・フェーズ発火制御）が P→D-9 で消費（発火制御は DD-12(b) で P-2-5 に一元化。旧記述の「P-3（カバレッジ点検・発火制御）」は実 edges に存在せず FND-94 G4 で削除）
**含むフィールド**: `current_phase`（現スプリント名）・`current_stage`（現ステージ名）・`phases`（有効スプリント名リスト・scheduled 値ドメイン照合用）・`stages`（有効ステージ名リスト）

> **注（G9・scheduled 同名語の出自）**: D-9 の `scheduled` 関連は config.yaml の **phases リスト（有効スプリント名＝scheduled 値ドメイン照合用の許容値集合）**由来である。各ノードのフロントマター `scheduled` 属性（フェーズ限定発火指定）を保持する D-18（属性検査ビュー）の `scheduled` とは別概念（前者はドメイン＝許容値集合、後者は個々のノードの指定値）。

**スタンプ結合解消**: D-3（全 12 セクション）を丸ごと配っていたのをフェーズ・ステージ制御に必要な 4 フィールドのみに絞った。P-6 等が phases を受けずに済む。
