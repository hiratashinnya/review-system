**もの**: config.yaml の trace_scope セクション（include/exclude の glob パターン対）
**種別**: 内部データ（系外へ出ない）
**生成元**: P-5-3（設定スライス組立）に依存（D→P）
**消費先**: P-6-1（include 一致抽出）・P-6-2（exclude 除外適用）が P→D-16 で消費
**含むフィールド**: `trace_scope.include`（対象 glob リスト）・`trace_scope.exclude`（除外 glob リスト）

**スタンプ結合解消**: P-6 が D-3 全 12 セクション（実際に使うのは trace_scope のみ）を受けていたのを D-16 の 1 セクションだけに絞った。これが D-3 スタンプ結合における最大の改善。
