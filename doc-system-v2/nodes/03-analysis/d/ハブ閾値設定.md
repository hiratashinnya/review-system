**もの**: in/out-degree のハブ判定閾値設定（上限閾値を超えるノードをハブと判定）
**種別**: 内部データ（系外へ出ない）
**生成元**: P-5-3（設定スライス組立）に依存（D→P）
**消費先**: P-9（参照関係複雑度計算処理）のみが P→D-15 で消費
**含むフィールド**: in-degree 閾値・out-degree 閾値（固定値運用時は P-9 内定数で代替可。設定化する場合は D-15 を使用・sprint-2 で確定）

> **post-mvp**（labels:[post-mvp]・scheduled: "sprint-2"）。D-15 存否の最終判断は sprint-2 でオーナー確認。
