**もの**: D-4 から射影した全ノード id・edges の最小トポロジスライス（依存グラフ出力・複雑度計算に特化）
**種別**: 内部データ（系外へ出ない）
**生成元**: P-1-6（検査ビュー射影）に依存（D→P）
**消費先**: P-8（依存グラフ出力処理）・P-9（参照関係複雑度計算処理）が P→D-22 で消費
**含むフィールド**: 全ノードの id・type・edges（to のみ）——型別拡張属性・condition・result 等は除外

> **post-mvp**（labels:[post-mvp]・scheduled: "sprint-2"）。

**スタンプ結合解消**: P-8/P-9 が D-4 全体を直読みしていたのを id/edges のみの最小集合に絞った。メトリクス系プロセスが属性情報を受けずに済む。
