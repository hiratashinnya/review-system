検査ノイズを 2 つの軸で抑制する：①`scheduled`（後フェーズ＝完全サイレント）、②ステージ発火（config の `activate_stage`/`rule_activation` が current_stage 未達なら沈黙・旧 stage_scope.disable を代替）。かつて存在した③`suppress`（ルール単位・理由必須・ノード単位サイレント）は issue #118 でオーナー方針により廃止した——drift／RULE-004 は依存先ノードが更新されたら自ノードへの影響有無の確認を強制するための契機であり、それを凍結して黙らせる発想自体が誤りだったため、無条件で発火させる設計に一本化した（RULE-004/007 等は今後も抑制不可）。タイトル「三軸の検査抑制機構」は slug=id=ファイル名の不変条件と被参照辺を壊さないため退役後も歴史的名称としてそのまま維持しており、実体は現在①②の二軸である。
