in-graph ノードの本文が孤立 `---`（ノード分離専用記法の本文内誤用）を含まないことを検査し、
パーサによる本文の silent 截断（データ欠落）を予防する要件のアンブレラ。

孤立 `---` 非存在の判定（子: 正常系）と検出時の WARNING 出力（子: 失敗系）に分けて子ノードで検証する。
本ノード自体は非テスタブルの傘。

> 注: これは新フォーマット（doc-system v2）の**サンプルノード**。本文は `.md` に本文のみ、
> 属性は同名 `.yaml`（サイドカー）に持つ。type=spec は path から、status は無し（spec は lifecycle なし）。
