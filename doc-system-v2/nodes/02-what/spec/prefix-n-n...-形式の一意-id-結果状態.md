**前提条件**: 既存ノード集合の ID リストが判明している
**入力/トリガ**: 新規ノードが in-graph ノード集合へ追加される
**期待動作**: 追加後のノード ID は、`PREFIX-N[-N...]` 形式に合致し、既存 in-graph ノード ID と重複しない一意な結果状態になる。
**例**: 既存 ID が `SPEC-3`・`SPEC-3-1` のとき、新規ノード ID `SPEC-3-2` は形式適合かつ一意で通過し、`SPEC-3-1` は重複として不適合になる。
