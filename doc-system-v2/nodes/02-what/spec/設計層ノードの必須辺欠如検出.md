**概要**: 設計層ノードの必須辺（上流の依存辺 `must_link_to` ＋下流の被依存＝実現辺 `must_be_linked_from`）の欠如検出（アンブレラ・design 段〜実装段にまたがる）。対象ノードは ORC/DS/MOD/DM/PORT/PRS/SCM/CFG/PROMPT/E/P。検証アサーションは config の各行を 1 行 1 SPEC に分割した子 SPEC を参照する（設計段 15 行＋実装段 7 行＝計 22 子・DD-9 で下流連続性の被依存辺 10 行を追加）。

**子の内訳**:
- 上流依存辺（`must_link_to`・design 段・11 子）: ORC→E / DS→P / MOD→[P,D] / DM→TERM / DM→MOD / PORT→MOD / PRS→DS / SCM→SPEC / CFG→SCM / CFG→SPEC / PROMPT→SPEC。
- 下流被依存辺（`must_be_linked_from`・design 段・4 子）: E←ORC / P←MOD / SCM←CFG / DS←PRS（DD-9 で P←MOD・SCM←CFG・DS←PRS を追加）。
- 下流被依存＝実現辺（`must_be_linked_from`・実装段・シンボル適格性条件付き・7 子・DD-9 追加）: MOD←SRC / DM←SRC / PORT←SRC / ORC←SRC / PRS←SRC / PROMPT←SRC / CFG←SRC。

各子は当該 config 行の severity で RULE-006 を報告する。発火ステージは子ごとに異なり、design 段の子（上流依存辺＋design 段の被依存辺）は `activate_stage: design` 到達時、実装段の子（*←SRC）は `activate_stage: implementation` 到達時に発火する。本傘は非テスタブル（condition 無）で RULE-016 対象外。
