**深刻度**: ERROR（**実害で判定する**。機械検査規則の有無・テストが現に落ちるか否かは判定根拠にしない）

判定根拠は次の3点。

1. **現に壊れている（latent ではない）** — Issue #387（PR #383）で `CLAUDE.md` は規範本文を `.claude/rules/*.md` へ分割し、**本体は 47 行**に縮小した（2026-08-18 実測）。in-graph 3 ノードが引用する `L86` / `L188–210` / `L192–195` は**いずれも現行ファイルの行数を超えており、指す先が存在しない**。「まだ発火していない欠陥」ではなく、**引用が例外なく空振りしている状態が既に成立**している。
2. **失われたのが「是正指示の宛先」そのもの** — 3 件はいずれも open FND であり、行番号は「何を直せば解消するか」を特定する唯一の座標として使われている。宛先を失った是正指示は実行できない。とりわけ open FND `tmp草案の出力先記述がv1の1親1ファイル形式のまま1ノード2ファイル化に未追随` は**解消条件4**が「`CLAUDE.md` L86 がオーナー承認のうえ是正されている」であり、**解消判定そのものが不能**になっている。同 FND は本文・実害・forward 辺選定理由・選択肢①②・推奨・解消条件の**6 箇所**が同一の `L86` に依存する。
3. **再発が構造化されている** — 規範の正本は今後「本体 ＋ `.claude/rules/*.md` 7 ファイル」の集合であり、各ファイルは独立に編集される。行番号引用は**編集のたびに無効化し続ける**。PR #383 は同時に `CLAUDE.md`「『CLAUDE.md』は総称として読む」節で「行番号での引用（`CLAUDE.md L86` 等）は分割で無効になったので使わない（節名で参照する）」という新しい読み方規範を追加しており、**既存コーパスがその規範に未追随である**という状態でもある。

**内容**: 分割前の `CLAUDE.md` を**行番号で**引用している記述が、in-graph ノード 3 件・ダッシュボード 1 箇所・退役ツール 2 ファイルに現存する。PR #383 による分割で、これらの引用先はすべて実体を失った。

**走査条件（2026-08-18 実測）**: リポジトリ全体に対し
`(CLAUDE\.md[^\n]{0,120}L[0-9]{1,4}|L[0-9]{1,4}[^\n]{0,60}CLAUDE\.md)` を適用。
ヒットは下表がすべてであり、これ以外に行番号引用は存在しない（`CLAUDE.md` 自身 L33 の新規範文＝**正しい記述**は対象外）。

**対象一覧（in-graph・forward 辺を張る）**

| 対象ノード | 引用箇所 | 引用値 | 現在の実体（節名で指すべき先） |
|---|---|---|---|
| FND `tmp草案の出力先記述がv1の1親1ファイル形式のまま1ノード2ファイル化に未追随`（v0.1.0） | 実測表（本文 L36）・forward 辺の張り先と選定理由（L73）・選択肢①（L84）・選択肢②見出しと内容（L88・L89）・推奨の根拠2（L110）・**解消条件4（L126）** の計 6 箇所 | `CLAUDE.md` L86 | `.claude/rules/05-skills-agents.md`「ノード著作の委譲ルール」（当該文＝「各著作エージェントは `tmp/<sprint>/<parent-id>.md` に出力する」は同節の末尾段落） |
| FND `tmp 掃除ガード追従テストの段落分割が空行基準で箇条書き・表内の保護名借用を見逃す`（v0.1.1） | 実測表の `CLAUDE.md` 行（本文 L18）に 6 個の行番号 | L192–195（ガード文）／L188–210（段落範囲）／L189（ハンドオフ置き場）／L205（worktree 相対パス）／L206–210（write 権限がないエージェント） | `.claude/rules/05-skills-agents.md`「戻り値のハンドオフ規約（write 権限の有無で分ける・2026-07-28）」（掃除ガード＝同節の `tmp/_handoff/` 除外に関する箇条書き。「write 権限がないエージェント」も同節内の別項目） |
| FND `tmp 掃除ガード追従テストの検査対象探索がガード記述の消滅とミラー文書を見落とす`（v0.1.1） | 実測表の `CLAUDE.md` 行（本文 L26）「L192–195 ほか。保護名 2 件を明記（是正済み）」 | L192–195 | 同上 |

**対象一覧（out-of-graph・辺を張れない）**

| 対象 | 引用箇所 | 引用値 | 現在の実体 |
|---|---|---|---|
| `doc-system-v2/00-dashboard.md` L117（open FND 一覧の備考欄） | 1 箇所 | `CLAUDE.md` L86 | `.claude/rules/05-skills-agents.md`「ノード著作の委譲ルール」 |
| `archive/backref-v1/README.md` L13 ／ `archive/backref-v1/reverse.py` L3・L252 | 3 箇所 | CLAUDE.md L27–29 ／ L28 | `.claude/rules/02-decision-process.md`「判断の仰ぎ方（フェーズ別・空で止めない＝PR7）」（「処置したら必ずバックリファレンス」「FND 起票時は ref_version を本文にも記録」の 2 項目） |

**具体的な破損の内訳**

1. **解消判定の不能**（最も重い）— `tmp草案…` の解消条件4「`CLAUDE.md` L86 がオーナー承認のうえ是正されている」は、指す行が存在しないため充足判定ができない。同 FND は「`CLAUDE.md` 訂正完了まで resolved にしない」（推奨の根拠3）と自ら定めているので、**この 1 行が直らない限り FND 全体が resolved にできない**構造になっている。
2. **受入基準の検証コマンドが対象を取り違えている**（正確に記す）— 同 FND の解消条件5 は `grep -rn 'tmp/<sprint>/<parent-id>\.md' doc-system-v2/ CLAUDE.md .claude/` である。実測すると `CLAUDE.md` 自身は当該文字列を**もはや含まず**、実体は `.claude/rules/05-skills-agents.md` にある。**コマンド全体としては第3引数 `.claude/` に偶然カバーされてヒットする**ため 0 件判定は破綻していないが、明示的に名指しした `CLAUDE.md` は**空振りする死んだ検査対象**であり、是正すべきファイルを読み手に誤って伝える。「たまたま拾えている」状態を受入基準として残すのは不可（PR2・機械判定は明示的であるべき）。なお同コマンドは配送用の写し `.claude/hooks/governance-directives.md` も走査範囲に含むが、実測では同写しに当該文字列は無い。
3. **2 件の掃除ガード FND の実測表が読めない** — 両 FND の中核は「どの記述がどの段落に同居しているか」という**位置に依存した実測**であり、L188–210・L192–195・L189・L205・L206–210 という行範囲がそのまま論拠になっている。分割後は行範囲を再取得しないと表の主張を検証できない。ただし主張の内容（同一段落内の借用の余地）は節構造として維持されているため、**論拠の再取得コスト**が実害であって主張が誤りになったわけではない。
4. **ダッシュボードの要約が誤情報を配る** — `00-dashboard.md` L117 は open FND 一覧の備考であり、進捗ハブとして最も読まれる。ここに死んだ座標が残る。
5. **退役ツールの依存仕様アンカーが切れる** — `archive/backref-v1` の `依存仕様:` 記述は「out-of-graph を唯一の根拠にしない」規律（`.claude/rules/07-project-structure.md`「依存仕様の参照原則」）の補助ナビとして CLAUDE.md 行番号を挙げている。退役済みで実害は小さいが、切れた参照が残る。

**forward 辺の張り先と選定理由**

- 辺を張るのは in-graph の 3 FND ノードのみ。**FND → FND の forward 辺**は `config.yml` `fnd_lifecycle.unresolved.must_link_to`（`target: any`）で許容される形であり、指摘対象が検証層ノードであることによる制約は無い。
- `CLAUDE.md`・`.claude/rules/*.md`・`doc-system-v2/00-dashboard.md`・`archive/backref-v1/*` は **out-of-graph**（`trace_scope` 外・対応ノードを持たない）であり辺を張る先が存在しない。したがって本文の上表が処置対象の記録であり、**解消時も `dsv2 reverse` はこれらに backref を付与できない**（3 FND ノードにのみ backref が付く）。out-of-graph 分の是正完了は**本ノード本文への追記で証跡を残すこと**。
- `archive/backref-v1` の 3 箇所は、`.claude/rules/02-decision-process.md`「起票先はプロジェクト区分で決める」の分類上**どちらのシステムにも含有されない汎用開発ハーネス（v1-legacy 退役ツール）**に当たり、正しい起票先は FND ではなく **Issue** である。本 FND では**事実の記録に留め**、Issue 起票を推奨する（起票要否・時期はオーナー判断。「対応不要」とは判断していない）。

**選択肢**

1. **行番号引用を節名引用へ一括置換する（PR #383 の新規範への適合として実施）** — 上表「現在の実体」列のとおり、`CLAUDE.md L86` → 「`.claude/rules/05-skills-agents.md`「ノード著作の委譲ルール」」のように置き換える。`CLAUDE.md`「『CLAUDE.md』は総称として読む」節が定めた ①行番号を使わない ②ファイル境界を越える参照は `.claude/rules/NN-*.md` の実パスを併記する、の2点をそのまま適用するだけであり、**新しい判断を一切持ち込まない**。ノード 3 件は `verification-author` → `reconciliation-validator` → `reconciliation` で確定（内容変更なので z バンプ＝本文修正のみ・DD-8 §4）、ダッシュボードは要約行を更新、`archive/backref-v1` は別 Issue。欠点は、3 ノード＋ダッシュボードの編集が発生すること（ただし置換は機械的）。
2. **行番号を現行の `.claude/rules/*.md` の行番号へ振り直す** — 座標としては最も精密になるが、**同じ壊れ方を将来にわたって再生産する**（rules ファイルは独立に編集される）。PR #383 が明示的に禁じた形式でもあるため、規範に正面から反する。採らない。
3. **引用形式には手を付けず、各 FND の解消時にまとめて直す** — コストはゼロだが、①解消条件4 が判定不能なままなので**そもそも解消できない**（デッドロック）、②その間ダッシュボードが誤情報を配り続ける。**この選択肢を AI が独断で採ることはしない**（「対応不要」の独断禁止）。オーナーが明示的に選択した場合のみ成立する。
4. **①に加えて、行番号引用を機械検査で禁じる（再発防止）** — `CLAUDE.md`／`.claude/rules/*.md` を行番号で参照する記述（`CLAUDE\.md\s*L\d+` 等）を検出して落とす lint を CI に追加する。①だけでは「次に誰かが書いた行番号引用」は止まらない。反面、検査対象・語彙の設計（`.claude/rules/NN-*.md L12` 形式も禁じるか、コード docstring も対象にするか）を別途詰める必要があり、①より重い。**新設する lint 自体はどちらのシステムにも含有されない汎用ハーネスに当たるため、起票先は Issue になる**点に注意。

**推奨**: **① を基軸に、④ を再発防止として併用するか否かをオーナー判断に委ねる。**根拠は3点。

- (a) ① は PR #383 が既に確定させた規範の**機械的な適用**であって、新規の設計判断を含まない。節名は `.claude/rules/*.md` 間で重複しない（`CLAUDE.md`「総称として読む」節が明言）ため、置換後の参照は一意に解決でき、行番号のような陳腐化もしない。
- (b) ① を先に済ませないと `tmp草案…` FND が**解消できない**（解消条件4が判定不能）。他の 2 件（掃除ガード系）も選択肢の採否がオーナー判断待ちであり、その判断材料である実測表の座標が読めない状態が続く。すなわち ① は他 3 件の処置の**前提ブロッカー**である。
- (c) ④ は正しい方向だが、①（既存の 10 箇所の是正）とは責務が異なる（追随 vs 再発防止）。①に混ぜると本 FND の単一責務が崩れる（PR1）ため、**採否と実装は切り出す**のが整合的。なお ④ を採らない場合、再発防止は `CLAUDE.md` L33 の規範文と各 author エージェントの規律のみに依存することになる。

**採否の決定はオーナー判断を仰ぐ**（実施スプリントは `scheduled: sprint-1`＝現行スプリント。繰り越しはオーナーの明示指示がある場合のみ）。**「対応不要」とは判断していない。**

**解消条件（すべて満たすこと）**

1. FND `tmp草案の出力先記述がv1の1親1ファイル形式のまま1ノード2ファイル化に未追随` の 6 箇所の `CLAUDE.md L86` が節名引用（`.claude/rules/05-skills-agents.md`「ノード著作の委譲ルール」）へ是正されている。あわせて**解消条件5 の検証コマンドの操作対象から死んだ `CLAUDE.md` を外し**、`.claude/rules/` を明示した形へ是正されている。
2. FND `tmp 掃除ガード追従テストの段落分割…` の実測表 `CLAUDE.md` 行が、節名（`.claude/rules/05-skills-agents.md`「戻り値のハンドオフ規約…」）を基準にした記述へ是正されている（段落同居の主張は現行ファイルで再取得した事実に置き換える）。
3. FND `tmp 掃除ガード追従テストの検査対象探索…` の実測表 `CLAUDE.md` 行が同様に是正されている。
4. `doc-system-v2/00-dashboard.md` L117 の備考欄が節名引用へ是正されている（out-of-graph・backref 不可のため本節に完了記録を追記すること）。
5. `archive/backref-v1/README.md` L13 ／ `reverse.py` L3・L252 の扱いが決着している（Issue 起票 → 処置、またはオーナーが明示的に据え置きを決定し理由が記録されている）。out-of-graph・別区分のため、本 FND では**決着の有無のみ**を条件とする。
6. `(CLAUDE\.md[^\n]{0,120}L[0-9]{1,4}|L[0-9]{1,4}[^\n]{0,60}CLAUDE\.md)` のリポジトリ全体走査で、`CLAUDE.md` L33 の規範文（「行番号での引用は使わない」）と本 FND 自身の記述**以外**のヒットが 0 件。

**接続規則変更の伴否**: 本指摘は `doc-system-v2/config.yml` の `must_link_to` / `must_be_linked_from` / `fnd_lifecycle` の追加・変更・削除を**含まない**（対象はノード本文の参照表記であって接続規則ではない）。したがって接続マトリクス（`docs/doc-system/03-connection-matrix.md`）・ドキュメント一覧（`docs/doc-system/01-document-items.md`）・各 author エージェント／スキルへの伝播同期は**不要**と判断した。ただし選択肢④（行番号引用の機械検査）を採る場合は、著作規約側（各 `*-author` の「よくある誤り」表）への追記が伴うため、その時点で伝播チェックを再実施すること。

**本指摘の対象外（留保）**

- **`tmp草案…` FND の対象集合が実態より狭い可能性** — 本 FND の走査中に、v1 形式の文字列 `tmp/<sprint>/<parent-id>.md` が同 FND の対象表（PRS/DS/ORC ＋ `CLAUDE.md`）に**挙がっていない**箇所にも現存することを実測した（in-graph: `nodes/03-analysis/p/草案-tmp-書出.md`・`nodes/03-analysis/d/ノード草案-tmp-草案.md`／out-of-graph: `.claude/tailoring-registry.md`）。これは**引用形式**ではなく**対象集合の網羅**の問題であり機序が異なるため、DD `FND 起票単位は機序で分け対象集合が同型の指摘は既存 FND のスコープ拡張とする` の判定手順に照らしても本 FND に混ぜるべきではない。同 FND のスコープ拡張として扱うか別途起票するかは**オーナー判断を仰ぐ**（**「対応不要」とは判断していない**）。
- **`archive/backref-v1` の 3 箇所** — 起票先分類により Issue 運用が正しい（上記「forward 辺の張り先と選定理由」参照）。本 FND では解消条件5 として「決着していること」のみを求め、処置内容は Issue 側に委ねる。
- **`.claude/agents/*.md` 等、CLAUDE.md 以外のファイルを行番号で引用している箇所**（例: `tmp草案…` FND が挙げる `.claude/agents/verification-author.md` L28-29、`保護名追従テスト…` FND が挙げる `.claude/agents/reconciliation.md` L63、`dsv2/cleantmp.py` L30–37 等）は、**当該ファイルが分割されていないため現に有効**であり、本指摘の対象外。行番号引用一般の是非は選択肢④の設計論点に属する。

**対応状況**: open

**経緯**: Issue #387（PR #383・`CLAUDE.md` を `.claude/rules/*.md` へ分割）の是正作業中に、`issue-fixer` が全数走査で発見した**スコープ外**の指摘。分割 PR 自体は新規範（行番号引用を使わない）を `CLAUDE.md` に追加したが、**既存コーパス側の追随は同 PR のスコープに含まれていなかった**ため、規範と実態の乖離として残った。

**指摘時 ref_version**: `tmp草案の出力先記述がv1の1親1ファイル形式のまま1ノード2ファイル化に未追随` "0.1"（同 `.yaml` v0.1.0 時点）
**指摘時 ref_version**: `tmp-掃除ガード追従テストの段落分割が空行基準で箇条書き・表内の保護名借用を見逃す` "0.1"（同 `.yaml` v0.1.1 時点）
**指摘時 ref_version**: `tmp-掃除ガード追従テストの検査対象探索がガード記述の消滅とミラー文書を見落とす` "0.1"（同 `.yaml` v0.1.1 時点）
