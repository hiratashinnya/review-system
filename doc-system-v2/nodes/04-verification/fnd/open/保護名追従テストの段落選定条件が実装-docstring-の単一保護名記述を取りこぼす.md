**深刻度**: WARNING（**実害で判定する**。テストが現に落ちるか否かは判定根拠にしない）

実害の判定根拠は次のとおり。

1. **見逃しは現に発生している** — `dsv2/cleantmp.py` の module docstring 内に、保護名を `_handoff` だけ挙げて `_karte` を欠く記述が**現存**しており、追従テストはこれを検査していない（後述の実測）。前 FND（段落粒度）が「現時点で誤ったガード記述は無い」であるのに対し、本指摘は**すでに1件を取りこぼしている**点で状態が異なる。
2. **ただし当該記述は削除主体が読む規範ではない** — 取りこぼされている文は「素の `rm -rf` を許すとパス解決を誤ったときに `tmp/_handoff/` やリポジトリ本体まで消しうる」という**ツール新設の背景説明**であり、`PROTECTED_DIRNAMES` の規範的な列挙（同 docstring L13、L45–51 の定義）は両保護名を正しく備えている。掃除を実行する `reconciliation` が読むのは `.claude/agents/reconciliation.md` 側であり、この記述の誤読が直ちに `tmp/_karte/` の削除に至る経路は無い。したがって ERROR には当たらない。
3. **それでも軽視できない** — K-11 が防ごうとしたのは「保護名を増やしたのに記述が追いつかない」ことであり、`.claude/agents/reconciliation.md` L63 では**まったく同じ文型**（`rm -rf` を許すと…を消しうる）が `_handoff`・`_karte` の両方へ是正済みである。すなわち**双子の文の片方だけが是正され、テストがその非対称を検出できていない**。テストが守ると宣言している範囲に穴が空いている、という意味で機構の信頼性そのものに関わる。

**内容**: `tests/unit/test_dsv2_cleantmp.py::TestGuardDocsMatchProtectedNames._guard_paragraphs()` は「ガード説明」段落を次の**両条件**で選ぶ。

1. 段落が `clean-tmp` または `保護名` の語を含む。
2. 段落が `PROTECTED_DIRNAMES` のいずれか1つ以上を含む。

条件1は**特定の語の出現**に依存する。そのため、**保護名だけを挙げて `clean-tmp`・`保護名` のどちらの語も使っていない記述**は、ガード説明としての実質を備えていても選定から漏れ、一度も検査されない。条件1はもともと「掃除ガードと無関係な `_handoff` 言及（ハンドオフ規約など）を拾わない」ための絞り込みだが、**絞りすぎて真のガード説明まで落としている**。

**実測（`dsv2/cleantmp.py`・段落境界は `_paragraphs()` の定義＝空行区切り）**

| 段落 | 行 | 条件1（`clean-tmp`/`保護名`） | 条件2（保護名の出現） | 選定 | 実際の記述 |
|---|---|---|---|---|---|
| P1 | L1 | ○（`dsv2 clean-tmp`） | ✗ | 非選定 | 表題のみ |
| **P2** | **L3–7** | **✗** | **○（`_handoff` のみ）** | **非選定（取りこぼし）** | **「素の `rm -rf` を許すとパス解決を誤ったときに `tmp/_handoff/` やリポジトリ本体まで消しうるため…」＝ `_karte` を欠く単一保護名記述** |
| P3 | L9–21 | ○ | ○（両方） | 選定・PASS | ガード1〜5 の列挙 |
| P4 | L23–28 | ✗ | ✗ | 非選定 | TOCTOU 対策 |
| P5 | L30–37 | ○ | ○（両方） | 選定・PASS | 依存仕様（ただし両保護名は別々の bullet に分かれており、**借用によって PASS している**＝前 FND の指摘対象） |
| P6 | L45–52 | ✗ | ○（両方） | 非選定 | `PROTECTED_DIRNAMES` の**定義そのもの**。両方書かれているため結果的に無害だが、**実装の正本が検査対象外**である |

対比すべき是正済みの双子文が `.claude/agents/reconciliation.md` L63 にある——「素の `rm -rf` は使わない（Bash の許可用途に無く、パス解決を誤ると `tmp/_handoff/`・`tmp/_karte/` やリポジトリ本体を消しうる）」。**同じ趣旨・同じ文型でありながら、実装ファイル側だけが `_handoff` 単独のまま残っている。**

なお P6（`PROTECTED_DIRNAMES` の定義行とコメント）が非選定である点は、テストの構造として見落としやすい。**実装の正本が「ガード説明」の母集合から外れている**ため、ここで保護名が増減しても本テストは何も言わない（実際の追従は他段落が担っている）。

**in-graph 根拠の不在**: 前 FND `tmp 掃除ガード追従テストの段落分割が空行基準で箇条書き・表内の保護名借用を見逃す` に記載したとおり、`dsv2/cleantmp.py` および `tests/unit/test_dsv2_cleantmp.py` に対応する MOD/P/TD/TC は in-graph に存在しない（`nodes/**` を `cleantmp`/`clean-tmp`/`clean_tmp` で検索して 0 件）。同じ理由で forward 辺は近縁ノードへの便宜的な張り先とする。

**forward 辺の張り先と選定理由**: 前 FND と同一の2件（設計層＝PROMPT `reconciliation 調停支援プロンプト（検証＋本ファイル転記）`＝取りこぼされた文の是正済み双子文を持つ文書の in-graph 表現／分析層＝P `調停・本ファイル反映`＝掃除を実行する活動）。選定理由の詳細は前 FND 本文に記載したものと同じ。

**選択肢**

1. **条件1を緩め、選定を「保護名の出現」に一本化した上で、除外対象を明示的な除外規則で落とす** — 具体的には、ハンドオフ規約のような無関係な `_handoff` 言及を「除外パターン」で落とし、それ以外の保護名言及はすべてガード説明とみなす。取りこぼしは無くなるが、除外パターンの保守が新たな追従対象になり（除外を書き忘れると偽陽性）、条件1導入時に避けたはずの問題が形を変えて戻る。
2. **条件1に「削除・掃除の危険を述べる語」を追加する**（例: `rm -rf`・`削除`・`消し`・`掃除`）— 今回の取りこぼし（`rm -rf`／`消しうる`）は確実に拾える。最小変更で済むが、**語の列挙に依存する構造は変わらない**ため、別の言い回しで書かれた次の記述をまた取りこぼす。同種の再指摘（K-11 → N-01 → N-02）が繰り返されてきた経緯からして、根本解にはならない。
3. **`STALE_PHRASES` 方式へ寄せる** — 取りこぼした実文言を `STALE_PHRASES` に追加して固定する。即座に赤くでき実装は最小だが、既知の文言しか止められない（`STALE_PHRASES` は本来「是正前の実文言」を再発防止で凍結するための仕組みであり、未知の記述には無力）。
4. **記述側を是正するだけに留める** — `dsv2/cleantmp.py` L5–6 を `.claude/agents/reconciliation.md` L63 と同じ形（`tmp/_handoff/`・`tmp/_karte/`）へ直す。今回の1件は消えるが、**テストの取りこぼし機序はそのまま残る**ため、次回の保護名変更で同じことが起きる。

**推奨**: **選択肢 4（記述の即時是正）を必ず行った上で、選択肢 2 を暫定策、選択肢 1 を本命として検討する**。根拠は3点。

- (a) 現に残っている単一保護名記述は、機序の議論と独立に**今すぐ直すべき事実誤り**である（`_karte` も保護対象なのに `_handoff` だけを挙げている）。テストの設計をどうするかの決定を待つ理由がない。
- (b) 選択肢 2 は変更が小さく、今回と同じ文型の再発を確実に止められるので、決定までの穴埋めとして有効。ただしこれを最終形にすると「語を1つずつ足す」保守に陥り、K-11 系列が繰り返してきた後追い是正と同じ轍を踏む。
- (c) 選択肢 1 は、除外規則という**明示的で一覧できる保守対象**へ問題を移す点で、暗黙の語彙依存より監査しやすい。前 FND の選択肢1（記述単位への段落分割）と同時に実施すれば、判定単位・選定条件の両面を1回の改修で揃えられる。
- なお P6（`PROTECTED_DIRNAMES` の定義そのものが検査母集合外）を対象に含めるかは、上記いずれの案とも独立した設計判断として併せて確認したい。

**採否の決定はオーナー判断を仰ぐ**（実施スプリントは `scheduled: sprint-1`＝現行スプリントのまま。繰り越しはオーナーの明示指示がある場合のみ）。**「対応不要」とは判断していない。**

**本指摘の対象外（留保）**

- 段落分割の粒度（箇条書き・表・docstring の中でブロック全体が1段落になる件）は機序が異なるため、別 FND `tmp 掃除ガード追従テストの段落分割が空行基準で箇条書き・表内の保護名借用を見逃す` として同時に起票した。両者を1件へ統合すべきかは引き続きオーナー判断を仰ぐ（DD `FND 起票単位は機序で分け対象集合が同型の指摘は既存 FND のスコープ拡張とする` の判定手順に照らすと機序が異なるため分離が既定であり、統合しない側に倒すのが整合的である旨を申し添える）。
- **`_discover_guard_docs` の探索母集合の欠陥**（ガード記述を丸ごと失った文書が黙って対象から外れる）〔**決着済み・v0.1.1 で更新**〕 — v0.1.0 は「扱いは前 FND の『本指摘の対象外（留保）』に記載した」として前 FND の未決留保へ委譲していたが、**起票要否は既に決着している**。オーナー判断（2026-08-04）により、独立した ERROR の FND `tmp 掃除ガード追従テストの検査対象探索がガード記述の消滅とミラー文書を見落とす` として起票済み（決定の記録＝上記 DD）。以後この件の正本は同 FND であり、本ノード・前 FND のいずれも未決として問わない。

**接続規則変更の伴否**: 本指摘は `doc-system-v2/config.yml` の `must_link_to` / `must_be_linked_from` の追加・変更・削除を含まない。したがって接続マトリクス・ドキュメント一覧・各 author エージェント／スキルへの伝播同期は不要と判断した。本改訂（v0.1.1）でもこの判断に変更はない。

**改訂履歴**

| 版 | 変更 |
|---|---|
| 0.1.0 | 初版（深刻度 WARNING） |
| 0.1.1 | **PR #326 レビュー指摘 R-02 の是正**。「対象外（留保）」節のうち `_discover_guard_docs` の件を、**決着済みであることと参照先**（独立起票された ERROR の FND 名・決定を記録した DD）を示す記述へ更新し、未決として問う形を残さないようにした。あわせて実測表 P5 の行番号を `_paragraphs()` の実際の段落境界（L30–37・docstring 閉じ `"""` を含む）へ揃え、同段落が借用によって PASS している事実（前 FND の指摘対象）を注記した。選択肢 4 の行番号表記も実体に合わせた（L6 → L5–6）。**深刻度（WARNING）・指摘内容・選択肢の構成・推奨・forward 辺 2 本・各 `ref_version`・`scheduled` は変更していない。** |

> **バンプ種別の根拠（v0.1.0 → v0.1.1 ＝ z バンプ）**: DD-8 §4 は「構造変更（YAML キー追加・型変更・**辺の追加/削除/変更**）→ MINOR バンプ（x.Y）」「内容のみ（**本文修正**・バックリファレンス辺追加・suppress 追加）→ z バンプ（x.y.Z）」と定める。本改訂は**本文修正のみ**で、YAML キー・型・辺のいずれにも触れていない（`edges:` は 2 本・`to`・`ref_version` とも不変）ため **z バンプ**。z バンプは伝播不要（DD-8 §4）であり、本ノードの被参照は実測 0 件でもある。

**対応状況**: open

**経緯**: PR #319（Issue #315）レビューの non-blocking 指摘（段落粒度）を起票する過程で、ガード対象文書を実地確認して発見した追加の指摘。レビューア指摘そのものではないため別ノードとして分離した。v0.1.1 は PR #326 のレビュー（`pr-reviewer`）で「既決の問いが未決の留保として残る」と指摘され（R-02）、その是正として改訂した。

**指摘時 ref_version**: `reconciliation-調停支援プロンプト-検証＋本ファイル転記` "0.1"（同 `.yaml` v0.1.2 時点）
**指摘時 ref_version**: `調停・本ファイル反映` "0.2"（同 `.yaml` v0.2.1 時点）
