**内容**: DD「識別子単位ノードは1ノード1YAMLを維持し本文は型別ポリシーで省略・共有を許可する」により、TD/TC/SRC の著作単位と本文方針が変わるが、現行テンプレート・test-strategy skill PROMPT・verification-author PROMPT は旧前提を含む。

**確認した事実**:

- `templates/verification/tc.body.md` は TC body に実装ファイルとテスト関数を書く前提である。
- `templates/verification/td.body.md` は1 TD body を単独 node body として扱う前提であり、shared body の `body_ref` を持たない。
- `test-strategy` PROMPT は TD/TC/TR 3層の出力形式を持つが、TD shared body と TC bodyless をまだ案内しない。
- `verification-author` PROMPT は TD/TC/TR/VERIFY/FND/DD/Q/PEND の著作支援を担うため、型別本文ポリシーに追随しないと新規著作が旧形式へ戻る。

**期待される解消**:

1. TD/TC/SRC テンプレートを型別本文ポリシーへ更新する。
2. test-strategy skill PROMPT を「1テストプログラム1 TD shared body、TD-TC 1:1、TC bodyless」へ更新する。
3. verification-author PROMPT を `body_ref` / `test` / `source` 属性に対応させる。
4. author/reconciliation validator 系プロンプトが `.md + .yaml` ペア固定を要求しないようにする。

**依存**: FORMAT/schema/validator の正本が決まった後に実施する。先にプロンプトだけ変更すると、著作結果を現行 validator が受け取れない。

**指摘時 ref_version**:
- `識別子単位ノードは1ノード1yamlを維持し本文は型別ポリシーで省略・共有を許可する` "0.1"
- `test-strategy-テスト戦略skillプロンプト-review-system-テーラリング済` "0.1"
- `verification-author-著作支援プロンプト-td-tc-tr-verify-fnd-dd-q-pend` "0.2"
- `reconciliation-validator-検証支援プロンプト-read-only-構造検証・validation_ok-rollback` "0.2"
- `reconciliation-調停支援プロンプト-検証＋本ファイル転記` "0.1"

**対応内容（2026-07-09）**:
- `doc-system-v2/templates/verification/td.*` を TD shared body 前提へ更新し、`body_ref.file` / `body_ref.anchor` をテンプレート化した。
- `doc-system-v2/templates/verification/tc.sidecar.yaml` を TC bodyless 前提へ更新し、`test.file` / `test.qualname` / `test.kind` をテンプレート化した。旧 `tc.body.md` は削除した。
- `doc-system-v2/templates/implementation/src.sidecar.yaml` を追加し、SRC bodyless の `source.file` / `source.qualname` / `source.kind` をテンプレート化した。
- `.codex/agents/doc-system-v2-authoring.toml`、`reconciliation-validator.toml`、`reconciliation.toml`、`authoring-fanout.toml` を、1ノード=1 YAML・本文は body policy の契約へ更新した。
- `.codex/agents/verification-author.toml` と `.agents/skills/test-strategy/SKILL.md` を、TD shared body、TC bodyless、TD-TC 1:1 の著作/検証方針へ更新した。
- required 型 author（requirements/spec/analysis/design）と `docidx-lookup` の v2 説明を、body policy 前提へ追随させた。
- 対応する PROMPT ノード本文（requirements-author / spec-author / analysis-author / design-author / test-strategy / verification-author / reconciliation-validator / reconciliation）を実体資産の新方針へ同期した。

**解消判定**: FORMAT/schema/validator と item 2 の SRC/TD/TC 規則が確定済みであり、著作テンプレート・主要 authoring PROMPT・reconciliation PROMPT が同じ body policy / `source.*` / `test.*` / TD-TC 1:1 前提を案内する状態になったため resolved 化する。
