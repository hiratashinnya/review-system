**status: decided**（2026-07-10 オーナー判断・選択肢A 採用）

> **辺の扱い**: 本 DD は decided。Q-2 の決定は既存の P→傘 SPEC マップを維持する現状追認であり、本 DD から未反映シグナルとして `DD→X` 義務辺を張る対象はない。被参照は昇格元 Q-2 の `→DD-23`（昇格辺）で確保され RULE-005（完全孤立）は生じない。先例（Q-6→DD-22、Q-5→DD-21、Q-3→DD-20）に倣い、本 DD は `edges: []` とする。既存の P→SPEC 辺は本決定で差し替えないため、対象 P ノードへの backref 追加も行わない。
> **指摘時 ref_version の記録（DD-3 制度）**: 本 DD は Q から昇格（DD であり FND でない）ため、FND のような指摘時 ref_version 本文記録は必須ではない。ただし Q-2 が保持していた open 義務辺の解消履歴として、関連 SPEC の当時 ref_version を下記「関連ノード（Q-2 由来・provenance）」に記録する。

**論点**（Q-2 より昇格・要約）: 分析層の DFD レベリング深化で生じたリーフプロセスの一部が、既存 SPEC に 1:1 対応を持たず、最も近い傘 SPEC へ暫定マップされていた。要件層 SPEC を即時細分化するか、傘 SPEC マップを正式に維持するかが判断待ちだった。

**決定**: **選択肢A（当面は傘 SPEC マップ維持・実害が出た時点で細分化）を採用**する。Sprint 1 では既存の P→傘 SPEC マップを正式許容し、実装・テスト設計で「傘 SPEC では一意にテストできない／責務が曖昧」という実害が顕在化した対象だけ、後続で対象リーフの SPEC を細分化する。選択肢B（要件層で即時細分化）は今回スコープ外、選択肢C（傘マップを永久固定して細分化しない）は不採用とする。

**根拠**:
- 本パスの主目的はスタンプ結合解消と DFD レベリング深化であり、要件層 SPEC の即時細分化は独立した大改訂になる。
- リーフ P→SPEC 必須辺（RULE-006）は傘 SPEC で満たせるため、傘マップ維持でも機械検証上の穴は生じない。
- 細分化の要否は、テスト設計・実装で「傘 SPEC では一意に検証できない」という実害が出た時点で対象ごとに判断する方が、過剰な SPEC 新設を避けられる。
- SPEC-29-1/29-2 についても同じ方針を適用し、当面は P-3-1 の傘 SPEC-29 マップで扱う。実害が出た場合だけ対象リーフへ `→SPEC-29-1` / `→SPEC-29-2` の細分化マップを設ける。

**関連ノード（Q-2 由来・provenance）**:
- `ステージ発火による検査制御` ref_version "0.3": P-5-1/5-2/5-3 など設定・ステージ制御周辺の傘 SPEC マップ判断。
- `出力形式と終了コード` ref_version "0.2": P-4-1/P-4-3 など違反統合・G 番号付与周辺の傘 SPEC マップ判断。
- `ノード埋め込みのパース` ref_version "0.3": P-1-6 検査ビュー射影周辺の傘 SPEC マップ判断。
- `グラフ網羅性点検の正常系` ref_version "0.1": SPEC-29-1/29-2 を P-3-1 の傘 SPEC-29 マップで扱う判断。

（辺の扱いは上記注記のとおり `edges: []`。上記は義務辺ではなく本文で追跡する provenance 参照であり、既存 P→SPEC マップ自体は本決定で不変。）

**影響範囲（A 採用時・本 PR で反映）**:
- `doc-system-v2/nodes/04-verification/q/open`: Q-2 を open から除去し、closed 配下へ移動。
- `doc-system-v2/nodes/04-verification/q/closed`: Q-2 を `status: closed` 化し、open 義務辺を削除して `→DD-23`（ref_version "0.1"）の昇格辺を付与。
- `doc-system-v2/nodes/04-verification/dd/decided`: 本 DD-23 を新設し、傘 SPEC マップ維持方針を在グラフ化。
- `doc-system-v2/00-dashboard.md` / `doc-system-v2/RECOMMENDED_PROCESSING_ORDER.md`: #157 完了と open Q 0 件を最小更新。

**接続規則変更チェック（FND-99 パターン）**: 本決定は `doc-system-v2/config.yml` の接続規則（`must_link_to` / `must_be_linked_from`）の追加・変更・削除を含まない。P→SPEC の規則自体は不変で、既存マップの粒度を「現時点では傘 SPEC で許容する」と決めるだけである。したがって接続マトリクス・ドキュメント一覧・author/skill 資産への接続規則伝播は不要。

**覆る場合の影響範囲**: 選択肢B（即時細分化）へ変更する場合、対象リーフごとに新規子 SPEC を起票し、P→SPEC 辺を傘 SPEC から子 SPEC へ張り替える。対象 SPEC の追加、親 FR/SPEC への接続、関連 TD/TC/TR と実装テスト設計の再調整が必要になる。選択肢C（永久固定）へ変更する場合、単一責務＝単一 SPEC の方針と逆行するため、カバレッジ粒度とテスタビリティの低下を別 DD/FND として扱う必要がある。
