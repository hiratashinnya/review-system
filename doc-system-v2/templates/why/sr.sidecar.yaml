# テンプレ: SR（ステークホルダー要求）。path: nodes/01-why/sr/{slug}.yaml
title: "[要求名]"
version: "0.1.0"
labels: []
scheduled: "sprint-1"   # 既定=current_phase を明示。別 sprint はオーナー承認時のみ（スケジュール独断禁止）
edges:
  # 必須（SR→VAL）: この要求が実現する価値命題（config must_link_to sr→val）
  - to: "[実現する VAL の slug]"
    ref_version: "0.1"
