# テンプレ: FR（機能要求）。path: nodes/02-what/fr/{slug}.yaml
# RULE-017: この FR を詳細化する SPEC に condition:normal が必要。
# RULE-018: failure/error の SPEC も推奨（無ければ WARNING）。
# suppress は #81 で正式サポート。RULE-018 を意図的に抑制する場合は
#   suppress: [RULE-018] ＋ suppress_reason: "抑制理由"（非空必須・理由は本文でなく属性）を追加する。
title: "[機能名]"
version: "0.1.0"
labels: []
scheduled: ""   # 既定=current_phase を設定。空/別 sprint はオーナー承認時のみ（スケジュール独断禁止）
edges:
  # 必須（FR→SR）: 詳細化するステークホルダー要求（config must_link_to fr→sr）
  - to: "[上流 SR の slug]"
    ref_version: "0.1"
