# テンプレ: TC（テストコード）。path: nodes/04-verification/tc/{slug}.yaml
# 実行後は TR ノードを作り結果を記録する（TR→TC）。
title: "[テスト名]"
version: "0.1.0"
labels: []
scheduled: ""   # 既定=current_phase を設定。空/別 sprint はオーナー承認時のみ（スケジュール独断禁止）
edges:
  # 必須（TC→TD・realizes）: 実現するテスト設計（config must_link_to tc→td）
  - to: "[実現する TD の slug]"
    ref_version: "0.1"
