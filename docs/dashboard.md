# ダッシュボード

議事録から抽出した未決事項とネクストアクションの運用ハブ。
新しい議論が出たら、ここを更新する。

最終更新：2026-06-04（設計側 高優先 Q4/Q7/Q12/Q13/Q18 を確定・intake設計08を追加）

## 🔥 ネクストアクション（次にやる候補）

| # | アクション | 目的 | 状態 |
|---|---|---|---|
| A1 | 評価基準ファイルのスキーマを実際に書く | サンプルで曖昧さを潰す | 🟡 叩き台あり（[schema/](schema/README.md)）→レビュー待ち |
| A2 | AI への入力設計を決める | 基準＋文書をどう渡すか / 長い文書・コードベースの扱い | 🟡 設計あり（[07](requirements/07-ai-input-design.md)）→レビュー待ち |
| A3 | MVP の線引き | 全機能のうち最初に何を作るか | 🟡 材料そろう（入力境界・LLM I/O 確定） |

## ❓ 未決事項（決めないと進めない論点）

| # | 論点 | メモ | 状態 |
|---|---|---|---|
| Q1 | 基準変更の承認は誰がやるか | 方向ゲートは「検出（機械・アクター不要）」と「誰が承認するか（actor 強制）」に分離。**MVP はアクターを区別しない**ため、ロール強制（管理者承認）は将来＝専用 RBAC とセット。MVP は「人間が確認して有効化」まで。Git ホスト権限には乗せない | 🟢 MVP 方針確定（強制は将来） |
| Q2 | determinism の判定をどう運用に乗せるか | 基準を書く人がメタ情報を宣言する運用で良いか | 未決 |
| Q3 | 自動修正サマリの粒度・revert 単位 | トランザクション境界をどう切るか。**実装候補：内部に一時ローカル git リポジトリを作り自動適用をコミット管理して revert**（外部 Git ホスト依存とは別物） | 未決（設計） |
| Q4 | 文書タイプの判定 | **AI 自動判定を既定＋手動上書き**。低確信時は手動を促す。誤判定＝誤基準なので上書きを常に効かせる（[08](requirements/08-intake-design.md)） | 🟢 確定 |
| Q5 | 技術スタック | 実装方法・AI モデル・ホスティング | 未着手 |
| Q6 | 無効化の責務の二重化 | 事実(基準)と好み(ポリシー)を混ぜない＋方向ゲート制で整理。緩めはポリシー/承認経由、完全オフは `enabled` に一本化 | 🟢 方針合意（明文化済み） |
| Q7 | LLM の id 誤付与のフォールバック | **第4区分「❓ 未分類」として surfacing**。自動仕分けせず、人の確認＋新ルール候補（育成）へ（[07](requirements/07-ai-input-design.md)/[02](requirements/02-evaluation-and-triage.md)） | 🟢 確定 |
| Q8 | `category` の語彙 | 自由記述だと集計がブレる。enum 化するか | 未決（A1派生） |
| Q9 | 承認者への「打ち上げ」条件と内容 | **設計（機構）＝方向検出の結果を人へ surfacing する通知**。運用パラメータ（バッチ/即時/しきい値/dedup/自動承認の線引き）は組織設定のデフォルトで、設計で詰めない＝運用ルール。本文差分は方向判定不能で量が出る点だけ留意 | 機構=設計 / 規則=運用 |
| Q10 | 同一 `doc_type×scope` の複数ファイル結合規約 | スコープ内は**対等な兄弟・順序なし**（順序での暗黙解決はしない）。各ルールに provenance（由来パス）を保持。同 id 衝突は「メタ＝決定的に同値判定／本文＝LLM が**矛盾の有無だけ**判定」し、**矛盾なし→共存・矛盾あり→親フォールバック＋警告**。LLM は方向ではなく整合性判定に使う（2軸原則を維持）。警告は人へ（Q9接続） | 🟢 方針合意（schema 明文化済み） |
| Q11 | `override`（特に `locked`）を宣言できるのは誰か | 3段継承サンプルで発覚。現仕様は「locked は org 宣言」だが team/project は自分の新ルールに override を付けている。継承ルールは宣言元のみ変更可／locked は段に関係なく不可侵／新規ルールの locked は org 限定か、を線引き | 未決（A1派生） |
| Q12 | 文書の入力粒度 | **関連ファイルをまとめて1レビュー**（バッチ単位・横断整合も見る）。アップロード基本（[08](requirements/08-intake-design.md)）。※回答中に I-13 参照コンテキストの欠落が発覚 | 🟢 確定 |
| Q13 | スコープの決定方法 | **MVP は org 単一スコープ**。team/project は継承機構のみ設計済みで非露出。明示選択 UI は MVP 後（[08](requirements/08-intake-design.md)） | 🟢 確定（MVP） |
| Q14 | 同一文書の再評価時の挙動 | イベントリスト(E11)で発覚。修正→再提出で前回との差分提示・重複指摘/重複自動修正の抑制。Q9（承認側 dedup）とは別の**レビュー側 dedup** | 未決（要設計） |
| Q15 | 基準/ポリシー更新の伝播 | イベントリスト(E12)で発覚。マージ後の再合成トリガ・矛盾チェックのキャッシュ無効化・過去文書への遡及再評価の有無 | 未決（要設計） |
| Q16 | 承認待ち滞留への督促 | イベントリスト(E13)で発覚。**機構（督促/ダイジェスト送出）は設計**、頻度・しきい値は運用デフォルト。出力 O-13・入力 I-12・設定 I-10 と接続 | 機構=設計 / 規則=運用 |
| Q17 | 異常系イベントの振る舞い | イベントリスト(E14)で発覚。基準パース失敗・LLM 障害・スコープ未解決・空文書での fail-close/open 方針とエラー通知（O-14） | 未決（要設計） |
| Q18 | 観点パックの肥大化対策 | **MVP は全部載せ**（doc_type で絞り済み）。膨張時の retrieval 的選別は MVP 後の最適化（[07](requirements/07-ai-input-design.md)） | 🟢 確定（MVP） |
| Q19 | 参照コンテキストの紐付け | Q12 回答で発覚（I-13）。上流成果物・前提条件を評価対象と別枠で入力。**MVP は手動添付・任意・複数可**。どの観点がどの参照を要求するか等の自動紐付けは将来（[08](requirements/08-intake-design.md)） | 🟡 MVP方針あり |

## 🗂 未決の分類と優先度

**区分**で「設計（＝システムが出す機構）」と「運用ルール（＝システムが前提とする条件・組織が設定するデフォルト）」を分ける。
**運用ルールは設計で詰めない**——機構だけ設計し、規則はデフォルト/前提として置く（2軸の運用側）。

| テーマ | Q# | 区分 | 優先 | メモ |
|---|---|---|---|---|
| 入力境界（文書→システム） | Q4 / Q12 / Q13 / Q19 | 設計（IF） | **高** | 🟢 確定（[08](requirements/08-intake-design.md)）。参照コンテキスト I-13 は MVP方針あり |
| LLM 出力の受け口 | Q7 / Q18 | 設計 | **高** | 🟢 確定（[07](requirements/07-ai-input-design.md)）。未分類=第4区分・観点パック全部載せ |
| 自動修正の機構 | Q3 | 設計（機構） | 中 | 適用範囲・revert（内部ローカル git 案） |
| 基準スキーマ細部 | Q8 | 設計 | 中 | category 語彙＝集計の厳密化 |
| 基準の上書き権限 | Q11 | 運用ルール | 中 | locked 宣言権＝ガバナンス前提 |
| 承認・ガバナンス | Q1 / Q9 / Q2 | 運用ルール | 中 | **機構のみ設計**、規則は組織のデフォルト。詰めない |
| ライフサイクル機構 | Q14 / Q15 / Q17 | 設計 | 低 | 再評価・伝播・異常系の機構 |
| 滞留督促 | Q16 | 運用ルール | 低 | 機構は設計、頻度は運用 |
| 実装 | Q5 | 設計 | 低 | A3（MVP 線引き）とセット |
| 合意済み（参照） | Q6 / Q10 | 設計 | — | 確定・明文化済み |

> 進め方の指針：**高（Q4/Q12/Q13/Q7/Q18）は全部「設計」**。ここを固める → A3（MVP 線引き）が最短。
> **運用ルール（Q1/Q9/Q11/Q16/Q2）は機構＋デフォルトに留め、設計で詰めない。**

## ✅ 決定済み（参照用）

- 社内ツールとして作る
- 評価基準は構造化 Markdown で外部ファイル化、Git 管理
- `文書タイプ × スコープ` で動的選択・継承
- 指摘は `深刻度 × 対応モード` で自動仕分け
- 自動化レベルはコンフィグ可能（決定論的＝ログのみ 〜 判断要＝人間）
- Human-only でも AI が原案提示
- auto-fix は自動修正サマリで可視化＋一括 revert
- 流し読みのフィードバックが基準・ポリシー育成に直結
- **設計（機構）と運用ルール（前提条件）を混ぜない**：設計は機構を出す（方向の検出・器・provenance・revert・AI I/O）。運用ルール（誰が承認・緩めの承認要否・督促頻度）は機構上のデフォルト/前提として置くだけで設計では詰めない（2軸の運用側）
- 上書きの**方向ゲート機構**：方向を機械検出し、厳しく=自由 / `locked`=機械拒否。「緩め＝承認」は運用デフォルト（前提条件）で機構とは別レイヤ
- 判定は常に**2軸で分ける**：システムの判定基準（順序ある属性=機械が自動ゲート）／運用ルール（本文・事実性=人が承認）
- 本文（観点）の上書き＝差し替え扱い（open以外は要確認・locked拒否）。観点/例の追加は別ファイルで union
- **MVP はアクターを区別しない**（単一ユーザー）。専用 RBAC も Git ホスト権限依存も採らない。役割ラベルは記述的。方向ゲートは「検出」まで機能し、「誰が承認するか」の強制は将来

> 各決定の詳細は [requirements/](requirements/) を参照。
> システムの入出力一覧は [requirements/05-io-overview.md](requirements/05-io-overview.md)、
> イベント駆動の俯瞰は [requirements/06-event-list.md](requirements/06-event-list.md)。
