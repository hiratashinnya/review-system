# ダッシュボード

議事録から抽出した未決事項とネクストアクションの運用ハブ。
新しい議論が出たら、ここを更新する。

最終更新：2026-07-29（**Codex 第4巡レビューの指摘10件を反映**：①**Q27 を新規起票**——`suggested_fix.diff` が「修正後ファイルの全内容」なのに `apply_auto` が finding ごとに全上書きするため、**同一ファイルに 2 件以上の 🤖 finding があると先行修正が黙って消える**（実測確認・`Success` が返るので気付けない）。衝突単位「同一 location」の要改訂を 05/03/13/00・Q20 へ横断注記、②A2 を ✅→🟡 へ後退（プロンプト雛形・ビルダーが未実装・`FilePlatformAdapter.review` は `pack`/`targets`/`references` を使わない＝**実装済み範囲は「有効な JSON の受理」と「単一 fix の全内容上書き」に限る**。07・04・07-system-prompts を同期）、③Q25/A9 の「既存 git 機構をそのまま再利用できる」を撤回（許可対象集合・基準点は**プロセス内 dict** で別 CLI プロセスからは再利用不能・`open` は `rmtree` するので再開に使えない → 永続化と非破壊な再開 API を A9 の要設計に追加）、④Q26 の作業範囲を是正（A-1＝**本番ロード結線を必須化**し CLI lint は択一でなく追加入口として A-5 へ分離／A-4 に **`load_policy` の例外境界**を追加／A-5 に **`LintError` が位置情報を保持できない**問題を追加）、⑤schema の検査実効性インベントリを**4分類（統制された拒否／未捕捉例外／無検査／黙って消える）× criteria・policy 別**へ網羅化、⑥「lint は合成時に毎回」を要件/将来設計と明記し未結線を併記（03/05）、⑦Q5a の文法説明を Q24=A 後（引用キー＋3段ブロック）へ更新し**規定上限と実装が受理する深さを分離**、⑧08 の版定数の利用関係を**版表示/criteria lint/未実装の policy lint/prompt 版スタンプ**の4本へ分離、⑨実装進捗表の**陳腐化したテスト件数を削除**（5行がずれていた・総数109件は実測で正）、⑩S6 版スタンプの責務所在を `io/cli`→**`core/pipeline.py:65-83`** へ訂正／**⑪2026-07-29 追補（オーナー指摘で分類是正）**：Q27 は「未決の論点」ではなく [Q3](#-未決事項決めないと進めない論点) 確定に対する**実装バグ**なので**クローズ**。実装方針＝**(A) baseline からの delta を 3-way マージ統合・MVP**（オーナー確定）を [A10](#-ネクストアクション次にやる候補) に起票し [#287](https://github.com/hiratashinnya/review-system/issues/287) で追跡。①の「実施はオーナー判断」は撤回——方針(A)は確定済みで、残るのは実装スケジュールのみ）
> 前回：2026-07-29（**Codex 第3巡レビューの指摘を反映**：①07 の `suggested_fix.diff` 例を実装契約＝**修正後ファイルの全内容**へ是正（前巡で unified diff 風に書いてしまった誤りを撤回）、②S5 lint の状態を**構文/値域＝実効・値レベル lint＝未結線・段数検査＝未実装・O-14 未達**の4項目へ分解し schema/dashboard/13/design を横断是正、③実装進捗表 #2 `parsing` と Q5・分類表の旧表記を同期、④Q25 の「増分は小さい／新規機構不要」を撤回し「既存 git 機構の再利用可否」と「F7 の前提設計の欠落」を分離、⑤Q26 の選択肢(A)に**作業範囲 A-1〜A-4** を明記——新たに判明した2ギャップ（`doc_type`/`scope` 欠落の無言スキップ・基準ロード失敗が O-14 にならない）を**別 Q を立てず本 Q に取り込み**、オーナー指示で確定）
> 前回：2026-07-28（**Codex 第二意見レビューの指摘を反映**：**Q26 を起票**（S5 lint 未結線）、A9 の「新規設計は不要」を撤回、`10-llm-system-boundary`・`05-io-overview` の I-15/L3 に MVP 印（#6 の5・6箇所目）、06 の凡例を F15 限定に是正、Q25 の状態併存を一本化。再レビューで自分の事実誤認（実 JSON 契約検証の不在）を撤回し 07 の JSON 例を実装型へ修正、A2 は完了へ復帰）

> ⚠️ **2026-07-27 追記（2026-07-29 更新）**：下記「実装進捗 13/13」は**層/モジュール単位の進捗**であり、**MVP 機能単位では3件が未完**——①**F7（✋/💬 人手適用・P2 差別化）が未実装**（→ **[Q25](#-未決事項決めないと進めない論点)**。方針は**決定＝A でオーナー確定済み**・実装が未着手＝[A9](#-ネクストアクション次にやる候補)）、②**S5 の値レベル lint が本番経路に未結線・入れ子段数検査が未実装**（→ **[Q26](#-未決事項決めないと進めない論点)**・オーナー未決）、③**🤖 自動修正が同一ファイル複数 finding で後勝ち上書きする（衝突検出・統合が未実装）**（→ **[Q27](#-未決事項決めないと進めない論点)**＝未決の論点ではなく **Q3 確定に対する実装バグ**と分類是正しクローズ・**方針＝(A) 統合・MVP でオーナー確定済み**・実装が未着手＝[A10](#-ネクストアクション次にやる候補)／[#287](https://github.com/hiratashinnya/review-system/issues/287)）。したがって「MVP(P1+P2)実装が完了」は現時点で正確でない。

> **スコープ注記**：本ダッシュボードは `review_system`（レビューツール本体）のみを対象とする。本リポジトリには `doc_system`（`doc-system-v2/`・`dsv2/`・`asset_parity/`・`.codex/` 等の仕様策定支援ツール群）も同居するが対象外。2プロジェクトのファイル構成・正本の所在は [`.claude/rules/07-project-structure.md`「このリポジトリ＝2つのプロジェクトが同居（混同注意）」](../.claude/rules/07-project-structure.md) 参照。review_system については **`docs/` 配下が実質的な正本**（doc-system-v2 コーパスに review_system 固有の要件/設計ノードは存在しない）。

## 🎯 MVP ターゲット（確定）

**MVP ＝ [12-mvp-scope](requirements/12-mvp-scope.md) の P1 ＋ P2。**

- **P1 コア（不可分A）**：受付 → 基準合成(org) → AI評価 → 検証・仕分け → レポート（＋未分類）。
  ＝「文書を出すと、観点に沿った指摘が 🤖/✋/💬/❓ に仕分けられたレポートが返る」。
- **P2 差別化**：✋/💬 人手適用 →（不可分B）🤖 自動修正＋revert。
  ＝「直してくれて、いつでも戻せる」。
- **前提（P0）**：観点ファイル＋ポリシーを書く（少数 doc_type）／決定的ツール群＋プロンプト雛形（Claude が LLM 役）。
- **MVP 外**：合成時警告(F11)・育成ループ(F12/13)・AI型推定(F15)・参照突き合わせ(F10)・異常系(F16)・team/project scope(F17)。

> 開発着手の前に、ここまでの設計プロセスを**資産化**（スキル/エージェント化）する → [methods/](methods/method-inventory.md)。

## 🔨 実装進捗（MVP・TDD）— 全13層のモジュールは実装済み（機能単位では未完）

> **13/13 行のモジュールが実装済み**（`review_system/` 全モジュール・`tests/unit/` の review_system 対象テスト109件が全パス・2026-07-29 実測）。
> ⚠️ ただしこれは**層/モジュール単位**の進捗で、**MVP 機能単位では3件が未完**：
> **F7（✋/💬 人手適用）が未実装**＝[Q25](#-未決事項決めないと進めない論点)／
> **S5 の値レベル lint が本番経路に未結線＋入れ子段数検査が未実装**＝[Q26](#-未決事項決めないと進めない論点)／
> **同一ファイルに複数 finding があると 🤖 自動修正が後勝ちで先行修正を消す（衝突検出・統合が未実装）**＝[Q27](#-未決事項決めないと進めない論点)。
> 各層 commit→テスト→成績書(commit id)→push。証跡は `tests/{cases,reports,logs}`。次に着手する場合は「MVP実装完了後」節を参照。
>
> 📝 **本表に個別テスト件数は持たない**（2026-07-29）。旧版は TC ごとの件数を括弧書きしていたが、
> テスト追加のたびに陳腐化し実際に5行がずれていた（例：`parsing` は 19 と書かれていたが実測 24）。
> 件数は `python3 -m unittest discover tests/unit -v` で都度確認すること（TC id と対応する成績書は `tests/reports/`）。

| # | 層/モジュール | 内容 | 状態 |
|---|---|---|---|
| 1 | `domain`（基盤） | enums・result・ids・review・intake | ✅ TC-domain-001 |
| 2 | `parsing` | 自前 mini-YAML パーサ（`frontmatter.py`）＋値レベル lint（`lint.py`） | 🟡 **モジュールは実装済み・TC-parsing-001**／⚠️ 機能単位では未完＝**①構文パーサは本番経路で実効**（サブセット外は `MiniYamlError`）／**②`lint.py` の値レベル lint は本番経路から呼ばれていない（未結線）**／**③入れ子段数の検査は未実装** ＝ [Q26](#-未決事項決めないと進めない論点) |
| 3 | `domain/criteria`＋policy 型 | RuleMeta/ComposedRule/CriteriaPack/MetaIndex/PolicyMatrix/TriagedFinding 等 | ✅ |
| 4 | `ports` | PlatformPort（Protocol）。Repository は persistence 直実装で代替 | ✅ |
| 5 | `core/triage`（P4） | rule_id検証(S1)→参照除外→mode判定／未宣言は HUMAN_ONLY(S2) | ✅ TC-triage-001 |
| 6 | `core/compose`（P2・org最小） | org 読込→pack/meta 組立（方向ゲート/矛盾は post-MVP印） | ✅ TC-compose-001（純粋部） |
| 7 | `core/intake`（P1） | 対象/参照集合・型確定（org固定・手動型・AI推定は MVP外） | ✅ TC-intake-001（純粋部） |
| 8 | `ports`＋`adapters/fake` | PlatformPort/能力宣言＋FakePlatformAdapter（テスト seam） | ✅ |
| 9 | `core/evaluate`（P3）＋`core/pipeline`（P1通し） | PF呼び→検証→仕分け→レポート＋版スタンプ。fail-close 直列 | ✅ TC-pipeline-001（e2e） |
| 10 | `persistence`（criteria/policy ローダ） | parser拡張(Q24=A)＋criteria_repo（.md→ComposedRule・policy→PolicyMatrix）。git/ledgers は P2 で | ✅ TC-repo-001／TC-parsing に追加分 |
| 11 | `core/apply`（P5）＋report＋CLI配線 | HTMLレポート＋内部git適用(S4)＋revert＋feedback(DS5)。`review`/`revert`/`feedback` は **レポートのパスだけ**(DD10/14)。実PF=FilePlatform(findings.json) | 🟡 **モジュールは実装済み・TC-cli-002**／⚠️ 機能単位では未完＝**衝突検出・統合が未実装**。`apply_auto` が finding ごとに全内容を書くため、**同一ファイルに 2 件以上の 🤖 finding があると先行修正が後勝ちで消える**（しかも `Success` を返すので気付けない）＝ [Q27](#-未決事項決めないと進めない論点) |
| 12 | `io/cli`（合成ルート） | `reviewer version`（版定数表示）・`review`→HTML。`python -m review_system` 実起動可 | ✅ TC-cli-001 |
| 13 | e2e（CLI・FakePlatform） | `python -m review_system review …`→HTML 実生成。version も実行可 | ✅ TC-cli-001 |

> **MVP外（実装しない・PR8 印）**：合成時警告(F11)・育成ループ(F12/13)・AI型推定(F15)・参照突き合わせ(F10)・異常系degrade(F16)・team/project scope(F17)。

> **テスト証跡の粒度に差あり（要フォロー）**：`tests/cases/`・`tests/reports/` の TC/TR Markdown は #1〜#7 相当（domain/parsing/triage/pipeline/repo/cli）の6件のみ。#5後半〜#13 で追加された `adapters/guard.py`・`persistence/workspace_git.py`・`core/apply.py`・`persistence/feedback_store.py`・`prompts/registry.py`・CLI の `revert`/`feedback` サブコマンド等は `tests/unit/`（unittest・全パス）はあるが、対応する TC/TR Markdown 成績書が無い。test-strategy の3点セット（TD/TC/TR）運用に照らすと成果物としては未整備 → A7 参照。

## 🔥 ネクストアクション（次にやる候補）

| # | アクション | 目的 | 状態 |
|---|---|---|---|
| A1 | 評価基準ファイルのスキーマを実際に書く | サンプルで曖昧さを潰す | 🟡 **記法は確定／S5 lint は一部のみ実効**（[Q26](#-未決事項決めないと進めない論点)・2026-07-28 後退・2026-07-29 精緻化）：記法とパース経路は実装済み・運用中（[schema/](schema/README.md)＝`parsing/frontmatter.py`+`persistence/criteria_repo.py` が準拠しテスト済）。**構文サブセット外と値域違反は実行前に拒否されるが、`parsing/lint.py` の値レベル lint は本番経路から呼ばれず、入れ子段数の検査は未実装**＝Q26。**schema/README.md の見出しも同表現へ更新済み** |
| A2 | AI への入力設計を決める | 基準＋文書をどう渡すか / 長い文書・コードベースの扱い | 🟡 **契約は確定／実装は一部のみ**（2026-07-29 に「✅ 完了」から後退・Codex 第4巡レビューで過大表示が判明）。**確定＝[07](requirements/07-ai-input-design.md) の入出力契約**（入力の構成・出力 JSON スキーマ・`suggested_fix.diff`＝修正後ファイルの全内容）。**実装済み＝有効な findings JSON の受理**（`adapters/file_platform.py`）**と単一 fix の全内容上書き**（`core/apply.py`→`persistence/workspace_git.py`）で、実 JSON の契約検証テストもある（`tests/unit/test_cli_p2.py`）。**未実装＝プロンプト雛形本体・ビルダー（`prompts/registry.py` は版定数のみ・`prompts/templates/` も `ReviewPromptBuilder` も存在しない）／`FilePlatformAdapter.review` は `pack`/`targets`/`references` を一切使わない（＝入力を渡す経路がコードに無い・PF 駆動 MVP の実体）／`diff` が本当に全内容かを検査する制御／同一ファイル複数 finding の衝突検出**（＝[Q27](#-未決事項決めないと進めない論点)）。**07 の JSON 例が実装型 `SuggestedFix{description, diff}` と食い違っていた誤記は修正済み**|
| A3 | MVP の線引き | **確定：MVP＝P1＋P2**（[12-mvp-scope](requirements/12-mvp-scope.md)）。方向性：決定的ツール群（MCP）＋プロンプト雛形を作り、Claude が LLM 役で回す（[11](requirements/11-platform-adapter.md)） | 🟢 線引きは確定・**P1+P2 は下表13/13 の層/モジュール単位で実装済み**。⚠️ ただし**機能単位では F7（✋/💬 人手適用・P2 差別化）が未実装**＝[Q25](#-未決事項決めないと進めない論点)／[A9](#-ネクストアクション次にやる候補)、**S5 の値レベル lint も未結線**＝[Q26](#-未決事項決めないと進めない論点)、**🤖 自動修正が同一ファイル複数 finding で後勝ち上書き**＝[Q27](#-未決事項決めないと進めない論点) |
| A4 | これまでの設計プロセスを資産化 | 案出し・イベントリスト・点検・価値分析などの手順/基準をスキル・エージェント化（[methods/](methods/method-inventory.md)） | ✅ 完了（[asset-plan.md](methods/asset-plan.md) に「実装済み」と明記・`.claude/skills`・`.claude/agents` 実体あり） |
| A5 | **実装前の凍結セット（8項目）を固める** | A モジュール構成・②外部IF・B PF駆動プロトコル・C 永続層・①オーケストレーション(スイムレーン)・③システムプロンプト・D ログ/版・④テスト戦略。索引＝[design/README](design/README.md)。判断ログ＝[design/decisions](design/decisions.md) | ✅ 8項目確定（[design/README](design/README.md) 全項目 ✅・DD1–DD23）→ **実装フェーズは下表13/13（層/モジュール単位）**。⚠️ 凍結セットで設計済みの `approve`/`decide`（[03](design/03-external-interfaces.md)）は未実装＝[Q25](#-未決事項決めないと進めない論点)／[A9](#-ネクストアクション次にやる候補)、同じく設計済みの `criteria lint`（同 :47）も未実装＝[Q26](#-未決事項決めないと進めない論点)、設計済みの**衝突解決**（[05 DS3](design/05-persistence.md)・[Q20](#-未決事項決めないと進めない論点) 2段構え）も未実装＝[Q27](#-未決事項決めないと進めない論点) |
| A6 | **post-MVP スコープの着手判断** | ⚠️ **前提注意**：MVP は**機能単位では未完**（F7 未実装＝[Q25](#-未決事項決めないと進めない論点)・[A9](#-ネクストアクション次にやる候補) が 🔴 最優先）。本アクションは **A9 完了後**に判断するのが順序。次の優先度をオーナーが決める：F10 参照突き合わせ／F11 合成時警告／F12+F13 育成ループ／F14 ひな形生成／F15 AI型推定／F16 異常系degrade／F17 team/project scope（いずれも [12-mvp-scope](requirements/12-mvp-scope.md) で P3/将来）。現状どれも未着手 | 🟡 未着手・オーナー判断待ち |
| A7 | **TC/TR 成績書の後追い整備** | `guard`・`workspace_git`・`apply`・`feedback_store`・`prompts/registry`・CLI `revert`/`feedback`/P2系（`test_apply.py`/`test_workspace_git.py`/`test_guard.py`/`test_cli_p2.py`/`test_compose_intake.py`/`test_criteria_repo.py`/`test_pr_fixes.py`）はコード＋unittestのみで `tests/cases`/`tests/reports` の Markdown 成績書が無い。**2026-07-27 実測**：unit 12本 vs `cases` 4／`reports` 6／`logs` 6 | 🟢 **方針確定＝全テストに3点セットを整備する**（オーナー確定 2026-07-27。「代表ケースのみ＋CI 生成に寄せる」案は**不採択**＝`/test-strategy` の現行運用を維持）。実作業は [#11](https://github.com/hiratashinnya/review-system/issues/11) へ一本化・未着手 |
| A8 | ~~**F7（✋/💬 人手適用）の実装可否の明確化**~~ | ~~記録から読み取れない → オーナー確認候補~~ **2026-07-27 調査で決着**：設計側は F7 を**明示的に設計済み**（[03](design/03-external-interfaces.md) の CLI 表に `reviewer approve <report>`（P5.2 ✋・I-6）・`reviewer decide <report>`（P5.2 💬・I-6）、関数契約 `cmd_approve`/`cmd_decide`、[DD10](design/decisions.md) も `decide`/`feedback`/`approve` の3コマンドを前提）。一方**実装は `version`/`review`/`revert`/`feedback` の4コマンドのみ**（`review_system/io/cli.py:49-63`）で `approve`/`decide` が無く、`core/apply.py` も `apply_auto`（🤖 バケツ専用）のみ＝**✋/💬 の適用経路が存在しない**。`feedback` は DS5 への記録だけで適用しない。→ 「系外と判断した」のではなく**単なる未実装**と確定 → **Q25 へ移送** | 🔴 矛盾確定 → [Q25](#-未決事項決めないと進めない論点) |
| **A9** | **F7（✋/💬 人手適用）の実装** | [Q25](#-未決事項決めないと進めない論点) 決定＝A に基づく実装タスク。`reviewer approve <report>` / `reviewer decide <report>` を追加し、既存の適用機構（DS3 内部 git・finding 単位コミット・revert＝`persistence/workspace_git.py`・`core/apply.py`）を ✋/💬 バケツへ再利用する。**コマンドの外形**は [03](design/03-external-interfaces.md):43-44,79-80・[DD10](design/decisions.md) に既存。ただし**「新規設計は不要」は誤り**（2026-07-28 Codex 第二意見レビューで判明・実装を確認）＝**適用に必要な設計が3つ欠けている**：①`feedback.json` は `{finding_id, decision}` しか持たず（`io/report_html.py` の書き出し JS）、**どう直すか（`suggested_fix`）を保持していない**ためレポートから修正内容を復元できない。②ワークスペースは `core/apply.py` の `apply_auto` 内でしか `workspace.open()` されないため、**🤖 対象が0件だと workspace 自体が作られず**、`approve`/`decide` の適用先が存在しない。③**プロセスをまたぐワークスペース状態が無い**（2026-07-29 追加）——`GitWorkspaceRepository` の書込許可対象集合と基準点コミットは `__init__` で作る**プロセス内 dict**（`workspace_git.py:37-38`）で永続化されないため、別プロセスの `approve`/`decide` からは**必ず `ValueError`（対象集合に無いパス）で弾かれる**。かつ既存の `open` は冒頭で `shutil.rmtree` するため**再開に使うと `review` のコミット列ごと消える**。→ **許可対象集合・基準点・コミット列の永続化（または git 履歴からの安全な再構成）と、既存作業場を破壊しない再開 API** が要る。→ **修正案・選択結果の保存形式／workspace の生成時点／ワークスペース状態の永続化と再開 API を A9/#7 で設計する**必要がある。**前提**＝[#7](https://github.com/hiratashinnya/review-system/issues/7)（`decide` の入力＝💬 の「決定＋任意の修正欄」が現 UI に無い）の設計決定が先行 | 🔴 **未着手・最優先**（2026-07-27 起票） |
| **A10** | **🤖 自動修正の後勝ち上書きを直す（Q3 違反の実装不具合）** | [Q27](#-未決事項決めないと進めない論点) の実装タスク。**オーナー決定＝統合・MVP**（2026-07-29）。`suggested_fix.diff` が「修正後ファイルの全内容」なので、`commit_fix` が `write_text` で丸ごと上書きすると**同一ファイルの先行 finding の修正が消える**。**直し方＝baseline からの delta を現在のファイルへ当てる**：`delta_i = diff(baseline, fix_i.diff)` を求め、共通祖先＝baseline の**3-way マージ**で現在のファイルへ適用する（`_baseline` は `persistence/workspace_git.py:37-38` に既存。git を `subprocess` で呼ぶ既存方針＝[Q5](#-未決事項決めないと進めない論点) と整合し標準ライブラリのみの制約も守れる）。**delta が重ならなければ決定的に合成でき、重なったときだけ [Q20](#-未決事項決めないと進めない論点) の2段構え（LLM マージ→迷いは人）へ回す**。この形なら**finding ごとに1コミットのまま各コミットの内容がその finding の修正だけ**になり、[Q3](#-未決事項決めないと進めない論点)（finding 単位コミット）が初めて実装で守られる。**波及**＝衝突単位の定義を「同一 location」→「**同一ファイル**」へ改める（[05 DS3](design/05-persistence.md)・[03](design/03-external-interfaces.md)・[13 S4](requirements/13-stabilization.md)・[00 データ辞書](design/00-data-dictionary.md) の `FixConflict`・[Q20](#-未決事項決めないと進めない論点)）。テストは `tests/unit/test_apply.py` に「同一ファイル2 finding」のケースが**無い**ので追加が要る。実作業は [#287](https://github.com/hiratashinnya/review-system/issues/287) へ一本化 | 🔴 **未着手**（2026-07-29 決定・#287 で追跡） |

## ❓ 未決事項（決めないと進めない論点）

| # | 論点 | メモ | 状態 |
|---|---|---|---|
| Q1 | 基準変更は誰がやってよいか | 個々の上書きの可否は override で**機械判定（承認ステップ無し）**。人間の確認が要るのは「基準ファイル自体を編集・確定する行為」だけ。**MVP はアクターを区別しない**（単一ユーザー・人間確認まで）、ロール強制（RBAC）は将来。Git ホスト権限には乗せない | 🟢 MVP 方針確定（強制は将来） |
| Q2 | determinism の判定をどう運用に乗せるか | **クローズ**：determinism は基準作者が**フロントマターで宣言する属性**（[schema](schema/README.md)）で、⑥仕分けが決定的に消費（`rule_id→determinism×severity→モード`）。別途の運用機構は不要＝2軸どおり（順序属性＝機械ゲート）。生成側の主体は Q21。安全側デフォルトは S2（未宣言は人間側へ） | 🟢 クローズ（schema/2軸に吸収） |
| Q3 | 自動修正サマリの粒度・revert 単位 | **revert 単位＝指摘(finding)単位でコミット**（finding id＝rule_id＋location）。個別 revert も実行ぶん一括も、対象コミット群の revert で成立（一括は操作）。実装：内部に一時ローカル git（外部ホスト依存とは別物） | 🟢 方針確定 |
| Q4 | 文書タイプの判定 | **AI 自動判定を既定＋手動上書き**。低確信時は手動を促す。誤判定＝誤基準なので上書きを常に効かせる（[08](requirements/08-intake-design.md)）。🔖 **ただしこれはフル論理**＝**MVP は手動 `--type` のみ**（`reviewer review --type` 必須・未指定は `EXIT_BADREQ`）で、**AI 型推定＝I-15 は F15＝post-MVP**（[12-mvp-scope](requirements/12-mvp-scope.md)・#6） | 🟢 確定（**MVP 印あり**＝実装は手動のみ） |
| Q5 | 技術スタック | **確定：アプリ本体＝Python・原則標準ライブラリのみ**（外部依存は最小）。AI レイヤは外部 PF へ委譲済み（[11](requirements/11-platform-adapter.md)）。git は `subprocess` で CLI 呼び。**Q5a 確定＝自前の最小フロントマターパーサ**（stdlib のみ・PyYAML 不採用）：対応文法を**意図的に小さく固定**し、**範囲外は S5 lint で fail-close**。**パーサが検証器(S5)を兼ねる**。対応文法の正本＝[schema](schema/README.md#対応フロントマター文法自前パーサが読むサブセットq5a)。<br>📝 **文法の記述を更新（2026-07-29・Codex 第4巡レビュー）**：旧記述「フラット key:value ＋単純リスト＋override の1段ネスト」は **[Q24](#-未決事項決めないと進めない論点)＝A（パーサ拡張）決定前の旧仕様**なので撤回。現在の対応サブセットは **①マッピング `key: value`（キーは小文字スネーク **または引用文字列キー** `"*"`/`'…'`＝Q24=A で追加）／②ブロック列 `- `（フロー `[]`/`{}` は非対応）／③スカラ（素文字列・`"…"`・`'…'`・10進整数・`true`/`false`/`null`）／④行頭・行末コメント／⑤インデントは半角スペース2刻みのみ（タブ禁止）**。<br>⚠️ **規定上限と実装が受理する深さは別物**：*規定*では **policy の `matrix`（`matrix:` → determinism → severity → mode）で使う3段ブロックネストを上限**とし、4段以上は弾く建付け（[schema](schema/README.md)「入れ子の上限」）。しかし *実装*（`parsing/frontmatter.py` の `_parse_block`/`_parse_mapping` 再帰）は**深さ任意で受理**し、段数を数える検査そのものが無い（同ファイル docstring に「深さ任意」と明記）＝[Q26](#-未決事項決めないと進めない論点) A-2。<br>⚠️ **実装状況（2026-07-29 精緻化）**：「パーサ＝検証器」は**構文サブセットの範囲でのみ実効**（`parsing/frontmatter.py` が `MiniYamlError`）。**値レベルの lint（必須キー・`extends`・`id` 一意性・`version` MAJOR）は `parsing/lint.py` にあるが未結線**、**入れ子段数の検査は未実装**、**policy 側は lint 関数自体が無い**、**拒否は未捕捉例外で O-14 形式ではない** ＝ [Q26](#-未決事項決めないと進めない論点) | 🟢 確定（Q5a＝自前パーサ）／⚠️ 実効範囲は Q26 |
| Q22 | PF 能力差の吸収 | 当面はファイル適用・ツール実行の2能力をシステム代替できれば十分（気にしすぎない）。別途、**自前実装の方がコントロールしやすい範囲が無いかを調査**＝[11](requirements/11-platform-adapter.md) に build-vs-delegate のたたき台 | 🟡 当面OK／要調査 |
| Q23 | 双方向の口の境界・順序ガード | PF→System の実装は **CLI＋stdout でシステムが制御フローを流し順序を握る**（起動プロンプトは「起動方法＋入力＋指示に従え」の最小）。どの関数を公開するか・適用前検証の強制は本番でツール側ガード。MVP は stdout 指示で軽量に担保（[11](requirements/11-platform-adapter.md)） | 🟡 MVP方針あり |
| **Q24** | **ポリシー記法 ↔ パーサ非対応（矛盾）** | [schema](schema/README.md) の policy 例が**フロー `{ "*": mode }`＋`*` キー**で、Q5a の mini-YAML サブセット（フロー/`*`/3段ネスト非対応）で**読めない**。実装で発覚。原案＝**(B) policy を `determinism: mode` のブロック平坦形に再設計**（MVP の `PolicyMatrix` は既に determinism→mode で severity 不使用＝損失なし／パーサ拡張不要）。代替＝(A) パーサ拡張（引用キー＋3段ブロック・Q5a を広げる）。**推奨 B**。要オーナー判断 | 🟢 **決定=A（パーサ拡張）**：引用キー("*")＋3段ブロックネストを mini-YAML に追加（flow は非対応のまま）。schema の policy 例はブロック形に書き換え。DD16 | 
| **Q25** | **F7（✋/💬 人手適用）未実装 ↔「MVP(P1+P2)実装完了」宣言の矛盾** | **矛盾**：[12-mvp-scope](requirements/12-mvp-scope.md) は **MVP＝P1＋P2** と定め、F7「✋/💬 人手適用＝**承認/決定した修正がコードに反映される**」を P2 差別化の構成要素として明記（同 :26, :96「F7（承認付き適用＝低リスクで適用機構を作る）→ F6＋F8」）。設計も [03](design/03-external-interfaces.md) で `reviewer approve <report>` / `reviewer decide <report>` と `cmd_approve`/`cmd_decide` を定義済み。**しかし実装に両コマンドが無く**（`io/cli.py` は `version`/`review`/`revert`/`feedback` のみ）、`core/apply.py` は `apply_auto`（🤖 auto バケツ専用）だけで**✋/💬 を適用する経路が存在しない**（`feedback` は `feedback.jsonl` への記録のみ）。よって本ダッシュボードの「MVP(P1+P2)実装が完了」は**機能単位では成立していない**。<br>**選択肢**：**(A) F7 を実装して MVP を閉じる**（`approve`/`decide` を追加し、既存の適用機構＝`workspace_git`＋finding 単位コミット＋revert を ✋/💬 バケツへ再利用する。**ただし「増分が小さい」は誤り**＝下記「実装規模の実態」参照）／(B) F7 を post-MVP へ降格し、12-mvp-scope・03・DD10 に MVP 印を入れて実装と整合させる（PR8 フル論理＋MVP印）／(C) 現状維持（記録のみ）。<br>**推奨 = A**。根拠：MVP の価値主張は「直してくれて、**いつでも戻せる**」＋「✋/💬 に仕分けたものが**反映される**」で、F7 を落とすと仕分け 4 区分のうち人手側 2 区分が**出口を持たない**（PR6「価値経路を遮断しない」に反する）。**(B) は価値経路の切断を仕様側で追認する**ことになるため非推奨。<br>**実装規模の実態（2026-07-29 訂正・旧記述「増分は小さい／新規機構は不要」を撤回）**：**「既存 git 機構を再利用できる」ことと「F7 の前提設計が揃っている」ことは別**。<br>・**再利用できる部分（事実）**：`persistence/workspace_git.py` の**メソッド群そのもの**（`open`/`commit_fix`/`rollback_execution`/`revert`）と finding 単位コミット・revert の**考え方**は F6/F8 で完成済みで、✋/💬 でも同じ機構を使える。<br>・**⚠️「既存 git 機構をそのまま再利用できる」は撤回（2026-07-29・Codex 第4巡レビュー）**：`approve`/`decide` は `review` とは**別の CLI プロセス**で後から走る。ところが `GitWorkspaceRepository` の**書込許可対象集合 `self._targets` と基準点コミット `self._baseline` は `__init__` で作られる素の dict＝プロセス内メモリ状態**で、どこにも永続化されない（`workspace_git.py:37-38`）。後続プロセスで `GitWorkspaceRepository(WORKSPACE)` を作り直しても両方とも空なので、`commit_fix` は `self._targets.get(exec_id.value, set())` に無いパスとして**必ず `ValueError` で弾き**、`rollback_execution` も基準点が無く no-op になる。**既存の `open` も再開には使えない**——`open` は冒頭で `shutil.rmtree(wd)` して作業場を作り直す設計（残骸排除・N1）なので、呼べば `review` が積んだコミット列ごと消える。<br>・**したがって A9 には次が追加で要る**：①**許可対象集合・基準点・既存コミット列の永続化**（または作業場の git 履歴からの安全な再構成）、②**既存作業場を破壊しない再開 API**（`open` とは別の「既存 workdir に attach する」口。`.review-state/<review_id>.commits.json` に近い置き場が既にあるので、そこに寄せるのが素直）。<br>・**欠けている前提設計（A9 で判明・要設計）**：①**保存形式**——`io/report_html.py` の書き出し JS は `{finding_id, decision}` しか出さず、**`suggested_fix`（＝適用すべき内容）も評価対象ファイルの中身も持たない**ため、レポート/`feedback.json` だけからは修正を復元できない。②**workspace の生成時点**——`workspace.open()` は `core/apply.py` の `apply_auto` 内でしか呼ばれず、`io/cli.py` はさらに `if report.auto:` でガードしている。**🤖 が0件ならワークスペースが作られず**、後から走る `approve`/`decide` には適用先も基準点コミットも存在しない。<br>→ したがって F7 は「バケツの入れ替え」では済まず、**修正案・選択結果の保存形式**・**ワークスペースのライフサイクル（誰がいつ open するか）**・**プロセスをまたぐワークスペース状態の永続化と非破壊な再開 API** の**新規設計が要る**。これらは [#7](https://github.com/hiratashinnya/review-system/issues/7)／[A9](#-ネクストアクション次にやる候補) で設計する。<br>**波及**：A8 は本 Q へ移送。[#7](https://github.com/hiratashinnya/review-system/issues/7)（HTML フィードバック UI の決定肢＝`modify`/修正欄・✋/💬 の UI 分離）が**前提論点として先行**する（`decide` の入力は 💬 の「決定＋任意の修正欄」で、現状 UI にその欄が無い）。 | 🟢 **決定＝A（F7 を実装して MVP を閉じる）**（オーナー確定 2026-07-27）。実装は未着手＝[A9](#-ネクストアクション次にやる候補) で追跡。前提＝#7 の設計決定 |
| **Q26** | **S5 事前 lint が本番経路に結線されていない** | **事実**：`review_system/parsing/lint.py` の `lint_criteria` は**本番経路のどこからも呼ばれていない**（grep 済み）。[schema](schema/README.md) が「書いたら実行前 lint で停止」と規定する検査が、**S5/O-14 の統制された経路として通っていない**。<br>**正確化（2026-07-28 再レビュー／2026-07-29 に項目別へ分解）**：<br>「実行時に強制されない」と一括りにするのは誤りで、**4項目に分かれる**。<br>・**(1) 構文サブセット外**（フロー/アンカー/複数行/タグ/マージ/複数ドキュメント/タブ・奇数インデント/閉じ `---` 欠落）＝**実効**。`parsing/frontmatter.py` がパース時に `MiniYamlError`。<br>・**(2) `override`/`severity`/`determinism` の値域**＝**実効**。`persistence/criteria_repo.py:33-35` の `Determinism(...)`/`_SEVERITY[...]`/`OverrideRule(...)` が `ValueError`/`KeyError`。**「強制されない」は不正確**。<br>・**(3) 必須キー（`doc_type`/`scope`/`rules`・`rules[].id`）・`id` 一意性・`extends` 先の存在・`version` の MAJOR 対応**＝**未結線**。`lint.py` に実装はあるがテストからしか呼ばれない。しかも `doc_type`/`scope` 欠落は `discover_criteria` の完全一致フィルタで**黙って対象外**、`rules` 欠落は**空扱い**＝「黙って空扱いにしない」に反する。<br>・**(4) ネスト段数上限＝検査そのものが存在しない**（`frontmatter.py` は入れ子を深さ任意で読む）。**lint を結線しても効かない**＝要新規実装。<br>・**共通**：(1)(2) の拒否は `core/pipeline.py` の `deps.load_criteria(...)` が try/except に包まれていないため**未捕捉例外**として上がり、**O-14 通知になっていない**。<br>・**(5) policy 側は lint 関数そのものが無い**（2026-07-29 追加）：`lint_criteria` に対応する `lint_policy` は存在せず、`SUPPORTED_POLICY_MAJOR` は `reviewer version` の**表示にしか使われていない**。`load_policy_file` は `version` を読みも検査もせず、`matrix` 行の severity キーも語彙照合しない（typo は `resolve` で引けず S2 により**黙って `HUMAN_ONLY`** に倒れる＝ポリシー無視に気付けない）。`matrix` キー自体が無ければ空マトリクスとして**黙って全件人間側へ**倒れる。項目別の一覧＝[schema](schema/README.md) 冒頭の検査実効性インベントリ。<br>・**結線先の口も無い**：設計 [03](design/03-external-interfaces.md):47 の `reviewer criteria lint` は未実装（`io/cli.py` は `version`/`review`/`revert`/`feedback` の4サブコマンドのみ）。**ただし「本番ロードで呼ぶ」と「CLI 口を作る」は択一ではない**（2026-07-29 是正・**同日オーナー承認済み**——これは未決だった小問を1つ閉じる変更なので、AI 側の判断で確定させず承認を得た）——CLI lint は任意起動なので単体では本 Q のギャップを閉じず、**本番ロード時の検証が土台・CLI lint は追加入口**という関係になる（下記 A-1／A-5）。<br>・当初併記した「07 の実 JSON 契約検証が無い」は**誤りだったので撤回**（`tests/unit/test_cli_p2.py` が `FilePlatformAdapter` で実 JSON をパースし `suggested_fix.diff` まで検証している）。07 の JSON 例と実装型の不一致は**機械的な文書誤記**として本論点から切り離し、[07](requirements/07-ai-input-design.md) を直接修正済み。<br>**選択肢**：**(A) lint を本番経路（`criteria_repo` のロード時）へ結線する**／(B) 結線は post-MVP とし、[schema](schema/README.md) の宣言を「記法のみ確定・値レベル lint は未結線」に限定して据え置く（**現状の記述はこの形**＝2026-07-29 に上記(1)〜(4)の項目別で明記し直した）。<br>**(A) を採る場合の作業範囲（2026-07-29 に明記／同日 Codex 第4巡レビューを受けて A-1・A-4 を是正し A-5 を追加）**：<br>・**A-1 本番ロード経路への結線（必須）**：`persistence/criteria_repo.py` の criteria ロード**および policy ロード**が、読み込む前に lint を通るようにする。**旧記述は「合成時ロードで呼ぶ」か「`reviewer criteria lint` の CLI 口を作る」かを択一の未決事項としていたが、それは誤り**（2026-07-29 撤回）——CLI lint は**利用者が明示的に叩いたときしか走らない任意の口**なので、それだけでは「レビュー実行時に未検査の基準がそのまま読まれる」という本 Q のギャップは閉じない。**本番ロード時の検証と O-14 化が必須**で、**CLI lint は本番結線とは別の“追加入口”として A-5 に分離**する（どちらか一方ではなく、前者が土台・後者が上乗せ）。<br>・**A-2 ネスト段数検査の新規実装**（上記(4)）：`frontmatter.py` に段数上限が無いため、結線だけでは効かない。<br>・**A-3 黙って対象外にしない／黙って空にしない**（上記(3)後半）：`doc_type`/`scope` 欠落を `discover_criteria` の完全一致フィルタで無言スキップせず、criteria の `rules` 欠落・policy の `matrix` 欠落も空扱いにせず、いずれも lint 違反として上げる。**policy 側の無検査項目**（`matrix` 行の severity キーが語彙照合されず typo が黙って `HUMAN_ONLY` に倒れる／`rules[].enabled` が `bool()` で何でも真に潰れる／policy の `version` が読まれもしない）も**同じ扱いで棚卸し対象に含める**（一覧＝[schema](schema/README.md) 冒頭の検査実効性インベントリ）。<br>・**A-4 criteria と policy の**両方**を O-14 経路へ載せる**（上記「共通」・2026-07-29 是正）：`core/pipeline.py` の `deps.load_criteria(...)` **だけでなく `deps.load_policy(...)`**（`pipeline.py:62`）も try/except で受ける。**旧記述は criteria ロードしか対象にしておらず、`load_policy` の例外境界が漏れていた**——`load_policy_file` は `Determinism(...)`／`ApplicationMode(...)` の `ValueError` と `o["rule"]`/`o["mode"]` の `KeyError` を投げうるうえ、`pipeline.py:62` の呼び出しは**仕分け段の途中**にあるため、ここで例外が出ると**評価（LLM 呼び出し）を終えた後に未捕捉クラッシュする**（コストを払ってから落ちる）。<br>・**A-5 診断情報（位置）の保持＋CLI 口（`reviewer criteria lint`）**：S5 は lint 失敗を「**O-14 と同形式（ファイル/行/理由）**」で返せと規定している（[13-stabilization](requirements/13-stabilization.md) S5）が、**現在の `LintError` は `reason` と `subject` の2フィールドしか持たず、ファイル名も行番号も保持できない**（`parsing/lint.py:25-28`）。`lint_criteria` は `dict` を受け取るだけで元ファイルのパスすら知らない。**位置情報を運べる型へ拡張しないと、結線しても O-14 の契約を満たせない**（行番号の出所＝`MiniYamlError.line_no` は構文段にしか無く、値レベル lint には伝わっていない）。この型の拡張と、[03](design/03-external-interfaces.md):47 の未実装コマンド `reviewer criteria lint` の実装を A-5 に置く。<br>※ A-3・A-4・A-5 は 2026-07-29 の実装調査で判明したギャップ。**別 Q を立てず本 Q の作業範囲に含める**（結線設計と論点が同一で、分けると片方だけ実装されて「lint は通るが黙って対象外」「拒否はするが O-14 にならない」「O-14 と言いながらどのファイルか分からない」という中途半端な状態を作るため）。<br>**推奨 = A（A-1〜A-5 を一括）**。根拠：S5 は「E14 を実行時でなく**作成時**に倒す」ための策で、結線しなければ目的を果たさない。異常系方針（Q17＝疑わしきは fail-close）とも、未検査のまま流すのは整合しない。**製品コードの変更を伴う**ため実施時期はオーナー判断。<br>**波及**：A1 は「記法は確定／値レベル S5 lint は未結線・段数検査は未実装」に後退。実装進捗表の #2 `parsing` 行も「モジュールは実装済み／機能単位では未完」に是正（2026-07-29）。A2 は 07 のプロンプト雛形・入力経路が未実装であることが判明し**🟡 へ後退**（2026-07-29）。 | 🔴 **未決・オーナー判断待ち**（2026-07-28 起票） |
| **Q27** | **🤖 自動修正が同一ファイル複数 finding で後勝ち上書きする（先行修正の消失）** | **⚠️ これは未決の論点ではなく「確定済み仕様に対する実装の不具合＝バグ」だった**（2026-07-29 オーナー指摘で分類是正）。[Q3](#-未決事項決めないと進めない論点)（**revert 単位＝finding 単位でコミット**）は既に確定しており、決めるべきことは残っていない。破れているのはその不変条件で、コミットは finding ごとに積まれる（＝粒度は保たれる）が、**各コミットの変更内容が「その finding の修正」＋「先行 finding の修正の取り消し」になっている**ため「1コミット＝1 finding の修正だけ」が成立していない。→ 本行は**クローズ**（起票時の分析として保存＝PR8）。決定と実装方針は [A10](#-ネクストアクション次にやる候補)、実作業は [#287](https://github.com/hiratashinnya/review-system/issues/287)。<br><br>**事実（2026-07-29 Codex 第4巡レビュー指摘・コードと実挙動で確認）**：`suggested_fix.diff` は unified diff ではなく**「修正後ファイルの全内容」**（[07](requirements/07-ai-input-design.md)・[04](design/04-platform-protocol.md)）。`core/apply.py:29-37` の `apply_auto` は 🤖 区分の finding を**順に**回して `workspace.commit_fix(exec_id, key, rel, fix.diff)` を呼び、`persistence/workspace_git.py:61-67` の `commit_fix` は受け取った内容で対象ファイルを `write_text` で**丸ごと上書き**する。両者に**同一ファイルを指す finding をまとめる処理が無い**ため、**同一ファイルに 2 件以上の 🤖 finding があると、location が別でも後続の全内容が先行の修正を丸ごと消す**。<br>**実挙動（実測）**：3行のファイルに対し finding A（1行目を修正した全内容）→ finding B（3行目を修正した全内容）を適用すると、最終ファイルは **B の内容だけ**（A の修正は消失）。しかも `apply_auto` は **`Success` を返し、コミットも 2 件積まれる**ため、レポート上は「2件適用」に見えて**利用者は消失に気付けない**。<br>**影響**：**🤖 自動修正は MVP の中核価値**（「直してくれて、いつでも戻せる」＝P2 差別化）。1ファイルに複数の指摘が付くのは**むしろ普通**なので、実運用ではほぼ確実に踏む。さらに「適用したはずの修正が入っていない」は**サイレントな正しさの破れ**で、S4（トランザクション性＝半端を残さない）と Q17（疑わしきは fail-close）の趣旨に反する。revert は積まれたコミットで戻せるので**データが壊れて戻せなくなるわけではない**が、**出力が誤っていることを検知できない**点が問題。<br>**設計との不整合**：既存設計は衝突単位を「**同一 location**」と定義している（[05 DS3](design/05-persistence.md)・[03](design/03-external-interfaces.md)・[13 S4](requirements/13-stabilization.md)・[00 データ辞書](design/00-data-dictionary.md) `FixConflict{location + …}`・[Q20](#-未決事項決めないと進めない論点)）。しかし `diff` が全内容である以上、**衝突の実単位はファイル**であり、location 単位の定義では**同一ファイル・別 location** の破壊を取りこぼす。加えて [Q20](#-未決事項決めないと進めない論点) が定めた2段構え（LLM マージ→迷いは人）は**そもそも未実装**（`core/apply.py` に衝突検出も統合も無い）。<br>**選択肢**：<br>・**(A) 同一ファイルの複数 fix を統合してから 1 回だけ書く**：`apply_auto` で `location.file` ごとに束ね、[Q20](#-未決事項決めないと進めない論点) の L4 マージ雛形で統合案を作り、1ファイル1書込にする。**マージが LLM 依存**になるので、迷い時は (B) へフォールバックする必要がある。finding 単位コミット（[Q3](#-未決事項決めないと進めない論点)）とは相性が悪く、「1コミット＝1 finding」の粒度を見直すか、統合後に finding 単位で分割コミットし直す設計が要る。<br>・**(B) 同一ファイルに 🤖 finding が 2 件以上あれば、そのファイルの自動適用を拒否して人へ回す**（安全側 fail-close）。1件だけのファイルは従来どおり適用。拒否したものは ✋ 相当としてレポートに出す。**実装が小さく、誤った出力を確実に止められる**が、複数指摘のあるファイルは自動修正の恩恵を受けられない。<br>・**(C) post-MVP へ据え置き**（現状維持＋文書化のみ）。<br>**推奨 = B（MVP）→ A（post-MVP）**。根拠：**(1)** 本 Q の害は「壊れる」ことより「**誤った結果を正しいと見せる**」ことなので、まず**確実に止める**のが Q17「疑わしきは fail-close」・S3 の方針に一致する。**(2)** (A) は非決定ステップ（LLM マージ）を自動適用経路に持ち込むため、**最大の不安定要因を中核に増やす**（S1–S4 の設計思想と逆行）。マージ品質の検証手段も無い現状で MVP に入れるのは危うい。**(3)** (B) は `apply_auto` に「ファイル単位で件数を数え、2件以上なら適用対象から外して人手側バケツへ送る」だけで済み、テストも決定的に書ける。**(4)** (C) は非推奨——中核機能がサイレントに誤るまま MVP を閉じることになる。<br>**波及**：衝突単位の定義を「同一 location」→「**同一ファイル**」へ改める必要があり、[05 DS3](design/05-persistence.md)・[03](design/03-external-interfaces.md)・[13 S4](requirements/13-stabilization.md)・[00 データ辞書](design/00-data-dictionary.md) の `FixConflict`・[Q20](#-未決事項決めないと進めない論点) が対象（本 PR で各所に⚠️注記済み）。(B) を採る場合は ✋ バケツへの送り先が要るため **[Q25](#-未決事項決めないと進めない論点)/F7（✋/💬 人手適用）が前提**になる。テストは `tests/unit/test_apply.py` に「同一ファイル2 finding」のケースが**無い**ので追加が要る。<br><br>**決定＝(A) 統合・MVP でやる**（オーナー確定 2026-07-29）。<br>**起票時の推奨（B）は撤回する**——「全内容だから統合には LLM マージが必須」という前提が**誤りだった**。`diff` が全内容でも `delta_i = diff(baseline, fix_i.diff)` で finding ごとの実変更は機械的に復元でき、共通祖先＝baseline の**3-way マージ**で**重ならない限り決定的に合成できる**（`_baseline` は `persistence/workspace_git.py:37-38` に既存）。LLM（[Q20](#-未決事項決めないと進めない論点) の2段構え）が要るのは delta が実際に重なったときだけで、非決定ステップを中核へ持ち込むという (B) 推奨の根拠は成立しない。<br>**さらにこの直し方は [Q3](#-未決事項決めないと進めない論点) を自動的に満たす**：`commit_fix` を「全内容で上書き」から「baseline→new_content の delta を**現在のファイル**へ当てる」に変えると、finding ごとに1コミットのまま各コミットの内容がその finding の修正だけになる。衝突検出も「delta が重なったか」で決定的に判定できる。<br>**実装方針の詳細＝[A10](#-ネクストアクション次にやる候補)、実作業＝[#287](https://github.com/hiratashinnya/review-system/issues/287)。** | 🟢 **クローズ（バグと判明・決定＝統合(A)・MVP）**。分類是正 2026-07-29。実装は [A10](#-ネクストアクション次にやる候補)／[#287](https://github.com/hiratashinnya/review-system/issues/287) |
| Q6 | 無効化の責務の二重化 | 事実(基準)と好み(ポリシー)を混ぜない＋方向ゲート制で整理。緩めはポリシー/承認経由、完全オフは `enabled` に一本化 | 🟢 方針合意（明文化済み） |
| Q7 | LLM の id 誤付与のフォールバック | **第4区分「❓ 未分類」として surfacing**。自動仕分けせず、人の確認＋新ルール候補（育成）へ。未分類化は2経路＝(a)LLM自己申告 (b)**rule_id がパックに無いものをプログラムが検証して回送**（[09](requirements/09-processing-pipeline.md)⑤） | 🟢 確定 |
| Q8 | `category` の語彙 | **enum 強制はしない（テキスト編集できる以上、非現実的）**。自由記述＋**推奨語彙（ソフト規約）**で運用、集計は存在する文字列でグループ化。AI クラスタリングによる正規化提案は MVP 不要＝将来の育成機能候補（[schema](schema/README.md)） | 🟢 確定（ソフト規約） |
| Q9 | 承認者への打ち上げ＋**警告の既出判定** | 機構(設計)＝(a)方向検出の結果を人へ surfacing (b)**警告レジャー：`hash(対象ルールのメタ+本文)` で既出判定し、既出は再警告せずレポートに「既知」として混ぜるだけ**（完全に機械的。[schema](schema/README.md#警告の既出判定warning-ledger)）。cadence/しきい値等は運用デフォルト | 🟢 機構=設計確定 / 規則=運用 |
| Q10 | 同一 `doc_type×scope` の複数ファイル結合規約 | スコープ内は**対等な兄弟・順序なし**（順序での暗黙解決はしない）。各ルールに provenance（由来パス）を保持。同 id 衝突は「メタ＝決定的に同値判定／本文＝LLM が**矛盾の有無だけ**判定」し、**矛盾なし→共存・矛盾あり→親フォールバック＋警告**。LLM は方向ではなく整合性判定に使う（2軸原則を維持）。警告は人へ（Q9接続） | 🟢 方針合意（schema 明文化済み） |
| Q11 | `override`（特に `locked`）を宣言できるのは誰か | **クローズ（MVP 非該当）**：機構（3値 `locked/tighten-only/open`＋方向ゲート＋合成時の機械判定）は確定済み。**「誰が宣言してよいか」の強制は将来の RBAC**（Q1 と同バケツ）。MVP は **org 固定スコープ**（Q13）で多段継承が無く論点が発生しない。**F17（team/project 3段継承）着手時に Q1 と一体で再起票** | 🟢 クローズ（Q1/F17 へ統合） |
| Q12 | 文書の入力粒度 | **関連ファイルをまとめて1レビュー**（バッチ単位・横断整合も見る）。アップロード基本（[08](requirements/08-intake-design.md)）。※回答中に I-13 参照コンテキストの欠落が発覚 | 🟢 確定 |
| Q13 | スコープの決定方法 | **scope の定義は観点ファイルのフロントマター**（`scope`/`extends`）＝I-4 の中身で別 config/状態にしない（旧 I-11 削除）。**MVP は org 固定**、per-review の選択は I-3（任意）。team/project は MVP 後（[08](requirements/08-intake-design.md) / [schema](schema/README.md)） | 🟢 確定（MVP） |
| Q14 | ~~同一文書の再評価時の挙動~~ | **クローズ：区別しない**。システムはステートレスに、渡されたものを淡々とレビュー＝再提出は単に再レビュー。区別には記憶が要り無駄。修正漏れは普通に起きる前提。※"警告"の dedup は別物で Q9 のレジャーで対応（指摘は毎回出す／警告は一度で足りる、と状態の要否が逆） | 🟢 クローズ |
| Q15 | 基準/ポリシー更新の伝播 | **クローズ**。変更検知トリガは持たない（テキスト編集・非Git前提で検知困難）。①合成はレビュー実行のたび毎回（決定的・AI不使用で安価）②本文矛盾チェック(LLM)だけ `content_hash` でキャッシュ照合・不一致で再実行（矛盾検知/警告レジャーと同機構）③過去文書への**遡及はしない**（新基準は以後／再提出で適用＝Q14と同じ） | 🟢 クローズ |
| ~~Q16~~ | ~~承認待ち滞留への督促~~ | **クローズ（削除）**：承認待ちが片付いたかを**システムは観測できない**（レビューはステートレス・対応は系外）。通知後の顛末を追えず督促の前提が無い。E13/O-13 削除 | 🟢 クローズ（不要） |
| Q17 | 異常系イベントの振る舞い | **確定**（下「🧯 異常系の方針」）：原則 **「疑わしきは fail-close＋明示エラー(O-14)／空文書だけ benign no-op」**。⑦自動適用は**クリーンな F4 が前提**＝失敗時は書込なし。**全段 fail-close＋O-14 明示は MVP に含む**、per-unit graceful degrade のみ F16(post-MVP)。要件＝[13-stabilization](requirements/13-stabilization.md) S3。S3/S5 と一体 | 🟢 確定 |
| Q18 | 観点パックの肥大化対策 | **MVP は全部載せ**（doc_type で絞り済み）。膨張時の retrieval 的選別は MVP 後の最適化（[07](requirements/07-ai-input-design.md)） | 🟢 確定（MVP） |
| Q19 | 参照コンテキストの紐付け | Q12 回答で発覚（I-13）。上流成果物・前提条件を評価対象と別枠で入力。**MVP は手動添付・任意・複数可**。どの観点がどの参照を要求するか等の自動紐付けは将来（[08](requirements/08-intake-design.md)） | 🟡 MVP方針あり |
| Q20 | 同一実行内の自動修正衝突 | **2段構えで確定**：①LLM がマージ案を生成 ②「迷い」は人が決定。LLM の返り値に迷いフラグを持たせ、システムが人へルーティング（[10](requirements/10-llm-system-boundary.md) L4）。finding 単位コミット(Q3)とセット<br>⚠️ **実装状況・衝突単位の不整合（2026-07-29 追記）**：この2段構えは**未実装**（`core/apply.py` に衝突検出も統合も無い）。かつ**衝突単位を「同一 location」と定めたのが実装の意味と合っていない**——`suggested_fix.diff` は「修正後ファイルの全内容」なので、**同一ファイルなら location が別でも後勝ちで先行修正が消える**。→ 衝突単位を**同一ファイル**へ広げるか安全側に拒否するかを含め、**[Q27](#-未決事項決めないと進めない論点) で別立てに起票**（本 Q は「衝突が起きたときどう解決するか」の方針、Q27 は「そもそも衝突が検出されず黙って壊れる」実装欠陥） | 🟢 方針確定／⚠️ **未実装＋衝突単位は要改訂**＝[Q27](#-未決事項決めないと進めない論点) |
| Q21 | 決定論的な修正の生成主体 | **確定：ツール化しやすいところはツール化**。整形/import整列/lint自動修正/誤字/ケース変換は決定的ツールが生成、判断含むものは LLM。候補一覧は[10](requirements/10-llm-system-boundary.md) | 🟢 確定 |

## 🧯 異常系の方針（Q17 確定）

> ✅ 承認済み（2026-06-06）。要件は [13-stabilization](requirements/13-stabilization.md) S3 に落とし込み済み。

原則：**価値経路は遮断しないが、誤った結果は出さない。疑わしきは fail-close＋明示エラー（O-14）。
fail-open は「続けてもリスクが無い良性ケース」に限る（＝空文書）。**
自動適用⑦は**完全・クリーンな仕分け(F4)が前提**で、上流失敗時は一切書き込まない（半端を残さない＝ステートレス）。

| 異常(E14) | 段 | MVP の振る舞い | 根拠 | post-MVP(F16) |
|---|---|---|---|---|
| 基準パース失敗 | ②合成 | **fail-close**：レビュー中止＋O-14（どのファイル/なぜ）。org 少数ファイルなので全体停止で安全 | 黙って落とすと「網羅した」と誤認＝偽の安心 | 当該ファイルのみ skip＋「N/M 適用」明示の degrade |
| LLM 障害（PF 到達不可/タイムアウト） | ④評価 | **bounded retry→fail-close**：findings 無し＝価値無し。**⑦自動適用は走らせない**。O-14 明示 | 中核(F3)。捏造・部分適用は事故 | バッチ単位で部分 degrade（独立呼出時） |
| LLM 出力が不正（schema 違反/偽 rule_id） | ⑤検証 | **degrade（既設計）**：不正 finding は ❓未分類へ。crash/silent-drop 禁止 | 受け口を固く（S1）。rule_id 検証は既設 | 同左（強化） |
| スコープ未解決（doc_type 不明/`extends` 切れ/基準ゼロ） | ①② | **fail-close＋実行可能エラー**：「doc_type=X の基準が無い／extends 先が無い」。空の既定にフォールバックしない | 黙って薄い評価＝偽の安心 | 切れた継承リンクを名指しで surfacing |
| 空文書（評価対象が実質ゼロ） | ①intake | **fail-open（良性 no-op）**：0件レポート「評価対象なし」。エラーにしない | 続けてもリスク無し＝遮断しない(PR6) | 同左 |

## 🛡 価値実現の安定化（洗い出し：S1–S6＝MVP／S7–S9＝post-MVP）

P1（出すと仕分けレポートが返る）・P2（直す＋戻せる）を**安定して**届けるための具体物。
最大の不安定要因は**唯一の非決定ステップ＝LLM(④)**。ここを固く・失敗を可視化・書込を安全側に倒すのが要。

> ✅ **S1–S4（MVP 必須）は [13-stabilization](requirements/13-stabilization.md) で要件化済み**（S5/S6 も同書に MVP 推奨として記載）。

| S# | 安定化策 | 何を防ぐ | 関連 | 段階 |
|---|---|---|---|---|
| S1 | **LLM 出力の契約検証を固く**（⑤）：strict schema→非適合は ❓未分類。crash/silent-drop 禁止 | 幻覚 id・欠損 location・壊れ JSON での事故/取りこぼし | Q7,⑤ | **MVP 必須** |
| S2 | **仕分けの安全側デフォルト**：determinism 未宣言/不明は**人間側(✋/💬)**へ、🤖 にしない | 誤宣言由来の誤自動適用 | Q2,⑥ | **MVP 必須** |
| S3 | **異常系ポリシー(Q17)**：疑わしきは fail-close＋O-14／空文書のみ no-op／⑦はクリーン F4 前提 | 部分結果での誤適用・偽の安心 | Q17 | **MVP 必須** |
| S4 | **自動適用のトランザクション性**：失敗時は書込ゼロ／finding 単位コミットで常に revert 可 | 半端な書き換えが残る | Q3,F6/F8 | **MVP 必須** |
| S5 | **基準の事前 lint**（実行前検証）：frontmatter 妥当・override∈{locked,tighten-only,open}・extends 存在 | E14 を実行時でなく**作成時**に倒す | Q17,schema | **MVP 推奨**／⚠️ **未達**＝[Q26](#-未決事項決めないと進めない論点)（**統制された拒否は記法サブセットのみ**。値域違反・必須キー欠落は未捕捉例外で落ちるだけ、`id` 一意性/`extends`/`version`/段数/`enabled` 型/policy 全般は無検査、`doc_type`/`scope`/`rules`/`matrix` 欠落は黙って消える。診断も O-14 が要求する**ファイル/行を保持できない**＝`LintError` に位置情報が無い） |
| S6 | **結果の provenance/版スタンプ**：PF/モデル＋プロンプト雛形版＋基準 content_hash をレポートに記録 | 「なぜこの結果か」が説明不能・再現困難 | Q15,07 | **MVP 推奨** |
| S7 | per-unit graceful degrade（F16 本体） | 1ファイル障害で全体停止 | Q17,F16 | post-MVP |
| S8 | 過大入力の検出/分割（無言の truncation 禁止） | 長文/肥大パックでの無言の劣化 | Q18,07 | post-MVP |
| S9 | 合成出力の正準順序（union を sorted＋provenance 固定） | プロンプト揺れ由来の出力不安定 | Q10,② | post-MVP |

## 🗂 未決の分類と優先度

**区分**で「設計（＝システムが出す機構）」と「運用ルール（＝システムが前提とする条件・組織が設定するデフォルト）」を分ける。
**運用ルールは設計で詰めない**——機構だけ設計し、規則はデフォルト/前提として置く（2軸の運用側）。

| テーマ | Q# | 区分 | 優先 | メモ |
|---|---|---|---|---|
| 入力境界（文書→システム） | Q4 / Q12 / Q13 / Q19 | 設計（IF） | **高** | 🟢 確定（[08](requirements/08-intake-design.md)）。参照コンテキスト I-13 は MVP方針あり |
| LLM 出力の受け口 | Q7 / Q18 | 設計 | **高** | 🟢 確定（[07](requirements/07-ai-input-design.md)）。未分類=第4区分・観点パック全部載せ |
| 自動修正の機構 | Q3 / Q20 / Q21 | 設計（機構） | 中 | revert＝finding単位コミット／衝突解決=2段構え(Q20)／決定論的修正の生成主体(Q21) |
| **🤖 自動修正の後勝ち上書き** | ~~**Q27**~~ | **確定仕様への実装バグ**（Q3 違反） | **高** | 🟢 **クローズ（未決の論点ではなくバグと判明・2026-07-29）**／🔴 **実装未着手**＝[A10](#-ネクストアクション次にやる候補)・[#287](https://github.com/hiratashinnya/review-system/issues/287)。`diff`＝**修正後ファイルの全内容**なのに `apply_auto` が finding ごとに全上書きするため、**同一ファイル 2 件以上で先行修正が黙って消える**（`Success` が返るので気付けない）。**決定＝(A) baseline からの delta を 3-way マージ統合・MVP**（オーナー確定）。起票時の推奨（B）は撤回。衝突単位「同一 location」→「**同一ファイル**」へ改訂 |
| **基準 lint の結線（S5）** | **Q26** | **設計（機構）** | 中 | 🔴 **未決**。分類＝**①統制された拒否＝記法サブセット外のみ**（パーサが `MiniYamlError`）／**②未捕捉例外で拒否＝値域（override 等）と必須キー欠落**（ローダの Enum 変換・直接添字参照）／**③無検査で通過＝`id` 一意性・`extends`・`version` MAJOR・入れ子段数・`enabled` の型・policy の severity 語彙**／**④黙って対象外・空扱い＝`doc_type`/`scope`/`rules`/`matrix` の欠落**。加えて**②の拒否は O-14 形式ではなく**、`LintError` が**ファイル/行を保持できない**ため結線しても O-14 契約を満たせない。**policy 側は lint 関数自体が無い**。一覧＝[schema](schema/README.md) 冒頭 |
| **人手適用の出口（F7）** | **Q25** | **設計（機構）** | **最高** | 🟢 **方針確定＝A**／🔴 **実装未着手**（[A9](#-ネクストアクション次にやる候補)）。✋/💬 バケツに適用経路が無く価値経路が切れている（PR6）。A8 から移送・[#7](https://github.com/hiratashinnya/review-system/issues/7) が前提論点 |
| 基準スキーマ細部 | Q8 | 設計 | 中 | category 語彙＝集計の厳密化 |
| 基準の上書き権限 | ~~Q11~~ | 運用ルール | — | 🟢 クローズ（MVP=org固定で非該当・F17/Q1 へ） |
| 承認・ガバナンス | Q1 / Q9 | 運用ルール | 中 | **機構のみ設計**、規則は組織のデフォルト。詰めない（Q2 はクローズ＝schema 吸収） |
| **価値実現の安定化** | **S1–S6** | **設計** | **高** | LLM 受け口(S1)・安全側仕分け(S2)・異常系(S3)・トランザクション(S4)。MVP 必須 → [13-stabilization](requirements/13-stabilization.md) で要件化 |
| ライフサイクル機構 / 異常系 | Q17 | 設計 | 中 | 🟢 確定（fail-close＋O-14／空文書 no-op）。[13](requirements/13-stabilization.md) S3。S3/S5 と一体 |
| ~~滞留督促~~ | ~~Q16~~ | — | — | 🗑 削除（滞留はシステムが観測不能） |
| 実装 | Q5 | 設計 | 中 | 🟢 Python・原則 stdlib のみ。**Q5a＝自前の最小フロントマターパーサ**（対応文法の正本＝[schema](schema/README.md)。**Q24=A で引用キー `"*"` ＋3段ブロックネストまで拡張済み**——旧「1段ネスト」表記は 2026-07-29 に撤回）。「検証器 S5 を兼ねる」は**構文サブセットの範囲でのみ実効**——値レベル lint は未結線・**規定の3段上限に対し実装は深さ任意で受理**（段数検査が未実装）＝**Q26** |
| 合意済み（参照） | Q6 / Q10 | 設計 | — | 確定・明文化済み |

> 進め方の指針：**高（Q4/Q12/Q13/Q7/Q18 ＋ 安定化 S1–S6）は全部「設計」**。実装前に S1–S4 を仕様へ落とす。
> **運用ルール（Q1/Q9）は機構＋デフォルトに留め、設計で詰めない。**（Q2＝schema 吸収・Q11＝F17/Q1 へ・Q16＝観測不能で削除）

## ✅ 決定済み（参照用）

- 社内ツールとして作る
- 評価基準は構造化 Markdown で外部ファイル化、Git 管理
- `文書タイプ × スコープ` で動的選択・継承
- 指摘は `深刻度 × 対応モード` で自動仕分け
- 自動化レベルはコンフィグ可能（決定論的＝ログのみ 〜 判断要＝人間）
- Human-only でも AI が原案提示
- auto-fix は自動修正サマリで可視化＋一括 revert
- 流し読みのフィードバックが基準・ポリシー育成に直結
- **設計（機構）と運用ルール（前提条件）を混ぜない**：設計は機構を出す（方向の検出・器・provenance・revert・AI I/O）。運用ルール（誰が承認・緩めの承認要否・督促頻度）は機構上のデフォルト/前提として置くだけで設計では詰めない（2軸の運用側）
- 上書きの**方向ゲート（org 権威）**：追加・厳しく=自由 / 緩め・無効化=**既定で機械拒否**（org が `open` したルールのみ可）/ `locked`=不可。完全に機械判定で**承認ステップ無し**。override の3値は `locked | tighten-only(既定) | open`
- 判定は常に**2軸で分ける**：システムの判定基準（順序ある属性=機械が自動ゲート）／運用ルール（本文・事実性=人が確認）
- 本文（観点）の上書き＝差し替え扱い（open のみ可・他は機械拒否・locked 不可）。観点/例の追加は別ファイルで union
- **MVP はアクターを区別しない**（単一ユーザー）。専用 RBAC も Git ホスト権限依存も採らない。役割ラベルは記述的。方向ゲートは「検出」まで機能し、「誰が承認するか」の強制は将来
- **LLM は自前実装せず外部 PF（Claude Code/Copilot 等）に委譲**。システムは PF のラッパーで、接続部は**アダプタパターン**（PF 差し替え可・自前実装も同じ口に差せる）。出力はアダプタ越しでもシステムが検証する（[11](requirements/11-platform-adapter.md)）
- **口は双方向**：System→PF（ヘッドレス）と PF→System（決定的処理を function calling / MCP ツールで公開）。同じポートを両向きから使う。**MVP は「ツール群＋プロンプト雛形」だけ作り、Claude が LLM 役を担って高速に検証**（オーケストレータ/UI は後）。ツールは後で System 駆動に切り替えても再利用（[11](requirements/11-platform-adapter.md)）
- **実装前の凍結セット（8項目）**を固めてから着手：A モジュール構成・②外部IF・B PF駆動プロトコル・C 永続層・①オーケストレーション(スイムレーン付フローチャート)・③システムプロンプト・D ログ/版・④テスト戦略。索引＝[design/README](design/README.md)
- **モジュール構成はヘキサゴナル＋依存内向き**：`domain ← core(ports) ← adapters/persistence/cli`。コアは PF/IO/保存形式を知らず、結線は合成ルート(`io/cli`)のみ（[design/02](design/02-module-architecture.md)）
- **テスト戦略**：unittest（全 public 関数）／ケース＝Markdown／成績書＝ケースコピー＋実測＋commit id／ログ＝stdout ダンプ／失敗も残す（隠蔽禁止＋原因/対策）／テスト前コミット／e2e は Claude Code エージェントで同じ3点セット。非決定（LLM）は `FakePlatformAdapter` で決定化＝アダプタ境界＝テスト境界
- **資産のテーラリング運用（A16）**：プロセスはスキルで実現するためテーラリング実体は `.claude/`（docs でない）。汎用標準＝`.claude/standards/`（非活性）、テーラリング済 active＝`.claude/skills/`、対応台帳＝`.claude/tailoring-registry.md`。テーラリング時は元を `git mv` で非活性化（消さない）し registry に記録。本PJは extract-then-generalize。初回適用＝`/test-strategy`
- **`scripts/lateral_deploy.py` のフロントマターパーサ分離（DD18）**：`review_system/parsing/frontmatter.py` は `KEY=[a-z_][a-z0-9_]*` regex が hyphenated key（`disable-model-invocation` 等）非対応のため、`lateral_deploy.py` 専用の寛容パーサを別実装。2パーサ併存を許容（`lateral_deploy` はスタンドアロン設計・既存パーサの拡張は Q5a 範囲外）→ [DD18](design/decisions.md)

> 各決定の詳細は [requirements/](requirements/) を参照。プロセス設計（DFD/状態）は [process/](process/00-context.md)。
> システムの入出力一覧は [requirements/05-io-overview.md](requirements/05-io-overview.md)、
> イベント駆動の俯瞰は [requirements/06-event-list.md](requirements/06-event-list.md)。
> **実装設計**：データディクショナリ集約は [design/00-data-dictionary.md](design/00-data-dictionary.md)、
> 型安全なドメインモデル（dataclass）は [design/01-class-design.md](design/01-class-design.md)。
