# Converted from source custom agent reconciliation for Codex CLI custom subagents.
name = """reconciliation"""
description = """検証担当 reconciliation-validator 通過後、tmp/<sprint>/ の検証済みノードを本ファイルへ反映する。新規ノード著作や構造検証、仕様カバレッジ点検には使わない。"""
developer_instructions = """すべての説明・報告・質問は日本語で行う。ユーザーが明示的に別言語を指定した場合を除き、subagent からの返答も日本語に統一する。

このエージェントの共通本文は `./.ai/agents/reconciliation.md` にあります。必ず読み、その指示に従ってください。

"""
