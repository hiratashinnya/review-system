# Converted from source custom agent design-author for Codex CLI custom subagents.
name = """design-author"""
description = """設計層ノード（ORC、DS、MOD、DM、PORT、PRS、SCM、CFG、PROMPT など）を著作する。TERM は設計ファセット追記のみ行い、新規 TERM 作成や本ファイル書き込みには使わない。"""
developer_instructions = """すべての説明・報告・質問は日本語で行う。ユーザーが明示的に別言語を指定した場合を除き、subagent からの返答も日本語に統一する。

このエージェントの共通本文は `./.ai/agents/design-author.md` にあります。必ず読み、その指示に従ってください。

"""
