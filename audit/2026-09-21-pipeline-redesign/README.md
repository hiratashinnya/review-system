# issue-pipeline 再設計 — 決定台帳（2026-09-21〜）

オーナーとの検討で確定した事項を、**生きている決定（§A）と訂正・置換された決定（§B）に分けて**記録する。
根拠となる実測は §C、未決事項は §D、機械強制を将来へ持ち越した検査項目は §E。

**読み方**

- **§A が現行の正**。ここに載っている内容だけが有効な前提である。
- **§B は無効**。残すのは経緯を再構成するためであって、参照して行動してはならない
  （`.claude/rules/01-principles.md`「PR8「消さない」の適用範囲」区分1＝経緯は保全する）。
- §A と §B は**相互ポインタ**で結ぶ。§A の各項は「何を置換したか」、§B の各項は「何に置換されたか」を持つ。
- **`オーナー逐語` は逐語であり、AI の解釈を混ぜない**（`feedback_ledger` の L4 と同じ規律）。
  解釈は `AI の原案と、オーナーが変えた点` に分けて書く。

記載するのは**決定とその根拠**であって、処置計画・実施予定ではない（それは Issue で管理する
＝`audit/README.md` の「置く / 置かない」）。

**実行状況の入り口（2026-09-24追加・U-7解消）**：本監査が起票・関係した GitHub Issue の一覧は、
この台帳に手書きしない（[決定台帳の正本をfeedback-ledgerとする](#fbk-20260921-決定台帳の正本をfeedback-ledgerとする)
の「audit/ は手書きをやめる」と同じ理由——手書き一覧はクエリで代替できるなら二重管理になる）。
代わりに GitHub 側を発生源として `gh issue list --label "audit:2026-09-pipeline-redesign" --state all`
で問い合わせる。決定の根拠・経緯はこのファイル、実行状態は上記クエリが一次情報源。

| 項目 | 規則 |
|---|---|
| **id の形** | `FBK-<YYYYMMDD>-<slug>`（`feedback_ledger` の `LEDGER_ID_RE` と同形） |
| **slug の作り方** | `doc-system-v2/slugify.py`（唯一実装）で `トピック` を正規化したもの。独自に正規化しない |
| **トピック** | **30字以内で「何を決めたか」を述語まで書く**（例：`リポジトリの目的は…とする`）。論点名ではない |
| **id は不変** | **訂正・置換されても id は変えない。**§A→§B の移動は `置換` ポインタからの導出であって、id の意味ではない |

---

## §A 生きている決定事項

<a id="fbk-20260921-リポジトリの目的はai駆動開発ノウハウ獲得とする"></a>

### リポジトリの目的はAI駆動開発ノウハウ獲得とする

| | |
|---|---|
| **id** | `FBK-20260921-リポジトリの目的はai駆動開発ノウハウ獲得とする` |
| **決定日時** | 2026-09-21 |
| **状態** | **生存** |
| **置換** | （新規） |
| **関連** | [QCDはQを制約としCとDを最小化する](#fbk-20260921-qcdはqを制約としcとdを最小化する)・[Qの下限は流出欠陥率1%とする](#fbk-20260921-qの下限は流出欠陥率1%25とする)・[必須施策を5項目に定める](#fbk-20260921-必須施策を5項目に定める) の前提 |
| **実行** | **#541**（2026-09-23 起票・「実施すること自体は確定・未実施」だった穴を埋める） |

**決定内容**

> 本リポジトリの目的は、**オーナーが AI 駆動開発のノウハウを獲得すること**である。
> `review_system` はその題材であって目的ではない。**製品の進捗はこのリポジトリの成功指標ではない。**
> ハーネスを作り込んで品質・進捗・コストを改善すること、およびアンチパターンを経験すること自体に価値がある。
> 獲得したノウハウの**転用先は車載 ECU** であり、品質担保はマストである。
>
> 上記を `README.md` と `CLAUDE.md` の冒頭に明記する（実施すること自体は確定・未実施）。

**経緯（周辺文脈）**

AI が「製品 `review_system/` が 2026-06-09 を最後に104日間 0 コミットであること」を最重要の欠陥として
報告した。その報告は、目的を「製品を作ること」と推論した上での評価だった。
点検の結果、**この目的は正本のどこにも書かれていなかった**——

```
$ grep -n "ノウハウ|習得|学習|目的は|題材|実験" CLAUDE.md .claude/rules/*.md .ai/guidance/common.md README.md
README.md:15:- **組織学習** — 評価基準が使うほど育ち、ナレッジとして蓄積される
```

ヒットは review_system（製品）の提供価値の1行のみ。`README.md` は製品紹介のまま、`CLAUDE.md` は
「このリポジトリでの仕様策定・設計の進め方」という**手段の説明から**始まっていた。
毎ターン 86,058 文字の規範が注入される環境で、その中に目的が1文字も無かった。

**オーナー逐語**

> cはもうちょい詰めるべき。但し、このリポジトリの運営目的は製品を作ることではないので、前提というか方向性を見直してほしい。このリポジトリの目的は俺がAI駆動開発のノウハウを獲得すること。
> 題材としてreview_systemを作るところから始めたが、ハーネスを作り込むことで品質と進捗とコストを改善していったり、アンチパターンを経験すること自体に価値があるから、製品が進んでないくても別に構わない。

> 得たノウハウは製品開発に転用するが、転用先は車載ECUだから品質担保はマストになる。

> 目的を正本に書くのは実施する。

**AI の原案と、オーナーが変えた点**

AI の原案は「製品が停止していることを最重要欠陥として是正する」だった。オーナーが目的そのものを提示し、
評価軸を差し替えた。**AI が推論した目的が誤っていたため、その上に積んだ分析（アンチパターン AP-1
＝手段の目的化）は評価として無効になった。**

**帰結として観測された実害**

目的関数が書かれていないため、各 Issue がそれぞれ推論したローカルな目的（#370＝欠陥混入率、
#368＝トークン効率、#371＝設計不足の早期検出、#409＝実行ライフサイクル、#513＝規律の機械強制、
#530＝レビュー網羅性、#521〜525＝オーナー判断の収束）を最適化し、**相互に競合する施策を裁定する
上位軸が無い**状態になっていた。オーナーが検討冒頭に述べた「局所最適による不都合が大きくなってきている」の
直接の原因はこれである。

---

<a id="fbk-20260921-qcdはqを制約としcとdを最小化する"></a>

### QCDはQを制約としCとDを最小化する

| | |
|---|---|
| **id** | `FBK-20260921-qcdはqを制約としcとdを最小化する` |
| **決定日時** | 2026-09-21 |
| **状態** | **生存** |
| **置換** | （新規） |
| **関連** | [リポジトリの目的はAI駆動開発ノウハウ獲得とする](#fbk-20260921-リポジトリの目的はai駆動開発ノウハウ獲得とする) が前提。[Qの下限は流出欠陥率1%とする](#fbk-20260921-qの下限は流出欠陥率1%25とする)（Q の下限）・[撤退条件Aから現行必須施策を除外する](#fbk-20260924-撤退条件aから現行必須施策を除外する)（撤退条件）がこの上に載る |

**決定内容**

```
minimize   C（コスト）, D（リードタイム）
subject to Q >= 閾値      ← 下げられない下限。ここを削って C/D を稼ぐことは禁止
```

**学習の成果 ＝「Q の下限を割らずに、C と D をどこまで下げられたか」。**

**表記についての注記（2026-09-24・Claude Code (AI)）**：ここでの `Q` は方向として「高いほど良い」
抽象指標として書いているが、実際に採用した具体値（[Qの下限は流出欠陥率1%とする](#fbk-20260921-qの下限は流出欠陥率1%25とする)）
は流出欠陥率という「低いほど良い」指標である。式の `Q >= 閾値` は、実運用では「流出欠陥率 <= 閾値」と
読み替える（数式上の抽象化と、採用した具体指標の向きが逆であることの注記。Opus 5.5サブエージェントの
点検で指摘・確認済み。制約の運用そのもの——0.37→0.01への低減を目指す——には矛盾は無い）。

**経緯（周辺文脈）**

オーナーが「ハーネスによって QCD を最大化できているか」を測定軸として提示した。
QCD は同時最大化できない（トレードオフ関係にある）ため、AI が「転用先が車載 ECU である以上、
Q は最大化の対象ではなく制約である」として制約付き最適化の形に直した。

この形により2つが同時に排除される——**(a) Q を上限なく追求して C/D を犠牲にする**（点検時点の状態。
制約であるべきものを目的関数にしている）／**(b) Q を落として C を下げる**（そのノウハウは転用先で
使えないため学習成果にならない）。

**オーナー逐語**

> が、学習の成果をどう測る？個人的にはハーネスによってQCDを最大化出来ているか？を測るのがよいと思う。

> 定式化はそれでよい。

**AI の原案と、オーナーが変えた点**

オーナー原案「QCD を最大化」に対し、AI が「Q は制約、C/D が目的関数」へ定式化し直し、オーナーが承認した。

---

<a id="fbk-20260921-qの下限は流出欠陥率1%とする"></a>

### Qの下限は流出欠陥率1%とする

| | |
|---|---|
| **id** | `FBK-20260921-qの下限は流出欠陥率1%とする` |
| **決定日時** | 2026-09-21 |
| **状態** | **生存** |
| **置換** | **[Qの下限は現在値0.37とする](#fbk-20260921-qの下限は現在値0.37とする) を置換** |
| **関連** | [QCDはQを制約としCとDを最小化する](#fbk-20260921-qcdはqを制約としcとdを最小化する) の `閾値`。[撤退条件Aから現行必須施策を除外する](#fbk-20260924-撤退条件aから現行必須施策を除外する)（撤退条件A）の判定値 |

**決定内容**

> **流出欠陥率 1%（0.01）。**

点検時点の実測値は **0.37**（`defect_metrics` の派生 Issue / merged PR）。**下限に対して約37倍、未達。**

| | 値 |
|---|---|
| 下限（ECU 転用要件） | **0.01** |
| 現在値（直近窓） | **0.37** |
| 基線（2026-08-02〜08-16） | 0.68 |
| 必要な削減幅 | **97.3% 減** |

**トレンドの外挿では届かない**——5週間で 0.68 → 0.37（46%減）。同率が続くと仮定しても 0.01 到達は
約29週間後。かつこの仮定は二重に甘い：(a) 46%減の因果が特定できていない（#370 本文が明記）、
(b) 同期間にコストは逆方向に増えている（規範 86k 文字・ゲート14本・台帳6つ）。

**経緯（周辺文脈）**

AI が「Q の下限をどこに置くか」を判断事項として提示し、**現在値 0.37 を下限とする案を推奨した**
（既に改善した水準を手放す施策を許さないため、という理由）。オーナーがこれを退け、転用先の
要求水準から 1% を提示した。

**オーナー逐語**

> Qの下限は未達だぞ？車載舐めんな。下限1%だ。これでも流出タイミング次第ではまだ高く付くわ。

**AI の原案と、オーナーが変えた点**

AI の推奨（0.37）は**転用先の要求水準を見ていない提案**だった。「現在の実力から出発する」という
発想自体が、制約（Q の下限は外部要件で決まる）と目的関数（C/D の最小化）を取り違えている。
訂正の実施者＝Claude Code (AI)。

---

<a id="fbk-20260924-撤退条件aから現行必須施策を除外する"></a>

### 撤退条件Aから現行必須施策を除外する

| | |
|---|---|
| **id** | `FBK-20260924-撤退条件aから現行必須施策を除外する` |
| **決定日時** | 2026-09-24 |
| **状態** | **生存**（枠組み・条件B/Cは変更なし。条件B の閾値は未決＝U-3） |
| **置換** | **[撤退条件をA/B/Cの3種で定める](#fbk-20260921-撤退条件をa-b-cの3種で定める) を置換** |
| **関連** | [QCDはQを制約としCとDを最小化する](#fbk-20260921-qcdはqを制約としcとdを最小化する) の定式化から導出。[Qの下限は流出欠陥率1%とする](#fbk-20260921-qの下限は流出欠陥率1%25とする) が条件A の判定値。除外対象＝[Issue371の作成工程分離は必須とする](#fbk-20260921-issue371の作成工程分離は必須とする)・[1a/2aと1b/2bはロール分離を必須とする](#fbk-20260922-1a-2aと1b-2bはロール分離を必須とする)・[Issue513のescalate機械強制は必須とする](#fbk-20260921-issue513のescalate機械強制は必須とする) |

**決定内容**

| | 条件 | 処置 |
|---|---|---|
| **A** | 観測窓終了時に Q が下限（0.01）を割った | **無条件撤退**。C/D が良くても残さない。**ただし現時点でオーナーが「必須」と明示的に裁定した施策
（[Issue371の作成工程分離は必須とする](#fbk-20260921-issue371の作成工程分離は必須とする)・
[1a/2aと1b/2bはロール分離を必須とする](#fbk-20260922-1a-2aと1b-2bはロール分離を必須とする)・
[Issue513のescalate機械強制は必須とする](#fbk-20260921-issue513のescalate機械強制は必須とする)）は
この無条件撤退の対象から除外する** |
| **B** | Q の改善率 < C または D の悪化率 | 撤退または縮退 |
| **C** | 観測窓終了時に**因果を識別できなかった**（交絡） | 撤退（学習成果を生まなかったため） |

**この除外は現時点で確定済みの3件に限定した限定的措置であり、将来の一般ルールではない。**
今後新たに「必須」と裁定される施策をこの除外へ含めるかどうかは、そのたびにオーナー判断を仰ぐ
（本エントリを自動的に拡張しない）。

**撤退条件C は [リポジトリの目的はAI駆動開発ノウハウ獲得とする](#fbk-20260921-リポジトリの目的はai駆動開発ノウハウ獲得とする) の目的に固有**である。品質目的なら「理由は分からないが良くなったからよし」で
通るが、学習目的では因果の不明な改善は成果にならない。

**点検時点で、最大の「成果」である 0.68 → 0.37 が既に撤退条件C に該当している**（#370 本文の自白）：

> この改善は本 Issue の対策によるものではない——間に #406 / #407 と機械ゲート群の整備が入っており、**因果は特定できていない**。

**経緯（周辺文脈）**

前バージョン（[撤退条件をA/B/Cの3種で定める](#fbk-20260921-撤退条件をa-b-cの3種で定める)・§B参照）は、
条件Aを無条件の例外なしとしていた。Opus 5.5サブエージェントへ決定台帳全体の内部矛盾点検を委譲した
ところ、条件Aが現在値（Q=0.37）から下限（0.01）へ到達するまで約29週間を要する間、オーナーが
明示的に必須と裁定した3件の施策までもが条件A発動時に無条件撤退の対象になってしまうことが判明した
——「必須」という裁定自体を、まだ発動していない将来条件が上書きしてしまう矛盾。

**オーナー逐語**

> 撤退条件A vs 必須施策については、現時点の必須事項についてのみb

（b＝「必須施策は条件Aの対象外とする例外を明記する」選択肢。「現時点の必須事項についてのみ」との
限定がついたため、将来の必須施策には自動適用しない範囲を明記した。）

**AI の原案と、オーナーが変えた点**

矛盾の発見・選択肢の提示（例外を設けるか/条件Aの基準自体を変えるか）はAI（Opus 5.5サブエージェント
経由）。「b（例外を明記）」の採用と、それを現時点の3件に限定する範囲設定はオーナーが判断した。

---

<a id="fbk-20260921-施策ごとに観測窓を切る"></a>

### 施策ごとに観測窓を切る

| | |
|---|---|
| **id** | `FBK-20260921-施策ごとに観測窓を切る` |
| **決定日時** | 2026-09-21 |
| **状態** | **生存** |
| **置換** | （新規） |
| **関連** | [撤退条件Aから現行必須施策を除外する](#fbk-20260924-撤退条件aから現行必須施策を除外する) の撤退条件C を判定可能にする前提 |

**決定内容**

- 施策ごとに観測窓を切る。
- 観測窓が開いている間は、**同じ指標に効く別の施策を入れない**。
- 入れざるを得ない場合は観測窓を閉じ、「交絡あり・判定不能」と記録する（＝撤退条件C）。
- **施策レジストリ**（施策ID / 投入日 / 観測窓 / 仮説 / 結果 / 判定）を本ディレクトリに置く。

**この方針は #445 の Wave 設計と正面から衝突する**（Wave 0 が #374/#386/#407/#452 を並行投入する形）。
#445 の Wave 構成は「観測窓の列」へ書き直す必要がある。

**経緯（周辺文脈）**

点検時点では「いつ何を入れたか」の一覧がどこにも存在せず、`git log` からの事後再構成しかできなかった。
これが #370 が因果を特定できなかった直接の原因である。

**オーナー逐語**

> 施策毎に観測窓はいれるのでいいと思う。

**AI の原案と、オーナーが変えた点**

AI が提示した案をオーナーがそのまま承認した（変更なし）。

---

<a id="fbk-20260921-issue371の作成工程分離は必須とする"></a>

### Issue371の作成工程分離は必須とする

| | |
|---|---|
| **id** | `FBK-20260921-issue371の作成工程分離は必須とする` |
| **決定日時** | 2026-09-21 |
| **状態** | **生存** |
| **置換** | **[Issue371と409と513を非推奨とする](#fbk-20260921-issue371と409と513を非推奨とする) を置換** |
| **関連** | [Issue371は軽い修正でテーラリング可とする](#fbk-20260921-issue371は軽い修正でテーラリング可とする)（テーラリング）が同じ Issue に掛かる。[必須施策を5項目に定める](#fbk-20260921-必須施策を5項目に定める) の①に対応 |

**決定内容**

> **#371 は必須。** A-SPICE でいう SYS / SWE 工程の実施にあたるプロセス補強である。

**経緯（周辺文脈）**

AI は「再発21件のうち67%がゲート機構由来」という実測から「検出強化（ゲート追加）は Q 下限 1% に
原理的に届かない」と結論し、**その例として #371 を非推奨に分類した**。オーナーがこの分類を否定した。

AI の分類の誤り（訂正の実施者＝Claude Code (AI)）：

- **#371 は検出器の追加ではなく工程の定義である。** investigation / planning / plan_review は
  **V字の左側**（要求分析・アーキテクチャ設計）にあたる。点検時点のプロセスには左側が存在せず、
  **左側が無いから右側（検証）をゲートで盛るしかなくなっていた。**
  「再発の67%がゲート由来」は、**左側の欠如が右側の肥大として現れた結果**と読むのが正しい。
- 「ゲートを足すな」と「工程を定義しろ」を同じ箱に入れていた。

**オーナー逐語**

> 検出強化を避けるべきはそのとおりだがら例示の371,513はやるぞ？A-SPICEベースで考えたら今のプロセスなんか遊びと変わらんし、品質低いのも当然だ。

> 重いロールを軽い修正では免除するという方向での調整(テーラリング)は余地ありだが、プロセス補強のために371は必須。

**AI の原案と、オーナーが変えた点**

AI は #371 を「コスト2倍・回転半減に見合わない」として撤退側に分類した。オーナーは A-SPICE の
プロセス成熟度の観点から、これを**工程の欠落を埋める必須施策**として位置づけ直した。

---

<a id="fbk-20260921-issue371は軽い修正でテーラリング可とする"></a>

### Issue371は軽い修正でテーラリング可とする

| | |
|---|---|
| **id** | `FBK-20260921-issue371は軽い修正でテーラリング可とする` |
| **決定日時** | 2026-09-21 |
| **状態** | **生存**（方向は確定。判定軸は未決＝U-1） |
| **置換** | （新規） |
| **関連** | [Issue371の作成工程分離は必須とする](#fbk-20260921-issue371の作成工程分離は必須とする) と同じ Issue。#371 本文の内部矛盾の処置先 |

**決定内容**

> 重いロールを**軽い修正では免除する**という方向での調整（テーラリング）には余地がある。

**経緯（周辺文脈）**

#371 本文には内部矛盾がある——目的節が「**小 Issue に不要な dispatch を強制せず**、品質条件を満たす
範囲でコストを抑える」と書く一方、ロール構成表は「`change-planner` は**全 Issue で必須**」としている。
さらに `plan-reviewer` の起動条件は11条件の OR で、open harness Issue 50件の大半が
「共有資産」「権限・セキュリティ」「fail-close」のいずれかに該当するため、**実質常時起動**になる。

結果、`pr_merge_gate` の classifier に1行足すだけの #533 でも
`investigation → planning → plan_review → implementation → review → remediation → re-review → merge`
の最低8 dispatch が掛かる。

**オーナー逐語**

> 重いロールを軽い修正では免除するという方向での調整(テーラリング)は余地ありだが、プロセス補強のために371は必須。

**AI の原案と、オーナーが変えた点**

AI は #371 本文の内部矛盾を指摘し「B-2：`change-planner` を必須から条件付きへ落とす／`plan-reviewer` は
当面入れない」を推奨していた。オーナーは #371 の実施自体は必須としたうえで、テーラリングの余地を
認めた。**判定軸は未決**（AI の推奨＝行数やファイル数ではなく「アーキテクチャに影響するか＝SWE.2 の
成果物を更新するか」で切る。1行でもアーキテクチャを変える変更を取り逃さないため）。

---

<a id="fbk-20260923-軽量経路は分析省略でなく分析縮小とする"></a>

### 軽量経路は分析省略でなく分析縮小とする

| | |
|---|---|
| **id** | `FBK-20260923-軽量経路は分析省略でなく分析縮小とする` |
| **決定日時** | 2026-09-23 |
| **状態** | **生存** |
| **置換** | （新規。[Issue371は軽い修正でテーラリング可とする](#fbk-20260921-issue371は軽い修正でテーラリング可とする) の判定軸に制約を追加。方向自体は置換しない） |
| **関連** | U-1（判定軸そのものの深掘りは優先度を下げる） |

**決定内容**

> **軽量経路を残すなら、「分析を省略する」のではなく「分析自体を軽量版で必ず実施する」**
> **（スキップではなく縮小）という形にする。**
>
> U-1（#371 のテーラリング判定軸そのものの設計）は優先度を下げる。

**経緯（周辺文脈）**

AI は U-1 の推奨として「行数やファイル数ではなく『アーキテクチャに影響するか』で切る」を出したが、
この判定自体をどう安く行うかが未解決だった。**「軽いかどうか」は、まさに軽量経路で省略しようとしている
分析（アーキテクチャへの影響評価）を経て初めて分かることであり、その分析より前に軽重を判定しようとする
発想自体が循環している。**

**オーナー逐語**

> あとu-1は優先度めっちゃ低いぞ。
> 一行しか変えなくてよい、は、上位工程を経た結果わかることであって、そこを飛ばすのは素人の勘に過ぎない。たまたま1行でよかったことはあるがしれないが、漏らして不具合を出す可能性が高い愚行。

**AI の原案と、オーナーが変えた点**

AI は「表面的シグナルで軽重を判定する基準」を U-1 の解として探していた。**オーナーが、この探し方
自体が誤りだと訂正した。** 正しい形は「分析をゼロにする」ではなく「分析を安く・速く行う」——
軽量経路であっても、上位工程（要件定義+アーキ設計）の縮小版は必ず通す。U-1（具体的な縮小の設計）は
#371 改訂の中で扱うが、優先度は下げる。

---

<a id="fbk-20260922-工程粒度は要件定義+アーキ設計統合・詳細設計廃止とする"></a>

### 工程粒度は要件定義+アーキ設計統合・詳細設計廃止とする

| | |
|---|---|
| **id** | `FBK-20260922-工程粒度は要件定義+アーキ設計統合・詳細設計廃止とする` |
| **決定日時** | 2026-09-22 |
| **状態** | **生存** |
| **置換** | （新規） |
| **関連** | [Issue371の作成工程分離は必須とする](#fbk-20260921-issue371の作成工程分離は必須とする) の具体化。[軽量経路は分析省略でなく分析縮小とする](#fbk-20260923-軽量経路は分析省略でなく分析縮小とする) が縮小版の対象とする「上位工程（要件定義+アーキ設計）」はこのフェーズ粒度を指す |
| **実行** | **#371**（未反映・本改訂で書き込む） |

**決定内容**

> #371 のフェーズ構成は、**要件定義とアーキテクチャ設計を1フェーズに統合**する（`change-planner` のまま）。
> **詳細設計は独立フェーズとして立てない。** 必要な設計判断は実装フェーズに吸収し、**成果物は ADR のみ**と
> する（シーケンス図等の詳細設計ドキュメントは作らない）。

**経緯（周辺文脈）**

オーナーが #371 の位置づけを A-SPICE 準拠のプロセス補強として確定した後、AI が「①A-SPICE を参考に
プロセスを作る」という大枠の下で、上位工程（要件定義・アーキ設計）と下位工程（詳細設計・実装）を
どこまで個別のフェーズに分けるかを問うた。既存 #371 は investigation/planning/plan_review/implementation
の4フェーズ構成だったが、要件定義とアーキ設計を分けるかどうかは規定していなかった。

**オーナー逐語**

> イメージとしては、要件定義でスコープとアウトオブスコープを決めて、アーキ設計で責務分割やIFとADRと結合検査設計をして、詳細設計(or実装)で素直なTDDとADR作って、V字の右側ではテストエビ作るのとバグ修正ループ回すのを主とする。

> あとは、上位工程を何処まで個別にするかだな。個人的にはchange-plannerみたいに要件定義とアーキ設計は一緒にしてもいいんじゃないかと思ってる。で、詳細設計はそもそも要らない派。作るにしてもシーケンス図じゃなくてADRでええやん(笑)

**AI の原案と、オーナーが変えた点**

AI は各工程の成果物・レビュー方法を問う形で選択肢を残していた。オーナーが要件定義+アーキ設計の統合と
詳細設計の廃止（ADR のみへの置換）を直接指定し、AI はこれをそのまま採用して後続の一覧
（[検証エビは全レベルでTD-TC-TRを揃える](#fbk-20260924-検証エビは全レベルでtd-tc-trを揃える)）へ反映した。

---

<a id="fbk-20260922-フェーズ内はテスト設計先行のフラクタル構造とする"></a>

### フェーズ内はテスト設計先行のフラクタル構造とする

| | |
|---|---|
| **id** | `FBK-20260922-フェーズ内はテスト設計先行のフラクタル構造とする` |
| **決定日時** | 2026-09-22 |
| **状態** | **生存** |
| **置換** | （新規） |
| **関連** | [AC妥当性確認はplan-reviewerを強化して担う](#fbk-20260922-ac妥当性確認はplan-reviewerを強化して担う) の後付けAC問題への構造的対策。[検証エビは全レベルでTD-TC-TRを揃える](#fbk-20260924-検証エビは全レベルでtd-tc-trを揃える) の1a/2a行に対応（2026-09-24訂正でレビュー分離を明文化済み） |
| **実行** | **#371**（未反映） |

**決定内容**

> 各フェーズ内で、**先に検証設計（AC・テストケース）を作りレビューしてから、本体の成果物を作りレビューする**。
> TDD の要素は「実装時に赤緑を回す」ことではなく、**各工程で右側（検証観点）を先に固定してから左側
> （解決策）を作る**という順序として工程設計に取り込む。

**経緯（周辺文脈）**

オーナーの元発言「TDD の要素を取り入れる」は実装フェーズの Red/Green サイクルとも読めたが、AI が確認
したところオーナーの意図は各工程内の順序（検証観点を先に固定する）だった。この順序は、#371 自身で
見つけた AC 妥当性問題（解決策を先に作ると、それに合わせて AC を後付けで歪める確証バイアスが起きる）
への直接の対策になる。

**オーナー逐語**

> tddは実装時にやるというより、各工程の右側の設計やってから左側をやるイメージのつもり。で、工程内でテスト設計のレビューしてから実際の工程成果物作ってレビューするイメージ。そしたらV&Vで検証できると思う。

**AI の原案と、オーナーが変えた点**

AI の当初の解釈は実装時 TDD（Red/Green）への言及だった。オーナーが「工程内の順序」という正しい解釈を
明示し、AI はこれを確認・採用した。

---

<a id="fbk-20260924-検証エビは全レベルでtd-tc-trを揃える"></a>

### 検証エビは全レベルでTD-TC-TRを揃える

| | |
|---|---|
| **id** | `FBK-20260924-検証エビは全レベルでtd-tc-trを揃える` |
| **決定日時** | 2026-09-24 |
| **状態** | **生存**。IT（結合検査）水準の review_system 側実装状況・各行の受け渡し方式は v4（§B）から変更なく引き継ぐ |
| **置換** | **[1a1bと2a2bのレビューを分離し無条件化する](#fbk-20260924-1a1bと2a2bのレビューを分離し無条件化する) を置換**（v5→v6） |
| **関連** | [工程粒度は要件定義+アーキ設計統合・詳細設計廃止とする](#fbk-20260922-工程粒度は要件定義+アーキ設計統合・詳細設計廃止とする)・[フェーズ内はテスト設計先行のフラクタル構造とする](#fbk-20260922-フェーズ内はテスト設計先行のフラクタル構造とする) の具体化。[AC妥当性確認はplan-reviewerを強化して担う](#fbk-20260922-ac妥当性確認はplan-reviewerを強化して担う)（1b行の無条件化根拠）・[harnessのテストエビはtest-strategyを手本にする](#fbk-20260923-harnessのテストエビはtest-strategyを手本にする)（3a行）・[マージ権限をAIから剥がしsettings.jsonへ移す](#fbk-20260923-マージ権限をaiから剥がしsettings.jsonへ移す)（各行の権限） |
| **実行** | **#371**（未反映。本表をたたき台に本文を書き直す）／**#543**（review_system 側 test-strategy へのレベル導入・2026-09-23 起票） |

**決定内容**

| # | 工程 | 成果物 | レビュー方法 | 担当ロール |
|---|---|---|---|---|
| **1a** | 要件定義+アーキ設計：検証設計を先に作る | 新設 `VerificationDesign`：**`ST-TD`**（AC・受け入れ基準）＋**`IT-TD`**（結合検査設計）＋**`ST-TC`・`IT-TC`**（システム/結合レベルのテストケース。実装前に確定できるため設計時にあわせて作成する。`UT-TC` と異なり Red/Green 運用ではなく「実装完了後に実行する検証手順」として書く） | **`plan-reviewer` が単独でレビューする（無条件）。1b の着手前に完了させる** | `ac-designer`（新設・1a専任） |
| **1b** | 要件定義+アーキ設計：本体 | `ChangePlan`（`selected_solution`＝スコープ＋責務分割を含む解決方針・`OOS`・`interfaces`・`invariants`・`failure_behavior`・`work_units DAG`。**`AC・test mapping` と `rationale` は 1a の `VerificationDesign`／ADR へ移したため除く**）＋ **ADR**（`phase=要件定義+アーキ設計`。`input_refs` で 1a の `VerificationDesign` を参照） | **`plan-reviewer` が 1a↔1b の整合＋既存決定との矛盾を検証する（無条件。risk gate 条件は撤廃）。1a のレビュー合格後に着手する** | `change-planner`（既存）→`plan-reviewer`（既存） |
| **2a** | 実装：テスト設計を先に作る | **`UT-TD`**（単体テスト設計。review_system 既存 `test-strategy` の `tests/designs/` に相当）＋**`UT-TC`**（1a の `IT-TD` の対象範囲内で、単体レベルのテストケース。Red 状態の実コードとして存在） | **`pr-reviewer` が単独でレビューする（無条件）。2b の着手前に完了させる** | `test-writer`（新設・2a専任） |
| **2b** | 実装：本体 | 実装コード（Green）＋必要なら軽量 **ADR**（`phase=実装`。シーケンス図等は作らない） | `pr-reviewer`（既存・無条件） | `issue-implementer`（既存） |
| **3a** | V字右側：テストエビ | **`ST-TR`／`IT-TR`／`UT-TR`**（各レベルの TC 実行結果。review_system 側は既存 `test-strategy` の UT/e2e 運用を流用——**IT 水準は形式的な区分としては無いが、`test_apply.py` 等の一部テストが実質的に結合レベルを既に含む**。harness 側の**道具ごとの置き場**（ADR の `karte/adr/` 等に相当する `<tool>/test-strategy/`）**は未決＝U-14**） | 既存の成績書運用 | 既存資産の流用 |
| **3b** | V字右側：バグ修正ループ | `karte` の診断→是正 | `issue-fixer`→`pr-reviewer` 再レビュー（既存） | 既存資産の流用 |

「詳細設計」は独立行として立てない（2b に吸収、成果物は ADR のみ）。**1a/2a のレビューと 1b/2b の
レビューは別 dispatch とする**（コスト：レビュー dispatch が 1a/1b/2a/2b の計4回に増える。#371 の
非目標「軽量 Issue への強制回避」とのトレードオフをオーナーが受け入れた）。

**各レベルの TD→TC→TR の鎖が全て揃う**：UT は 2a（TD+TC・Red）→2b（実装）→3a（TR・Green確認）。
IT は 1a（TD+TC）→2b（実装に含まれる）→3a（TR）。ST は 1a（TD＝AC+TC）→2b（実装に含まれる）→3a（TR）。

**経緯（周辺文脈）**

前々バージョン（v4・§B参照）は、1a/2a行を「1b/2bとまとめてレビュー」、1b行の`plan-reviewer`を
「risk gate該当時」の条件付きとしていた（Opus 5.5サブエージェントの点検で発見・v5で訂正済み）。
前バージョン（v5・§B参照）はレビュー方法の訂正のみで、`ST-TC`/`IT-TC`/`UT-TD`を欠いたまま
`ST-TR`/`IT-TR`/`UT-TR`だけが3aに現れる非対称な鎖になっていた——同じOpus 5.5サブエージェントの点検が
指摘し、オーナーが「モデルの欠落だと思う」と裁定した。本エントリでTD→TC→TRの鎖を全レベルで揃えた。

table構造・成果物名・検査レベルプレフィックス・レビュー方法（v1〜v5で確定した部分）は変更なし。
変更は成果物列への`ST-TC`/`IT-TC`（1a）・`UT-TD`（2a）の追加のみ。

**オーナー逐語**

> ドリフト⑦はモデルの欠落だとおもう。

**AI の原案と、オーナーが変えた点**

欠落の発見はAI（Opus 5.5サブエージェント経由）。「意図的な省略か欠落か」の判断をオーナーへ仰いだところ
「欠落」と裁定されたため、AIが埋め方（1aへST-TC/IT-TCを追加・2aへUT-TDを追加）を設計した。

**追記（2026-09-23）：各行の受け渡し方式**——1a〜2bは`tmp/_handoff/`を経由しない。1a
（`ac-designer`）が`gitgate new-branch`でworktreeを新規に作り、1b/2a/2b（`change-planner`/
`test-writer`/`issue-implementer`）は順に`gitgate adopt-branch`で同一worktreeへ合流する。
各ロールは自分の成果物をworktree内の永続パスへ直接Writeし、自分でcommit+pushする（詳細・
権限表は[成果物ロールは各自pushし直接コミットする](#fbk-20260923-成果物ロールは各自pushし直接コミットする)）。

---

<a id="fbk-20260922-1a-2aと1b-2bはロール分離を必須とする"></a>

### 1a/2aと1b/2bはロール分離を必須とする

| | |
|---|---|
| **id** | `FBK-20260922-1a-2aと1b-2bはロール分離を必須とする` |
| **決定日時** | 2026-09-22 |
| **状態** | **生存** |
| **置換** | （新規。深掘り論点(A)を解消） |
| **関連** | [検証エビは全レベルでTD-TC-TRを揃える](#fbk-20260924-検証エビは全レベルでtd-tc-trを揃える) の1a/1b・2a/2b区分に対応。U-1（テーラリング判定軸）の重みを増やす |
| **実行** | **#371**（未反映） |

**決定内容**

> **検証設計を書く工程（1a/2a）と、本体成果物を書く工程（1b/2b）は、別ロール（別コンテキスト）で
> 実施することを必須とする。**
>
> 同一ロールが両方を書く場合、「順序を守った」という規律だけに頼ることになり、#371 自身の
> AC 自己認定問題と同じ構造が形を変えて残るため。

**経緯（周辺文脈）**

AI は「1a/2a と 1b/2b を同一ロールにするか分けるか」を深掘りが必要な未決事項として提示した
（分けると dispatch が #371 の見積りに対しさらに増え、非目標＝軽量 Issue への強制回避と衝突するため）。
オーナーが即座に「必須」と裁定した。

**オーナー逐語**

> tddの順序の話は1a/2aと1b/2bはロールというかコンテキストを分けるのは必須。

**AI の原案と、オーナーが変えた点**

AI は「機械的な代替チェックで自己認定問題を埋める案」を対案として持っていたが、オーナーはロール分離
そのものを必須とし、コスト増を受け入れる形で裁定した。コスト増（dispatch 増）は #371 のテーラリング
判定軸（U-1）の設計へそのまま重みとして持ち越される。

---

<a id="fbk-20260923-トレサビはref宣言駆動の共通アダプタで実現する"></a>

### トレサビはref宣言駆動の共通アダプタで実現する

| | |
|---|---|
| **id** | `FBK-20260923-トレサビはref宣言駆動の共通アダプタで実現する` |
| **決定日時** | 2026-09-23 |
| **状態** | **生存** |
| **置換** | **[トレサビは参照方式+doc_system水準の査定を必須とする](#fbk-20260922-トレサビは参照方式+doc_system水準の査定を必須とする) を置換** |
| **関連** | [検証エビは全レベルでTD-TC-TRを揃える](#fbk-20260924-検証エビは全レベルでtd-tc-trを揃える) のトレーサビリティ機構。[成果物ロールは各自pushし直接コミットする](#fbk-20260923-成果物ロールは各自pushし直接コミットする) により ChangePlan/VerificationDesign が永続化されるため索引対象に含める |
| **実行** | **#540**（本文訂正として反映済み・2026-09-24） |

**決定内容**

> トレーサビリティは4案（A：doc-system-v2 流用／B：envelope の `input_refs` 等の参照のみ／
> C：GitHub ネイティブ／D：feedback_ledger 型軽量インデックス）を比較した結果、**B を出発点とする**。
> **ただし B の選択そのものはオーナーの確信度が低い（「B でもいいけど」程度の温度感）**——
> **固定された前提は「索引/整合検査を doc_system 同水準で必須にする」ことであって、B という
> 具体案そのものではない。** B は現時点の妥当な出発点にすぎず、後段の実現可能性検証の結果によっては
> 変更の余地がある。
>
> **「著作の重さ」（`*-author`→`reconciliation-validator`→`reconciliation` の2段階）は持ち込まない**
> 一方、**「索引/整合検査の重さ」（`dsv2 index`/`deps`/`dependents` 相当の機構）は doc_system 同水準で
> 必須**とする。含有されない汎用ハーネスだからといって検査水準を落とさない。
> 逆引きクエリが実際に必要になったら D（feedback_ledger 型軽量インデックス）を追加する。

**実現可能性の検証（2026-09-23・Claude Code (AI)・「出来んの？」への回答）**

**結論：技術的に可能。`dsv2/query.py` の `deps`/`dependents`/`orphans` は doc-system-v2 の
YAML ノード形式に依存しない純関数であることをソースで確認した。**

```
$ grep -n "^def " dsv2/query.py
deps(meta: dict, node_id: str) -> list[dict] | None
dependents(meta: dict, node_id: str) -> list[dict]
orphans(meta: dict) -> list[dict]
```

これら3関数はいずれも `meta = {"nodes": [{"id", "type", "edges": [{"to", "ref_version", "note"}], ...}]}`
という**抽象的な辞書形状**だけを引数に取る（`dsv2/meta.py` の `LAYOUT`/`STATUS_DIRS`（doc-system-v2 の
ディレクトリ構造検証）や `nodeyaml.py`（`<details><yaml>` サイドカー構文）には一切触れない）。
`dependents` が `src["yaml_path"]` を参照する1箇所のみ軽微な依存（アダプタ側で空文字を入れれば足りる）。

**つまり、doc-system-v2 の重い部分（著作パイプライン・ディレクトリレイアウト検証・YAML サイドカー構文）を
経由せずに、`dsv2.query` の3関数をライブラリとしてそのまま import して再利用できる。**

具体設計（案）：

1. ADR（TOML・`[[source]]`/`supersedes`/`affected_assets`）・feedback_ledger（TOML・`[[source]]`/
   `supersedes`）・#371 の共通 envelope（`input_refs`）など、harness 側の各データ源ごとに**薄いアダプタ**
   を書く。アダプタの責務は「その形式のレコードを読み、`{id, type, edges, version, yaml_path}` の
   抽象形へ変換する」だけ（新規パーサは要るが、doc-system-v2 の著作パイプラインとは無関係）。
2. アダプタが出した `meta` 辞書を `dsv2.query.deps`/`dependents`/`orphans` へそのまま渡す
   （**コードの複製ではなく import**）。
3. 辺の存在検証（`deps` の `exists` フィールド）・孤立検出（`orphans`）・逆引き（`dependents`）が
   これだけで doc_system と同じ精度で動く。

**アダプタ実装方式の再検討（2026-09-23・Claude Code (AI)）**

`feedback_ledger/schema.py` を実際に読んだところ、当初の想定より良い設計があることが分かった。
`Field` データクラスは最初から `ref: str | None`（値が他文書の id を指す欄の種別）を持っており、
実際の宣言（`supersedes`→`LEDGER`、`derived_from`→`LEDGER`、`decided_in`→`TRIAGE`、
`entry`/`merged_into`→`LEDGER`、`proposal`→`PROPOSAL`）が**既に辺の宣言そのもの**になっている
（feedback_ledger 自身の L3＝参照先実在検査のために存在する宣言で、新設不要）。

| 案 | 内容 | 評価 |
|---|---|---|
| i | フィールド名（`[[source]]`/`supersedes`）を決め打ちする汎用アダプタ1本 | **却下**——スキーマ乖離に弱い |
| ii | データ源ごとに個別アダプタを手書き（前回の推奨） | 安全だが3本のロジックが分散する |
| **iii** | **`DocSpec.field_map()` の `ref=` 宣言を読んで動く共通関数1本。各データ源は自分の `DocSpec` 宣言とTOML I/Oだけ用意する** | **採用**——フィールド名を一切ハードコードせず、各スキーマが自分で宣言した `ref=` を読むだけなので、i の弱点（乖離への弱さ）を持たない |

**iii を採用する。** 前提は「ADR（`adr/schema.py`・#540 で新設予定）が feedback_ledger と同じ
`Field`/`TableSpec`/`DocSpec` パターンを踏襲すること」——これは #540 の既定方針
（[ADRはコード共有・データは道具ごとに分離する](#fbk-20260923-adrはコード共有・データは道具ごとに分離する)
の「造りを真似する」）そのものなので、前提は崩れない。

**索引対象の見直し**：当初は #371 の共通 envelope（ChangePlan/VerificationDesign）を
「`tmp/_handoff/` の一時ファイルで、work unit 完了後に消える」という前提で索引対象外としていたが、
[成果物ロールは各自pushし直接コミットする](#fbk-20260923-成果物ロールは各自pushし直接コミットする)
により **ChangePlan/VerificationDesign は最初から worktree 内の永続パスへ直接 commit される
（tmp を経由しない）**ため、この前提が崩れた。**ADR/feedback_ledger と同様に索引対象へ含める。**
それぞれのスキーマ（`input_refs` 等）に `ref=` 相当の宣言を持たせれば、同じ共通関数（案iii）で
そのまま扱える。

**経緯（周辺文脈）**

AI は「issue-pipeline は doc_system/review_system に含有されない」という起票先判定表の境界を、
doc-system-v2 流用を排除する不変の壁として扱い、「重い機構だから使わない」で止めていた。オーナーが
この扱い自体を誤りとして訂正し、「軽くして使うでもいいし、思想だけ使い回すでもいい」
「今のdoc_systemと同レベルのチェック機構は必須」と、境界の意味を再定義した。AI はこれを受けて
「著作の重さ」と「索引/整合検査の重さ」を分離し、前者は持ち込まず後者は同水準で必須とする設計に
修正した。

**オーナー逐語**

> トレサビは『.claude/rules/02-decision-process.md の起票先判定表は「issue-pipeline は doc_system/review_system のどちらにも含有されない」』は、未完成だから今まではそうしてただけで、そうしなくてはならないでも、ずっとそうするつもりでもない。もっと柔軟に考えてください。シンプルに機構として重いというのは分からんでもないが、軽くして使うでもいいし、思想だけ使い回すでもいい。結局bにするでもいいけどクエリ層というか、今のdoc_systemと同レベルのチェック機構は必須では？doc_systemも作りかけでまだまだ機能不足の認識だから、aにしろbにしろ、機能不足です。

**AI の原案と、オーナーが変えた点**

AI の原案は「B（envelope 参照のみ・新規ツール0）で始め、痛くなったら D を足す」という、クエリ層の
必須性に触れない YAGNI 一辺倒の推奨だった。オーナーが「今の doc_system と同レベルのチェック機構は
必須」と、YAGNI で先送りしてよい範囲を「著作」に限定し、「索引/整合検査」は先送り不可と訂正した。

**追訂正（2026-09-23・Claude Code (AI)）**：本エントリの初版は「B を出発点とする」を確定的な選択
として記述していたが、オーナーが「『B でもいいけど』くらいの温度感」と自身の確信度を明示した。
**固定された前提は doc_system 同水準の索引/整合検査であって、B という具体案の選択度合いは低い。**
これを「決定内容」節に反映した。あわせて「出来んの？（実現可能か）」という問いに対し、
`dsv2/query.py` を実際に読んで技術的な裏付けを取った（上記「実現可能性の検証」節）——
確認前は「同水準の機構を作る」と言っただけで実現手段を検証していなかった。

**本訂正（2026-09-23・Claude Code (AI)）**：AI が「データ源ごとの個別アダプタ（案ii）を推奨」と
まとめたのに対し、オーナーが「envelope消すならトレサビどうやって取るの？」と、索引対象からの
envelope除外そのものを問い直した。この指摘は
[成果物ロールは各自pushし直接コミットする](#fbk-20260923-成果物ロールは各自pushし直接コミットする)
の訂正（tmp経由をやめ worktree へ直接 commit する）と連動し、AI は (a) `feedback_ledger/schema.py`
の `ref=` 宣言を読む案iiiへの実装方式の格上げ、(b) envelope を索引対象へ戻す、の2点を訂正した。

**オーナー逐語（本訂正）**

> envelope消すならトレサビどうやって取るの？

---

<a id="fbk-20260923-成果物ロールは各自pushし直接コミットする"></a>

### 成果物ロールは各自pushし直接コミットする

| | |
|---|---|
| **id** | `FBK-20260923-成果物ロールは各自pushし直接コミットする` |
| **決定日時** | 2026-09-23 |
| **状態** | **生存** |
| **置換** | **[権限設計は工程表の各ロールへ役割必須性で接続する](#fbk-20260922-権限設計は工程表の各ロールへ役割必須性で接続する) を置換** |
| **関連** | [検証エビは全レベルでTD-TC-TRを揃える](#fbk-20260924-検証エビは全レベルでtd-tc-trを揃える) の各ロールに紐づく。[マージ権限をAIから剥がしsettings.jsonへ移す](#fbk-20260923-マージ権限をaiから剥がしsettings.jsonへ移す) は影響を受けない（merge と push は別軸） |
| **実行** | **#542**（マージ権限の結論を実行済み・起票済み）／**#371**（push 側も本文へ反映済み・2026-09-24 確認） |

**決定内容**

> ②（権限設計）は、工程一覧（1a/1b/2a/2b/3a/3b）の各ロールへ紐づけて設計する。**既存の権限配分
> （現状の形）は前提にせず、「役割として必須か」「戻すのは容易か」の2基準で一から設計し直す。**
>
> **成果物を作るロールは全員 push を持ち、自分の成果物を自分で commit+push する。
> レビュー専任ロール（`plan-reviewer`／`pr-reviewer`）だけが push を持たない。**

各ロールへの適用結果（2026-09-23 時点の再点検）：

| 役割 | push | merge | 必須か | 戻すのは容易か | 結論 |
|---|---|---|---|---|---|
| `issue-investigator` | 無 | 無 | 調査は書込不要 | — | 変更なし（既に最小） |
| `ac-designer`（1a） | **有** | 無 | worktree へ直接 `VerificationDesign` を書き、自分で commit+push する。1a は `gitgate new-branch` で worktree を新規に作る | — | **付与** |
| `change-planner`（1b） | **有** | 無 | 同一 worktree へ `gitgate adopt-branch` で合流し、`ChangePlan` を書いて自分で commit+push する | — | **付与** |
| `plan-reviewer` | 無 | 無 | レビューは書込不要——push された branch を直接読む（PR 未オープンでも `git log`/`git diff` で読める） | — | 変更なし（既に最小） |
| `test-writer`（2a） | **有** | 無 | 同一 worktree へ合流し、Red テストを書いて自分で commit+push する | — | **付与** |
| `issue-implementer`（2b） | 有 | 無 | 同一 worktree へ合流し、実装を commit+push。ここで初めて PR を開く（既存の役割のまま） | — | 変更なし |
| `issue-fixer`（3b） | 有 | 無 | 同上（既存の adopt-branch 運用のまま） | — | 変更なし |
| `pr-reviewer` | 無 | 有 | **必須ではない**——判定を出すことと merge を実行することは別行為 | **容易**——hook 側の設定変更のみで戻せる | **剥がす**（既存決定・変更なし） |

**この点検の結論のうち merge 側は [マージ権限をAIから剥がしsettings.jsonへ移す](#fbk-20260923-マージ権限をaiから剥がしsettings.jsonへ移す) として既に実行済み。push 側（ac-designer/change-planner/test-writer への付与）は本訂正で新たに確定した。**

**経緯（周辺文脈）**

前バージョンは、1a/1b/2a を「push 不要・handoff artifact で足りる」と判定していた——これは
`tmp/_handoff/` 経由で成果物を渡し、最終的に `issue-implementer` が代表してまとめて commit する
という**誤った workflow 前提**（[検証エビはTD-TC-TRの単一鎖とする](#fbk-20260923-検証エビはtd-tc-trの単一鎖とする)
系の訂正過程で見つかった、tmp を経由させる必要が無いという指摘）の上に組み立てられていた。

オーナーが「成果物作ってコミットしないのは意味不明」と、**tmp 経由という前提そのものではなく、
push を持たない役割設計そのものを**指摘した。既存の `issue-fixer` が既に持つ
「`gitgate adopt-branch` で既存 worktree へ合流し、自分の変更を自分で commit+push する」という
パターンを 1a/1b/2a にもそのまま適用すれば、tmp 経由も「誰かが代表して commit する」設計も不要になる
——**1a が `gitgate new-branch` で worktree を新規に作り、1b/2a/2b が順に `adopt-branch` で合流する**。
各ロールは worktree 内の永続パスへ直接 Write し、自分で commit+push する。

**オーナー逐語**

> 各ロールにもコミット・push権限が必要では？レビュー役は兎も角、成果物作ってコミットしないのは意味不明。

**AI の原案と、オーナーが変えた点**

AI は直前のターンで「tmp 経由は不要（shared worktree へ直接書く）」までは訂正していたが、
**「誰が commit するか」を `issue-implementer` に一元化したままにしていた**——「1a/1b/2a は worktree
へ直接書くが、commit は最後にまとめて `issue-implementer` が行う」という案。オーナーがこれ自体を
「成果物を作った本人が commit しないのはおかしい」と訂正し、AI は `issue-fixer` の
`adopt-branch` パターンを他の全成果物ロールへ一般化し、push を必須へ変更した。

---

<a id="fbk-20260924-枠組みフィールドは対象prefixなしで置く"></a>

### 枠組みフィールドは対象prefixなしで置く

| | |
|---|---|
| **id** | `FBK-20260924-枠組みフィールドは対象prefixなしで置く` |
| **決定日時** | 2026-09-24 |
| **状態** | **生存** |
| **置換** | **[envelopeは対象ごとにwork_unit分置する](#fbk-20260923-envelopeは対象ごとにwork_unit分置する) を置換**（v1→v2） |
| **関連** | [ADRはコード共有・データは道具ごとに分離する](#fbk-20260923-adrはコード共有・データは道具ごとに分離する) と同じ「コード共有・データは発生源ごとに分離」原則の`ChangePlan`/`VerificationDesign`への適用。[トレサビはref宣言駆動の共通アダプタで実現する](#fbk-20260923-トレサビはref宣言駆動の共通アダプタで実現する) の索引対象 |
| **実行** | **#371**（本文へ反映済み）／**#540**（`ChangePlan`/`VerificationDesign`を索引対象に明示・2026-09-24 反映） |

**決定内容**

> v1（`work_units DAG`の各ノードを対象ごとに分置する設計）はそのまま維持する。**加えて**、
> `ChangePlan`/`VerificationDesign`にはwork_unitとは別に、Issue全体に1つだけ存在する
> **枠組みフィールド**（`ChangePlan`の`selected_solution`/`OOS`/`interfaces`/`invariants`/
> `failure_behavior`、`VerificationDesign`の`ST-TD`＝AC）がある。これらは対象ごとに分けられない
> （分ければ同じ決定が複数ファイルに散るSSoT違反になる）ため、**対象prefixの無い同一パス**に置く。
>
> ```
> change_plan/CP-<issue番号>.toml              ← 枠組み（selected_solution/OOS/interfaces/invariants/failure_behavior）
> verification_design/VD-<issue番号>.toml      ← 枠組み（ST-TD＝AC）
>
> gitgate/change_plan/CP-<issue番号>.toml       ← shard（gitgateに属するwork_unit・IT-TD/IT-TC等）
> gitgate/verification_design/VD-<issue番号>.toml
> issue_start/change_plan/CP-<issue番号>.toml   ← shard（issue_startに属するwork_unit）
> ```
>
> 対象prefix無しの枠組みファイルと、対象prefix付きのshardファイルは**同じ相対パス構造**
> （`change_plan/CP-<issue番号>.toml`）を持つ。区別はファイルが対象ディレクトリの中にあるか
> どうかだけで判定する——`issue_envelope/`のglob（`**/change_plan/CP-*.toml`）は両方を等しく拾い、
> 「対象ディレクトリ配下か repo 直下か」で枠組み/shardを判別する。
>
> **主対象（primary）を著者が宣言する案は不採用**——本当に対等な複数対象Issueでは「どちらが主か」が
> 恣意的な判断になり、以前訂正した「複数対象にまたがる状態を無理に単一化しようとする」誤り
> （Issue分割の誤り）の変奏になりかねない。枠組みの真のスコープ（Issue全体）をそのまま物理配置に
> 反映するほうが一貫する。
>
> **1b/2bが作るADRの置き場も同じ考え方で埋める**：ADRは「その対象自身の設計判断の履歴」という
> 性質上、枠組みフィールドと違い対象ごとに分けるのが自然（[ADRはコード共有・データは道具ごとに
> 分離する](#fbk-20260923-adrはコード共有・データは道具ごとに分離する) が元々想定した「各ツール自身の
> 決定履歴が積み上がる」性質にそのまま合致する）。`<対象>/adr/<issue番号>-<slug>.toml` とし、
> 新しい機構は追加しない（既存パターンの対象を issue_start 以外の全ツールへ広げるだけ）。

**経緯（周辺文脈）**

v1は`work_units DAG`の分解構造をそのまま物理分置に使ったが、`ChangePlan`/`VerificationDesign`が
`work_units`以外にIssue全体スコープのフィールドも持つことへの言及が無かった。この不完全さが、
[1a1bと2a2bのレビューを分離し無条件化する](#fbk-20260924-1a1bと2a2bのレビューを分離し無条件化する)
以降の一連の点検の中でオーナーから問われ、3つの選択肢（主対象宣言／全shardへの複製／対象prefix
無しの枠組みファイル）を比較検討した。

**オーナー逐語**

> 項目6は選択肢3がよい

**AI の原案と、オーナーが変えた点**

3選択肢の提示・比較（メリデメ）・推奨（選択肢3）はAI。オーナーが推奨どおり選択肢3を承認した。

---

<a id="fbk-20260921-issue513のescalate機械強制は必須とする"></a>

### Issue513のescalate機械強制は必須とする

| | |
|---|---|
| **id** | `FBK-20260921-issue513のescalate機械強制は必須とする` |
| **決定日時** | 2026-09-21 |
| **状態** | **生存** |
| **置換** | **[Issue371と409と513を非推奨とする](#fbk-20260921-issue371と409と513を非推奨とする) を置換** |
| **関連** | [Issue371の作成工程分離は必須とする](#fbk-20260921-issue371の作成工程分離は必須とする) と同じ訂正に由来 |

**決定内容**

> **#513 は必須。** 機構が弱くて無視されたことへの補強である。

**経緯（周辺文脈）**

AI は #513 を「方向A 寄りだが `ask` は判定器を置かずオーナーへ渡す形なので誤検出の害が小さい。
相対的にマシ」と評価していた。これは弱すぎる評価だった（訂正の実施者＝Claude Code (AI)）。

正しくは——**#513 は新しい判定面を作らない。** `karte` が既に出している `escalate: yes` を無視できなく
するだけで、`escalate: no` のときは何も起きない。**誤検出が構造的に発生しない。**列挙型ゲートとは別クラスである。

引き金となった実測は PR #509（Issue #431）で、`karte status` が `escalate: yes`（無進捗 F-431-07・
飽和 Attempt 3,5）を出していたにもかかわらず、主文脈（AI）が「原因が分かっているから実質的な停滞ではない」
と独自解釈し、オーナーへ承認取り付け型の質問として提示した事象である。

**オーナー逐語**

> 513は機構が弱くて無視されたから補強するって話だから必須。

**AI の原案と、オーナーが変えた点**

AI は #513 を「相対的にマシ」という消極的評価に留めた。オーナーは「機構が弱くて無視された」という
**原因の性質**から、補強を必須と位置づけた。

---

<a id="fbk-20260921-issue452を不変条件型の手本にしない"></a>

### Issue452を不変条件型の手本にしない

| | |
|---|---|
| **id** | `FBK-20260921-issue452を不変条件型の手本にしない` |
| **決定日時** | 2026-09-21 |
| **状態** | **生存** |
| **置換** | **[Issue452を不変条件型の手本とする](#fbk-20260921-issue452を不変条件型の手本とする) を置換** |
| **関連** | Codex 利用の継続可否は [Codexを主文脈にせずサブ扱いとする](#fbk-20260922-codexを主文脈にせずサブ扱いとする) で解消済み（旧・未決U-2。2026-09-24 Claude Code (AI) が訂正——本文が解消後も「未決＝U-2」と書いたままだった） |

**決定内容**

> **#452 を手本にしない。** 「出来もしないことをやろうとして4週間トークンを溶かしている」ため。
> Codex 利用の継続そのものについては、その後 [Codexを主文脈にせずサブ扱いとする](#fbk-20260922-codexを主文脈にせずサブ扱いとする) で解消済み（当時は未決＝U-2）。

**経緯（周辺文脈）**

AI は「列挙型の統制を不変条件型へ置き換える」という方向を提案し、その唯一の実装例として
#452（OS sandbox による隔離）を「最も価値が高い可能性がある」と評価した。
オーナーがこれを否定した。

#452 の実測状態（2026-09-17 時点の本文）：方針・設計は完了、PR #506 / #518 / #520 が merge 済み。
ただし P4（implementer / fixer の実起動検証）は診断段階で、F-452-31〜36 が open。起票 2026-08-26 から
4週間、**P4 を一度も通せていない**。加えて #452 は #371 の唯一の残存ネイティブブロッカーである。

**オーナー逐語**

> 452は出来もしないことやろうとして4週間トークン溶かしてるから手本にするな。なんなら見切りつけてCodex使うのやめようかと思ってるくらい。。。

**AI の原案と、オーナーが変えた点**

AI は #452 の**着想**（列挙をやめてプロセス境界で解く）だけを見て推した。オーナーは**実行**
（4週間で P4 未達）も併せて評価すべきだと指摘した。着想と実行は別に評価する——後者を見ずに
手本として推したのは誤り。訂正の実施者＝Claude Code (AI)。

---

<a id="fbk-20260921-必須施策を5項目に定める"></a>

### 必須施策を5項目に定める

| | |
|---|---|
| **id** | `FBK-20260921-必須施策を5項目に定める` |
| **決定日時** | 2026-09-21 |
| **状態** | **生存**（項目は確定。実施順は未決＝U-4） |
| **置換** | （新規） |
| **関連** | [リポジトリの目的はAI駆動開発ノウハウ獲得とする](#fbk-20260921-リポジトリの目的はai駆動開発ノウハウ獲得とする) が前提。①＝[Issue371の作成工程分離は必須とする](#fbk-20260921-issue371の作成工程分離は必須とする)/[Issue371は軽い修正でテーラリング可とする](#fbk-20260921-issue371は軽い修正でテーラリング可とする)、⑤＝[リポジトリの目的はAI駆動開発ノウハウ獲得とする](#fbk-20260921-リポジトリの目的はai駆動開発ノウハウ獲得とする)〜[施策ごとに観測窓を切る](#fbk-20260921-施策ごとに観測窓を切る) |

**決定内容**

| # | 項目 | 主な既存 Issue | A-SPICE 対応 |
|---|---|---|---|
| ① | プロセスの見直し（SYS / SWE 工程の実施） | #371 | SYS.2 / SYS.3 / SWE.1 / SWE.2 |
| ② | ロールの権限とその実現機構の見直し | #530 / #535 / #533 / #129 / #220 | 基盤 |
| ③ | フィードバックループによる改善の仕組み化 | #521 / #523 / #524 / #525 | SUP.9 → プロセス改善 |
| ④ | SSoT / DRY / YAGNI / 単一責務原則を徹底する仕組みの構築 | **該当 Issue なし** | SUP.1 / 構成管理の質 |
| ⑤ | リポジトリの目的の永続化＋計測指標の定義＋計測機構の確立 | #488 / #489 / #461 ＋ 本台帳 | MAN.3 / PIM.3 |

**経緯（周辺文脈）**

AI が A-SPICE でハーネス開発をマッピングし、**左側（SYS.1〜3・SWE.1〜2）が丸ごと無く、
SUP.1（独立した品質保証）・SUP.10（変更要求管理）・双方向トレーサビリティも無い**ことを示した
（詳細は §C-3）。オーナーが必須項目を5つ挙げた。

**オーナー逐語**

> だから、今必須だと思ってるのは、プロセスの見直し(SYS,SWE工程の実施)、ロールの権限とその実現機構の見直し、フィードバックループによる改善の仕組み化、SSoTやDRYやYANGI,単一責務原則などの基本原則を徹底する仕組みの構築、リポジトリの目的の永続化と計測指標の定義と計測機構の確立だ

**AI の原案と、オーナーが変えた点**

オーナーが5項目を提示した。AI は各項目に既存 Issue を対応づけ、**④ だけ該当 Issue が存在しない**ことと、
実施順の推奨（⑤ → ④明文化 → ② → ① → ③）を提示した。実施順は未決（U-4）。

---

<a id="fbk-20260921-議論の永続化先をauditディレクトリとする"></a>

### 議論の永続化先をauditディレクトリとする

| | |
|---|---|
| **id** | `FBK-20260921-議論の永続化先をauditディレクトリとする` |
| **決定日時** | 2026-09-21 |
| **状態** | **生存** |
| **置換** | （新規） |
| **関連** | [決定台帳の書式を生存上段訂正下段とする](#fbk-20260921-決定台帳の書式を生存上段訂正下段とする) が本台帳の書式を定める |

**決定内容**

> 議論の結果をファイルに永続化しながら進める。置き場は `audit/2026-09-21-pipeline-redesign/`。

`audit/README.md` の「置く / 置かない」は「処置計画・実施予定は Issue で管理する」としている。
本台帳が置くのは**決定とその根拠**であり、処置計画・実施予定は従来どおり Issue 側に置く。
点検（定点観測）ではなく議論の決定ログであるため既存の点検記録とは性格が異なるが、
「リポジトリ運営そのものを対象にした記録」という `audit/` の主旨には合致するため同ディレクトリに置く。

**オーナー逐語**

> まず、議論の結果をファイルに永続化しながらやってくれ。決まったことを後で分かるように。

**AI の原案と、オーナーが変えた点**

置き場の選定は AI が判断した（`audit/` 配下・命名規約に従う）。オーナーは置き場を指定していない。

---

<a id="fbk-20260921-決定台帳の書式を生存上段訂正下段とする"></a>

### 決定台帳の書式を生存上段訂正下段とする

| | |
|---|---|
| **id** | `FBK-20260921-決定台帳の書式を生存上段訂正下段とする` |
| **決定日時** | 2026-09-21 |
| **状態** | **生存**（Skill の書き込み先は [決定台帳の正本をfeedback-ledgerとする](#fbk-20260921-決定台帳の正本をfeedback-ledgerとする) で解消。id 規則は [決定台帳のidはFBK日付スラグとする](#fbk-20260922-決定台帳のidはfbk日付スラグとする) が追加した） |
| **置換** | （新規） |
| **関連** | [議論の永続化先をauditディレクトリとする](#fbk-20260921-議論の永続化先をauditディレクトリとする) の置き場に対する書式定義 |

**決定内容**

- **生きている決定事項を上段**（§A）に置く。
- **訂正された決定事項は下段**（§B）に移し、**相互ポインタ**を貼る。
- 各決定事項に **トピック / 日時 / 経緯（周辺文脈）/ オーナー逐語 / 置換先（元）のポインタ** を記載する。
- **上記を Skill で永続化する。**

**経緯（周辺文脈）**

初版の台帳は節ごとに「確定 / 未決 / 訂正」を混在させ、訂正を本文中の注記として埋め込んでいた。
生きている決定と無効な決定が同じ高さに並び、**どれが現行の正かが一読で判別できなかった**。

**オーナー逐語**

> 帳票のデザインを見直してほしい。
> ・生きている決定事項を上段におく
> ・訂正された決定事項は下段に移し、相互ポインタを貼る。
> ・決定事項にはトピック、日時、経緯(周辺文脈)、オーナー逐語、置換先(元)のポインタ(トピックなど)を記載する
> ・上記をSkillで永続化する

**AI の原案と、オーナーが変えた点**

書式はオーナーが指定した。AI は Skill の書き込み先について、既存の `feedback_ledger`（#522・merge 済み）
との重複を申告した（U-9）。

---

<a id="fbk-20260921-決定台帳の正本をfeedback-ledgerとする"></a>

### 決定台帳の正本をfeedback-ledgerとする

| | |
|---|---|
| **id** | `FBK-20260921-決定台帳の正本をfeedback-ledgerとする` |
| **決定日時** | 2026-09-21 |
| **状態** | **生存** |
| **置換** | **[決定台帳のsource.issueをoptional化する](#fbk-20260921-決定台帳のsource.issueをoptional化する) を置換**（未決 U-9 を解消） |
| **関連** | [決定台帳の書式を生存上段訂正下段とする](#fbk-20260921-決定台帳の書式を生存上段訂正下段とする)（書式）の実装方式。[必須施策を5項目に定める](#fbk-20260921-必須施策を5項目に定める) の④（SSoT / DRY / YAGNI）の適用 |
| **実行** | **#537**（2026-09-21 起票・`area:harness` / `type:feature` / P1 / Now） |

**決定内容**

> 決定台帳の正本は既存の **`feedback_ledger`**（`.ai/feedback/`）とし、**2つ目の台帳を作らない**。
> Skill はその上に薄く載せる。`audit/` の markdown は**台帳からの導出ビュー**にし、手書きをやめる。
>
> 埋めるギャップは4点：
> 1. **`source` を repeated table（`[[source]]`）化し、`min_items=1` で1件以上を強制する。**
>    **null を許容せず、複数を許容する。**（[決定台帳のsource.issueをoptional化する](#fbk-20260921-決定台帳のsource.issueをoptional化する) からの変更点）
> 2. `DECISION_POINTS` に方針決定系の語彙を追加する（現在は issue-pipeline 内の決定点のみ）
> 3. `DIVERGENCES` に「AI 案をそのまま承認した」を表す語彙を追加する（現在は「推奨を曲げた」前提）
> 4. 生存（§A）／訂正（§B）に分けて出力する `render` verb を追加する

**経緯（周辺文脈）**

AI が Skill の設計に入る前に既存資産を点検したところ（`.claude/rules/05-skills-agents.md` A14＝
新規資産の前に重複点検）、`feedback_ledger`（#522・merge 済み・2,364行）が [決定台帳の書式を生存上段訂正下段とする](#fbk-20260921-決定台帳の書式を生存上段訂正下段とする) の要求項目を
ほぼ実装済みであることが判明した。

| [決定台帳の書式を生存上段訂正下段とする](#fbk-20260921-決定台帳の書式を生存上段訂正下段とする) の要求 | `feedback_ledger` の既存フィールド |
|---|---|
| トピック | `topic` |
| 日時 | `occurred_at` |
| 経緯（周辺文脈） | `narrative.background` / `recommendation` / `recommendation_reason` / `uncertainty` |
| オーナー逐語 | `narrative.owner_verbatim`（**L4 が推測語彙の混入を機械拒否**） |
| 置換ポインタ | `supersedes`（**L3 が参照先の実在を検査**） |
| 生存／訂正の区別 | `supersedes` チェーンから**導出**（README「台帳側に `status` を保存しない（PR5）」） |

加えて、markdown では AI の規律頼みだった規則が既に機械 lint になっている——
**L4**（逐語への推測語彙の混入拒否）／**L5**（`inferred_reason` は `推測：` マーカー必須）／
**L6**（merge base に在るエントリのバイト変更・削除を拒否＝過去の決定を書き換えられない）／
**L7**（canonical シリアライズのバイト一致）。

決定的な観測：

```
$ ls -R .ai/feedback
ledger/  queue/  triage/     ← すべて .gitkeep のみ。エントリ 0 件
```

**#522 で 2,364行＋schema 3本＋テスト792行を実装したのに、台帳は1件も入っていない。**
これは2つの意味を持つ——(a) L6（immutability）が保護すべき既存エントリが無いため、**今ならスキーマを
壊さずに直せる**。(b) **#521 の完了条件「台帳エントリが1件以上、CLI 経由で実際に記録されている
（運用が回ることの実測）」が未達**であり、本議論の決定を投入すればそれが最初の実データになる。

そして**実装済みで1件も使われていない 2,364行は、[必須施策を5項目に定める](#fbk-20260921-必須施策を5項目に定める) の④（YAGNI）の実例そのもの**である。
ここに2つ目の決定台帳を建てれば、④に自ら違反することになる。

**`source` を複数許容にする理由（[決定台帳のsource.issueをoptional化する](#fbk-20260921-決定台帳のsource.issueをoptional化する) からの変更）**

`source.issue` を optional 化する案は、**「紐づけをサボったエントリが通る」ことを許す**変更だった。
複数許容なら `required_nonempty=True` を維持したまま、紐づけの強制を落とさずに済む。

実装方式は2案あり、**repeated table 化（案 ii）を採る**。

| | 案 | 評価 |
|---|---|---|
| i | `source.issue` → `source.issues`（INT のリスト） | 軽いが、`issues=[371,445]` のとき **`pr`/`round` がどちらの Issue のものか表現できない** |
| **ii** | **`source` を repeated table 化（`[[source]]`・`min_items=1`）** | **各要素が `(issue, pr, round, finding_ids)` の1組。対応関係が保たれる** |

ii を採る理由は3点——(a) 原則（null 許容より複数許容）を `issue` 単独ではなく**出所のまとまり全体**へ
一貫して適用できる、(b) #523 が同じ台帳へパイプライン内の捕捉（issue + pr + round + finding_ids が揃う）を
入れてくるため、設計決定（issue のみ）と同居する以上ここで正規化しておく必要がある、
(c) `min_items=1` を実装すれば「1件以上の出所が必須」が機械強制になる。

**(c) には副次効果がある**——`min_items` は `feedback_ledger/schema.py:85` で宣言されているが
**検査側から一度も参照されていないデッドフィールド**である（`grep -n "min_items" feedback_ledger/*.py` の
ヒットは宣言1行のみ）。本件で実装すれば、[必須施策を5項目に定める](#fbk-20260921-必須施策を5項目に定める) の④（YAGNI）の実例を1つその場で解消できる。

**オーナー逐語**

> ・上記をSkillで永続化する

（U-9 の選択肢 A / B / C に対して）

> A: feedback_ledger を拡張（推奨）

（起票案の `source.issue` optional 化に対して）

> feedback_ledger/schema.py：source.issue を optional 化
> については、どっちかって言うと複数を設定可にすべきでは？
> Nullを許容するよりは複数を許容する方が適切だと思う。
> 他はいいと思う

**AI の原案と、オーナーが変えた点**

AI が3案（A＝既存拡張／B＝新規台帳／C＝書式手順書のみ）を提示し、A を推奨。オーナーが A を選択した。
その後の起票案でギャップ1を「`source.issue` を optional 化」としたところ、**オーナーが null 許容ではなく
複数許容へ訂正**した。実装方式（案 i / ii）は AI が比較し ii を推奨している。

---

<a id="fbk-20260922-決定台帳のidはfbk日付スラグとする"></a>

### 決定台帳のidはFBK日付スラグとする

| | |
|---|---|
| **id** | `FBK-20260922-決定台帳のidはfbk日付スラグとする` |
| **決定日時** | 2026-09-22 |
| **状態** | **生存** |
| **置換** | （新規。[決定台帳の書式を生存上段訂正下段とする](#fbk-20260921-決定台帳の書式を生存上段訂正下段とする) の通番規則だけを差し替える） |
| **関連** | [決定台帳の正本をfeedback-ledgerとする](#fbk-20260921-決定台帳の正本をfeedback-ledgerとする) の `LEDGER_ID_RE` と同形 |

**決定内容**

> **id は `FBK-<YYYYMMDD>-<slug>`。`slug` は `トピック` を `doc-system-v2/slugify.py`（唯一実装）で
> 正規化したもの。**
>
> - **id は不変。訂正・置換されても変えない。** §A→§B の移動は `置換` ポインタからの導出であって、
>   id の意味ではない。
> - **トピックは30字以内で「何を決めたか」を述語まで書く。** 論点名（「Q の下限」）ではなく
>   決定文（「Qの下限は流出欠陥率1%とする」）にする。
> - 既存17件（`D-001`〜`D-014` / `D-101`〜`D-104`）はこの規則へ振り直す。

**経緯（周辺文脈）**

初版は `D-0xx`（生存）／`D-1xx`（訂正済み）の通番を使っていた。**状態を id に埋めたため、決定が
訂正された瞬間に id が変わった**——`Q の下限` は §A の `D-003` から §B の `D-101` へ振り直され、
過去のチャット・Issue・PR から `D-003` を指していた参照はサイレントに別物を指すようになった。
`D-013` は欠番で、3桁ゼロ埋めなのに実体は17件だった。

`feedback_ledger` は最初から `FBK-YYYYMMDD-<slug>` 形式を持っており（`feedback_ledger/schema.py:44`
`LEDGER_ID_RE`）、生存／置換を台帳に保存せず `supersedes` チェーンから毎回導出する設計だった
（`feedback_ledger/README.md`「台帳側に `status` を保存しない（PR5）」）。**つまり「非活性化で id を
変えない」は既存設計で構造的に保証されており、新体系を作る必要はなかった。**

```
$ grep -n "SLUG\|LEDGER_ID_RE" feedback_ledger/schema.py
43:SLUG = r"[a-z0-9]+(?:-[a-z0-9]+)*"
44:LEDGER_ID_RE = re.compile(rf"^FBK-(?P<date>\d{8})-(?P<slug>{SLUG})$")
```

**オーナー逐語**

> あと、id体系がクソだから見直せ。
> 決定事項が実質2桁なうえに非活性化するときにid変わるとか使い方なんにも想像してないだろ？
> ①日付とトピックからidは作れ
> ②非活性化時にidを変えるな
> ③トピックは30文字以下で何を決めたか分かるようにかけ。例えばd-001は『リポジトリの目的は〇〇とする』と言うようにかけ。
> ④特殊文字の扱いはdoc_systemのノードのパス作るときの正規化？を参考にしろ。

**AI の原案と、オーナーが変えた点**

AI の原案が `D-0xx` / `D-1xx` の通番だった。**状態を id に埋めるという設計誤りをオーナーが指摘した。**
④の指示により、`doc-system-v2/slugify.py` が「唯一実装・独自再実装を禁じる」と明記していること、
`dsv2/cli.py:45-56` が動的 import で再利用する前例があることを確認し、同じ手を採る方針とした。

---

<a id="fbk-20260922-slugの文字集合はslugify準拠とする"></a>

### slugの文字集合はslugify準拠とする

| | |
|---|---|
| **id** | `FBK-20260922-slugの文字集合はslugify準拠とする` |
| **決定日時** | 2026-09-22 |
| **状態** | **生存** |
| **置換** | （新規） |
| **関連** | [決定台帳のidはFBK日付スラグとする](#fbk-20260922-決定台帳のidはfbk日付スラグとする) の実装詳細 |

**決定内容**

> **`slugify.py` の出力をそのまま id にする。`feedback_ledger` 専用の文字規則を足さない。**
>
> - **日本語を許す。** 現行の `SLUG = r"[a-z0-9]+(?:-[a-z0-9]+)*"` を拡張する。
> - **`%` と `.` を slug に残す。** `slugify.py` はこの2文字を敵性文字としていないので、そのまま通す。

**経緯（周辺文脈）**

`SLUG` が ASCII 限定のため、日本語トピックをそのまま id にできなかった。一方 `slugify.py` は
「ASCII は小文字化（日本語はそのまま）」と定めており、doc-system-v2 のノード群は既に日本語
ファイル名で運用実績を持つ（`doc-system-v2/nodes/04-verification/dd/decided/` 配下）。

`%` については、URL のパーセントエンコード導入子であるため markdown アンカーで誤読される理屈上の
リスクを AI が申告した（`Qの下限は流出欠陥率1%とする`）。**`feedback_ledger` 側に専用の敵性文字を
足すことは、正規化規則の分岐実装を作ることであり、`slugify.py` の「独自再実装を禁じる」に反する。**
実害が出たら `slugify.py` 側を直して両方に効かせる。

なお markdown リンクの fragment 内では `%` を `%25` としてエスケープする（アンカー本体は `%` のまま）。
これは URL の書き方の問題であって、slug の文字集合の話ではない。

**オーナー逐語**

> aは日本語を許す。
> bは% と . を slug に残す

**AI の原案と、オーナーが変えた点**

AI が (a) 日本語許容・(b) `%` と `.` の温存のいずれも推奨し、オーナーが両方を承認した。原案からの変更なし。

---

<a id="fbk-20260922-トピックの述語は機械強制しない"></a>

### トピックの述語は機械強制しない

| | |
|---|---|
| **id** | `FBK-20260922-トピックの述語は機械強制しない` |
| **決定日時** | 2026-09-22 |
| **状態** | **生存** |
| **置換** | （新規） |
| **関連** | [決定台帳のidはFBK日付スラグとする](#fbk-20260922-決定台帳のidはfbk日付スラグとする) の③。強制先は §E |

**決定内容**

> **機械強制するのは「30字以内」と「`id == FBK-<date>-slugify(topic)` の一致」だけ。**
> 「述語まで書いて何を決めたか分かるようにする」は**今は強制しない**。
>
> **将来 `jev` で強制する。** 対象項目は §E に列挙して持ち越す。

**経緯（周辺文脈）**

「述語つきで何を決めたか分かるように」は意味の要求であり、機械化すると語尾語彙リストの照合＝
**列挙型のゲート**になる。本点検の実測（§C-2）では、**記録済み再発欠陥21件のうち14件（67%）が
ゲート・隔離・権限といった列挙型の機構を発生源としていた**のに対し、不変条件型の検査
（存在一致・ハッシュ一致・マーカー有無）は再発ゼロだった。保守が要る列挙型ゲートをもう1つ
増やすことになるため、AI は長さと id 一致という不変条件だけを機械化するよう推奨した。

**オーナー逐語**

> cは今は強制しない。
> そのうちjevで強制したいから、jevでやることリスト作って載せといて。

**AI の原案と、オーナーが変えた点**

AI の原案は「規約＋レビューに委ねる」で止まっていた。**オーナーが `jev` による将来の強制という
到達点を示し、持ち越し項目をリスト化して台帳に残すよう指示した**——据え置きを「対応不要」に
するのではなく、強制先が決まるまでの保留として明示的に管理する形に変わった。

---

<a id="fbk-20260922-feedback-ledgerのschemaはv1を保つ"></a>

### feedback-ledgerのschemaはv1を保つ

| | |
|---|---|
| **id** | `FBK-20260922-feedback-ledgerのschemaはv1を保つ` |
| **決定日時** | 2026-09-22 |
| **状態** | **生存** |
| **置換** | （新規） |
| **関連** | [決定台帳の正本をfeedback-ledgerとする](#fbk-20260921-決定台帳の正本をfeedback-ledgerとする) のスキーマ拡張 |

**決定内容**

> **`schema = "feedback-ledger/v1"` と `.ai/schema/feedback-ledger-v1.json` は v1 のまま据え置く。**
> `source` の repeated 化（`[source]` → `[[source]]`）は後方非互換だが、**壊す既存データが
> 存在しないため版を上げない。**
>
> **この判断が成立するのは `.ai/feedback/ledger/` のエントリが 0 件の間だけ。**
> エントリが入った後の非互換変更は v2 を要する。

**経緯（周辺文脈）**

```
$ find .ai/feedback -type f
.ai/feedback/README.md
.ai/feedback/ledger/.gitkeep
.ai/feedback/queue/.gitkeep
.ai/feedback/triage/.gitkeep
```

`feedback_ledger` は 2,364 行が実装済みでエントリ 0 件。L6（merge base に在るエントリの
バイト変更・削除を拒否）が保護すべき対象が存在しない。

**オーナー逐語**

> feedback-ledger-v1.jsonってバージョンアップするの？まだもの作ってないならv1のままでいいと思う

**AI の原案と、オーナーが変えた点**

AI は起票案（#537）で `.ai/schema/feedback-ledger-v1.json` の「追従」とだけ書き、**版を上げるのか
据え置くのかを明示していなかった**。オーナーが「まだものを作っていない」＝永続化済みインスタンスが
無いことを根拠に据え置きを指示した。AI はこれに同意し、成立条件（0 件の間だけ）を明記した。

---

<a id="fbk-20260922-codexを主文脈にせずサブ扱いとする"></a>

### Codexを主文脈にせずサブ扱いとする

| | |
|---|---|
| **id** | `FBK-20260922-codexを主文脈にせずサブ扱いとする` |
| **決定日時** | 2026-09-22 |
| **状態** | **生存** |
| **置換** | （新規。未決 U-2 を解消） |
| **関連** | [Issue452を不変条件型の手本にしない](#fbk-20260921-issue452を不変条件型の手本にしない) の帰結 |
| **実行** | **#538**（2026-09-22 起票） |

**決定内容**

> **Claude Code を主文脈とし、Codex はサブエージェント的な実行者として使う（三択の②）。**
>
> - **撤去する**：`issue_start` の Codex supervisor スタック（**6,489行**）とそのテスト（**4,993行**）、
>   `.codex/hooks/codex-launch-intent-gate.sh`、`managed-entrypoints-v2.json` の
>   `codex_supervisor_launch` ブロック。
> - **残す**：`codex-review`（別モデルファミリによる第二意見）と、`codex exec` による作業委譲。
> - **隔離の実現方式**：Codex 用フックを自弁せず、**`codex exec -C <worktree>` の薄いラッパー**を作り、
>   起動前にロール別の permission profile へ差し替える（オーナー案・2026-09-22）。
> - **要設計として持ち越す**：サブエージェント向けロール（`issue-implementer` 等）のフックをどうするか。

**経緯（周辺文脈）**

#452 は「Codex の `issue-implementer`/`issue-fixer` を対象 Issue 専用 worktree へ機械束縛する」ため、
`codex exec` を別 process として起動する repo supervisor を自弁した。退役理由として本文が挙げたのは
「custom-agent schema に子ごとの作業ディレクトリ指定がなく、親の sandbox と live runtime overrides を
継承する」こと。**この記述は 2026-09-22 時点の公式ドキュメントでも有効である**
（`Subagents inherit your current sandbox policy` / `Codex also reapplies the parent turn's live
runtime overrides when it spawns a child`）。

しかし**この制約は「Codex が主文脈である」ときにだけ効く**。主文脈を Claude Code にすれば、
implementer/fixer の隔離は Claude Code 側の `isolation: "worktree"` が与えるか、
`codex exec -C <worktree>` の1引数で足りる。**6,489行は前提ごと不要になる。**

撤去の切り口も実測できれいだった——Codex 用の設定は `managed-entrypoints-v2.json` の
**独立したトップレベルキー `codex_supervisor_launch`** に隔離されており、dispatch 判定に使う
`managed` / `isolation_only` の `binding_transports` には `claude` しか入っていない。
`gate.py` 本体に Codex が現れるのは常時 deny のエラーメッセージ1箇所だけで
（`ISSUE_START_TRANSPORT_UNAVAILABLE`）、**Claude Code の経路は supervisor を一切 import していない。**

```
$ grep -rn "codex_supervisor_launch" --include=*.py .
issue_start/codex_launch_intent.py:416, 995     ← 撤去対象
issue_start/codex_launch_control.py:281         ← 撤去対象
tests/unit/test_issue_start_gate.py:814, 917    ← テスト
tests/unit/test_issue_start_cli_assets.py:162   ← テスト
```

**オーナー逐語**

> とりあえず別セッションで調べた範囲では、Codexのネイティブ機能で隔離ワークツリーは実現できるみたいで、452の自弁はサブエージェント扱いする場合は不要になる認識。
> フック周りがCodexのフックが動くようになるから注意が必要。
> 後めんどくさいのが452が中途半端にmainに入ってるから、除去すべきかどうか調べないといけない。

> u-2は②で行こう。
> Issueも新規起票してよい。
> ②でいくにしても、サブエージェント向け(Issue-imprimenterなど)のフックなどはどうするか設計がいることは注記しといて。
> Codex用フックを自弁するよりはcodex exec -C <worktree>の薄いラッパーを作って、Codex/setting.jsonのpermissions denyとかをロール別で差し替えてからCodexを起動する方が簡便かもね。

**AI の原案と、オーナーが変えた点**

AI は当初 U-2 を「やめる／続ける」の二択で提示していた。**オーナーが「Claude Code をメインにして
Codex をサブエージェントチックに使う」という第3の選択肢を提示し、これが採用された。**
実現方式についても、AI は撤去範囲の実測までしか出しておらず、
**起動方式（`codex exec -C` の薄いラッパー＋ロール別 permission profile の差し替え）は
オーナーの案である。**

AI が確認した点として、オーナーの案は Codex の文書化された機構でそのまま実現できる——
`[permissions.<name>]` の名前付きプロファイル、`extends = ":workspace"`（workspace root 配下の
`.codex/` と `.git/` を read-only に保つ）、`[permissions.<name>.workspace_roots]`、
`default_permissions` による選択。**`permissions` 系キーは project-local `.codex/config.toml` で
無視されるキーの一覧に含まれていない**ため、プロファイル定義自体はリポジトリに版管理できる。

**残る設計事項**（#538 で扱う）

1. **ロール別プロファイルの選択経路**。`--profile` が選ぶのは *config* profile（`$CODEX_HOME/<name>.config.toml`）
   であり、*permission* profile（`[permissions.<name>]`＋`default_permissions`）とは別概念。
   しかも `profile` / `profiles` キーは project-local config では**無視される**。
   ラッパーがどう選択を伝えるか（task-private `CODEX_HOME` / `--config` override / 別経路）は未決。
2. **project trust が前提になる**。project-local の `.codex/config.toml` と `.codex/hooks.json` は
   **プロジェクトを trust するまで読み込まれない**。trust していない状態では user/system 層の
   フックだけが動く。
3. **フックは置換ではなく加算**。`~/.codex/hooks.json` と `<repo>/.codex/hooks.json` の両方に
   一致するフックがあれば**両方とも走る**（"Higher-precedence config layers don't replace
   lower-precedence hooks"）。
4. **サブエージェント向けロールのフック設計**（オーナー明示の持ち越し事項）。Codex には
   `SubagentStart` / `SubagentStop` フック（`matcher` は `agent_type` に適用）が在るが、
   `codex exec` を別 process として呼ぶ経路ではそもそも subagent イベントが発生しない。
   どの層で権限の非対称（implementer は merge 不可・reviewer は push 不可）を担保するかは未決。

---

<a id="fbk-20260922-4基本原則の採否は実測で足切りしない"></a>

### 4基本原則の採否は実測で足切りしない

| | |
|---|---|
| **id** | `FBK-20260922-4基本原則の採否は実測で足切りしない` |
| **決定日時** | 2026-09-22 |
| **状態** | **生存** |
| **置換** | **[④の原則は実測で足切りする](#fbk-20260922-④の原則は実測で足切りする) を置換** |
| **関連** | [④の原則セットはISO25010の8特性で充足確認する](#fbk-20260922-④の原則セットはiso25010の8特性で充足確認する) の前提 |

**決定内容**

> **U-5（④基本原則の徹底機構）が対象とする原則の**採否**は、確立されたソフトウェア工学の知見を出発点とし、
> このリポジトリの実測（census 274件等）で足切りしない。** 実測は「どれを先に機構化するか」という
> **実装の優先順位付け**にだけ使う。

**経緯（周辺文脈）**

U-5 の検討で、AI は census 274件のキーワード再集計をもとに「SSoT/DRY/YAGNI/SRP＋不変条件型検証＋
最小権限＋fail-safe defaults＋テスト容易性」という11項目に絞り、SOLID の O/L/I/D・KISS・型安全性・
冪等性・可観測性などを「根拠が薄い」として採用しなかった。この判定基準は「このリポジトリでまだ
痛い目を見ていない原則は不要」という論法であり、**事故が起きていないことをリスクがないことの
根拠にする誤り**だった。

**オーナー逐語**

> まず、リポジトリの実測を導入是非の根拠にするのは論外だ。ソフトウェアエンジニアリングの目線で見ると、素人がたまたま見つけたことがある罠だけを根拠に教科書は要らないと言ってるに等しい。糞すぎる。

**AI の原案と、オーナーが変えた点**

AI の原案は「census の再集計で件数が付いた原則だけを採用する」だった。**オーナーがこの選別方法自体を
方法論の誤りとして訂正した。** 以後、原則の採否は確立された知見（ISO 25010 等）を基準にし、
このリポジトリの実測は優先順位付けにのみ用いる方針へ変更した。

---

<a id="fbk-20260922-原則は流入対策と流出対策の両輪で設計する"></a>

### 原則は流入対策と流出対策の両輪で設計する

| | |
|---|---|
| **id** | `FBK-20260922-原則は流入対策と流出対策の両輪で設計する` |
| **決定日時** | 2026-09-22 |
| **状態** | **生存** |
| **置換** | **[④は保守性中心の11原則で足りる](#fbk-20260922-④は保守性中心の11原則で足りる) を置換** |
| **関連** | C-3（A-SPICE マッピング）が SWE.1/SWE.2（流入側の工程）欠如を独立に裏付ける |

**決定内容**

> **④の徹底機構は、流入対策（不具合をそもそも書けなくする）と流出対策（書かれた不具合を捕まえる）の
> 両輪で設計する。流出対策（テスト・ゲート）は当然の前提であり、それだけで十分とはしない。**
> 流入対策を優先する。

**経緯（周辺文脈）**

AI がこのセッション全体で行ってきた点検は、ゲート・フック・テスト・lint の点検、すなわち**ほぼ全てが
流出対策**だった。C-2 の目玉所見「不変条件型 > 列挙型」ですら、検出方式2つの比較にすぎず、
「その不具合をそもそも書けなくできたか」は一度も問うていなかった。

**この訂正は、既に確認済みの別の実測と独立に符合する。** C-3 の A-SPICE マッピングは、
SWE.1／SWE.2（要件定義・設計＝流入側のプロセス成熟度）と SUP.1 が**存在しない**一方、
SWE.4〜6／SUP.8〜9（検証・テスト＝流出側）は**存在する**と既に出していた。これはインシデントを
漁った結果ではなく「どの工程が実在するか」の直接観測であり、①（#371＝プロセスの見直し）が
必須と決まった（[Issue371の作成工程分離は必須とする](#fbk-20260921-issue371の作成工程分離は必須とする)）
のも、まさにこの流入側の欠如を埋める処置だった。

**オーナー逐語**

> 次に、不具合の対策や品質担保の考え方が甘すぎる。
> 流入の原因と流出の原因の両方に手立てを打たないのは、テストでバグを捕まえるから大丈夫と言ってるに等しい。
> テストで捕まえるのは当たり前で、そもそも不具合を入れないようにすべき。

**AI の原案と、オーナーが変えた点**

AI の原案は流出対策（検出機構）の点検・改善に終始していた。**オーナーが流入対策の欠如を指摘し、
両輪であることを明示した。**

---

<a id="fbk-20260922-④の原則セットはiso25010の8特性で充足確認する"></a>

### ④の原則セットはISO25010の8特性で充足確認する

| | |
|---|---|
| **id** | `FBK-20260922-④の原則セットはISO25010の8特性で充足確認する` |
| **決定日時** | 2026-09-22 |
| **状態** | **生存**（暫定から確定へ——オーナーが「④というか足りない原則を入れるIssueというか器はいる」「作っていいよ」で本原則セットを追認・2026-09-22） |
| **置換** | **[④は保守性中心の11原則で足りる](#fbk-20260922-④は保守性中心の11原則で足りる) を置換** |
| **関連** | [4基本原則の採否は実測で足切りしない](#fbk-20260922-4基本原則の採否は実測で足切りしない) の適用 |
| **実行** | **#539**（2026-09-22 起票・`area:harness` / `type:tracking` / P2 / Now） |

**決定内容**

> **④の原則セットの網羅性は、AI が自分で作ったリストの主観ではなく、ISO 25010（ソフトウェア品質特性
> モデル・8特性）に照らして確認する。**
>
> | 特性 | 扱い |
> |---|---|
> | 機能適合性（新規・最優先） | AC 充足の正確性・完全性 |
> | 保守性 | SOLID・DRY・YAGNI・KISS・SSoT・テスト容易性 |
> | 信頼性（格上げ） | fail-safe defaults・耐故障性・回復性・冪等性 |
> | セキュリティ（格上げ・広義化） | 最小権限・秘密情報の扱い・信頼境界・サプライチェーン |
> | 互換性（既存機構の明文化） | `asset_parity` が担保する4ツリー整合 |
> | 検証・型 | 型安全性／Parse, don't validate |
> | 可観測性・トレーサビリティ | ③のフィードバックループの前提として維持 |
> | 性能効率性／移植性／ユーザビリティ | **優先度を下げる**（SLA未定義・単一デプロイ環境という構造的理由。実測での足切りではない） |

**経緯（周辺文脈）**

「④の原則は実測で足切りする」の訂正後、AI が SOLID・DRY・YAGNI・SSoT・fail-safe defaults・最小権限・
テスト容易性の11項目を再提示したが、オーナーから「方向性はいいけど、充足するの？」と問われた。
AI が自分のリストを再度自分で検証する（同じ誤りの繰り返し）のを避けるため、ISO 25010 の8特性で
照合したところ、**8特性中まともに被覆していたのは保守性1つだけ**だった。機能適合性・信頼性・
セキュリティが不足しており、特に信頼性とセキュリティは D-001（転用先＝車載ECU・ISO 26262 対象）
という既に確定した目的から見て構造的に必須と判断した。

**オーナー逐語**

> 方向性はいいけど、充足するの？

**AI の原案と、オーナーが変えた点**

AI の原案（11項目）は保守性に偏った部分集合だった。**オーナーの質問を受け、AI が ISO 25010 という
外部の確立された基準に照らして自己点検し、機能適合性・信頼性・セキュリティの不足を発見して
拡張した。** この拡張自体はオーナーの明示確認をまだ経ていない（次回のやり取りで「いいけど」と
一定の同意は得たが、AC妥当性確認という新たな論点の提起に続いたため、本決定は暫定として記録する）。

---

<a id="fbk-20260922-ac妥当性確認はplan-reviewerを強化して担う"></a>

### AC妥当性確認はplan-reviewerを強化して担う

| | |
|---|---|
| **id** | `FBK-20260922-ac妥当性確認はplan-reviewerを強化して担う` |
| **決定日時** | 2026-09-22 |
| **状態** | **生存** |
| **置換** | **[機能適合性はAC充足の正確性で足りる](#fbk-20260922-機能適合性はac充足の正確性で足りる) を置換** |
| **関連** | ④ではなく①（#371）が担う。[Issue371の作成工程分離は必須とする](#fbk-20260921-issue371の作成工程分離は必須とする) の具体化 |
| **実行** | **#371 へコメント投稿済み**（2026-09-22・[issuecomment-5773669483](https://github.com/hiratashinnya/review-system/issues/371#issuecomment-5773669483)） |

**決定内容**

> **「AC 自体が正しいか」（妥当性確認・Validation）は「実装が AC を満たすか」（検証・Verification）とは
> 別の問題であり、④（コード構築の原則）の管轄ではなく①（#371 のプロセス設計）が担う。**
>
> #371 は既に `plan-reviewer` の `PlanReview`（evidence→design→AC→verification のtrace）と、
> 「曖昧AC」を risk gate トリガーとする設計を持つが、2点の穴がある。
> 1. AC 検査が「trace の有無」であって「AC 自体の品質」の構造化チェックリストではない。
> 2. 「曖昧AC」トリガーが AC を書いた `change-planner` 自身の自己申告に依存する（自己認定問題）。
>
> 新規ロールは追加せず、`plan-reviewer`/`PlanReview` を強化する形で対策する。

**経緯（周辺文脈）**

AI が「機能適合性」を④の新規項目として提案した際、それは「実装が AC を満たすか」（検証）だけを
指しており、「AC 自体が正しいか」（妥当性確認）を欠いていた。実例として、**#371 自身の AC に
数値目標が1つも無い**（「計測」節の7項目いずれにも閾値なし）ことを確認した——#371 は
`/gh-create-issue` の構造化テンプレートとオーナー承認を経てなお、この欠陥がすり抜けている。

**オーナー逐語**

> いいけどAC充足以前にAC設計が糞なら意味無いけどどう対策するの？

> aでいいよ

**AI の原案と、オーナーが変えた点**

AI の原案（「機能適合性」＝AC充足の正確性・完全性）は検証（Verification）のみを指しており、
妥当性確認（Validation）が欠けていた。**オーナーの質問により、AC 設計そのものの品質確認が
別問題として必要だと判明した。** 処置先の選択肢（(a) #371 へ即時コメント／(b) 他の未決事項と
合わせて後で改訂）はオーナーが (a) を選択した。

**実施者** Claude Code (AI)

---

<a id="fbk-20260923-adrはコード共有・データは道具ごとに分離する"></a>

### ADRはコード共有・データは道具ごとに分離する

| | |
|---|---|
| **id** | `FBK-20260923-adrはコード共有・データは道具ごとに分離する` |
| **決定日時** | 2026-09-23 |
| **状態** | **生存** |
| **置換** | **[.ai/adr/（データ）＋adr/（コード）を新設する](#fbk-20260922-.ai-adr-データ-＋adr-コード-を新設する) を置換** |
| **関連** | [決定台帳の正本をfeedback-ledgerとする](#fbk-20260921-決定台帳の正本をfeedback-ledgerとする) の SSoT/PR1 と同じ教訓 |
| **実行** | **#540**（2026-09-23 起票・`area:harness` / `type:feature` / P2 / Now） |

**決定内容**

> ADR は**コード（共有ライブラリ）とデータ（道具ごとに分離）**を分ける。`.ai/adr/` という共有データ壺は作らない。
>
> ```
> adr/                    ← 共有ライブラリ（schema.py/check.py/cli.py）。--root 引数で対象ディレクトリを取る
> karte/adr/               ← karte 自身の設計判断
> gitgate/adr/              ← gitgate 自身の設計判断
> feedback_ledger/adr/       ← feedback_ledger 自身の設計判断
> issue_start/adr/           ← #371 の新ロール群の設計判断
> ```

**経緯（周辺文脈）**

AI が最初に提案した `.ai/adr/`（データ）＋`adr/`（コード）は、無関係な発生源（karte/gitgate/feedback_ledger 等）の設計判断を1箇所に混ぜる形になっていた。これは #537 で直したばかりの SSoT/PR1 の教訓（1台帳1目的）を、AI 自身が早速別の形で繰り返していたことになる。AI は「per-project（review_system/doc_system/harness）」という前段の教訓を適用していたが、「harness」が単一のプロジェクトではなく多数の独立した道具の集合であることを見落としていた。

**オーナー逐語**

> .ai/adr/（データ）＋adr/（コード）を新設するの意味がわからん。どういうこと？なぜデータとコードで分ける必要がある？karteはカルテ用、gitgateはgitgate用のADRフォルダを付ければいいでしょ？なぜ違うもののADRを混ぜる？

**AI の原案と、オーナーが変えた点**

AI の原案は harness 全体で1つの ADR データ置き場（`.ai/adr/`）。オーナーが道具単位（karte/gitgate/feedback_ledger/issue_start...）での分離に訂正した。

---

<a id="fbk-20260923-harnessのテストエビはtest-strategyを手本にする"></a>

### harnessのテストエビはtest-strategyを手本にする

| | |
|---|---|
| **id** | `FBK-20260923-harnessのテストエビはtest-strategyを手本にする` |
| **決定日時** | 2026-09-23 |
| **状態** | **生存** |
| **置換** | **[harness側テストエビはdoc_systemの点検実施記録型を写す](#fbk-20260922-harness側テストエビはdoc_systemの点検実施記録型を写す) を置換** |
| **関連** | ADR と同じ「道具ごとに分離」の置き場方針を踏襲 |
| **実行** | **#540**（ADR と同一 Issue で扱う） |

**決定内容**

> harness のテストエビ（検証記録）は、doc-system-v2 の現状（`04-verification/verify/`）ではなく、**review_system の `test-strategy`（TD/TC/TR 3点セット・完成済み）を手本にする**。

**経緯（周辺文脈）**

AI が確認した `doc-system-v2/nodes/04-verification/verify/` は、実施記録が1レコードに混在していた。AI はこれを doc_system の設計意図と誤認したが、実際は**移行途中の暫定状態**だった。TD（設計）/TC（ケース）/TR（結果）は作成タイミングが本質的に異なるため、1レコードに混ぜることはできない。

**オーナー逐語**

> あれ？doc_systemって検証系ノードをそんな雑に作ってたっけ？td/tc/trにするって設計してたはず。今たまたまverifyノードにまとめられてるだけで、直してる途中の認識。作るタイミングが違うから一つに混ぜることは不可。

**AI の原案と、オーナーが変えた点**

AI は `verify/` の現状を設計意図と誤認し、そこから「点検実施記録」型を提案した。オーナーが移行途中であることを指摘し、参照先を review_system の完成済み実装（`test-strategy`）へ差し替えた。

---

<a id="fbk-20260923-マージ権限をaiから剥がしsettings.jsonへ移す"></a>

### マージ権限をAIから剥がしsettings.jsonへ移す

| | |
|---|---|
| **id** | `FBK-20260923-マージ権限をaiから剥がしsettings.jsonへ移す` |
| **決定日時** | 2026-09-23 |
| **状態** | **生存** |
| **置換** | **[pr-reviewerのmerge権限を剥がし主文脈が機構的ゲート付きで実行する](#fbk-20260922-pr-reviewerのmerge権限を剥がし主文脈が機構的ゲート付きで実行する) を置換** |
| **関連** | C-2（列挙型の機構が再発欠陥21件中14件の発生源）と直結 |
| **実行** | **#542**（2026-09-23 起票・「実行先未起票」だった穴を埋める） |

**決定内容**

> merge 権限を**どの AI ロール・主文脈からも剥がし**、`.claude/settings.json` の `permissions.deny`（既存の curl/wget 等と同じネイティブ機構）へ拒否パターンを追加する。実際の merge は**オーナーが手動で行う**。
>
> - `pr_merge_gate/classifier.py`（51.1K・本セッションで複数回誤爆）は不要になる。
> - ブロッカー再検証部分（`gate.py`/`audit.py`）は、オーナーが手動 merge する前に読む**検証済みレポート**へ役割を変える。

**経緯（周辺文脈）**

`pr_merge_gate` の分類器が、read-only の複合コマンドに対し `CLASSIFIER_UNKNOWN`／`RECLASSIFIED_NOT_MERGE` を本セッション中に複数回誤爆した。§C-2 の実測（再発欠陥21件中14件＝67%が列挙型の機構に由来）とも整合する。`.codex/config.toml` には `merge_pull_request`／`enable_auto_merge` をコネクタレベルで既に無効化している先例があった。

**オーナー逐語**

> マージ権限はそもそもAIに渡さないほうがいいのでは？そしたらsetting.jsonで設定できるようになるから自弁してバグ祭りになってる現状から脱却できるのでは？なんかセキュリティ周りは自弁するのは地雷だと思ってる。

**AI の原案と、オーナーが変えた点**

AI は前段で「`pr-reviewer` から merge 権限を剥がし主文脈が実行する」という中間案を出した。オーナーがさらに踏み込み、「AI には誰にも渡さず、ネイティブな許可機構で止める」という、より根本的な解に訂正した。

---

<a id="fbk-20260923-tracking_statusにpendingを正当な状態とする"></a>

### tracking_statusにpendingを正当な状態とする

| | |
|---|---|
| **id** | `FBK-20260923-tracking_statusにpendingを正当な状態とする` |
| **決定日時** | 2026-09-23 |
| **状態** | **生存** |
| **置換** | **[tracking_issueとreasonの二択のみとする](#fbk-20260923-tracking_issueとreasonの二択のみとする) を置換** |
| **関連** | [ADRはコード共有・データは道具ごとに分離する](#fbk-20260923-adrはコード共有・データは道具ごとに分離する) のスキーマ詳細。#540 本文へ反映済み |
| **実行** | **#540**（本文訂正として反映・2026-09-23） |

**決定内容**

> ADR の `[decision]` に `tracking_status`（enum：`filed`／`pending`／`not_needed`）を必須フィールドとして
> 追加する。**`pending`（Issueが要ると決めたが、まだ起票していない）を正当な保存可能状態とする。**
>
> - `filed` → `tracking_issue`（番号）が必須
> - `pending` → 追加フィールド不要。この状態自体が正当
> - `not_needed` → `no_tracking_reason`（理由）が必須
>
> あわせて、`pending` の書込直後に `systemMessage` で通知する PostToolUse hook（`karte-notify.sh` と同型）
> を Scope に含める。

**経緯（周辺文脈）**

「決めたが実行を忘れる」対策（#540）の当初案は `tracking_issue`（番号）／`no_tracking_reason`（理由）の
**二択**で、両方空なら書き込みを拒否する設計だった。この二択には「Issueは要ると決めたが番号はまだ無い」
という状態を保存する場所が無く、**まさにこの穴へ本セッション中に実際に2件落ちた**——
[リポジトリの目的はAI駆動開発ノウハウ獲得とする](#fbk-20260921-リポジトリの目的はai駆動開発ノウハウ獲得とする)
と[マージ権限をAIから剥がしsettings.jsonへ移す](#fbk-20260923-マージ権限をaiから剥がしsettings.jsonへ移す)
が、決定から Issue 化（#541／#542）まで数ターンの空白を作った。

オーナーの最初の質問（「Issue切る予定(まだ切ってない)はどう表現されて、どう刈り取るのか」）に対し、
AI は「対策はまだ実装されていない」という実装状況の話として答えてしまい、**質問の核心（設計そのものに
この状態の表現が無い）を1往復取り違えた。**

**オーナー逐語**

> 話が通じねぇなぁ。
> 対策はこうしますって決めたんだろ？
> で、Issue切る予定(まだ切ってない)はどう表現されて、どう刈り取るのか質問してんだけど伝わってる？

**AI の原案と、オーナーが変えた点**

AI の原案（`tracking_issue`／`no_tracking_reason` の二択）には「決めたが未着手」の状態を保存する
スロットが無かった。オーナーの再質問を受け、AI が `tracking_status` の3値 enum へ設計を訂正し、
併せて Out of scope としていた PostToolUse 通知を Scope へ格上げした。

---

<a id="fbk-20260924-器はgithub-issue継続で確定する"></a>

### 器はGitHub Issue継続で確定する

| | |
|---|---|
| **id** | `FBK-20260924-器はgithub-issue継続で確定する` |
| **決定日時** | 2026-09-24 |
| **状態** | **生存** |
| **置換** | なし（U-6 は本セッションで一度も §A に起票されず、§D に置かれたまま棚上げされていた論点の初回確定） |
| **関連** | 本セッション全体の設計（V字プロセス7ロール構成＝[検証エビは全レベルでTD-TC-TRを揃える](#fbk-20260924-検証エビは全レベルでtd-tc-trを揃える)、push権限＝[成果物ロールは各自pushし直接コミットする](#fbk-20260923-成果物ロールは各自pushし直接コミットする)等）は、器＝GitHub Issue継続を暗黙の前提にしていた。本エントリはその前提を明示的に確定する |

**決定内容**

> 器（Issue運用の実行基盤）は**現行どおり GitHub Issue を継続する**。新しい追跡システムへの置き換えは行わない。
> 役割分離（issue-investigator/ac-designer/change-planner/plan-reviewer/test-writer/issue-implementer/pr-reviewer
> の7ロール）は GitHub Issue 上でのサブエージェント dispatch として実現する（既存の issue-pipeline 運用の延長）。

**経緯（周辺文脈）**

U-6「器の最終判断（GitHub Issue 継続＋役割分離でよいか）」は、監査初期（2026-09-21）に §D へ起票されたまま、
本セッション全体を通じて一度も §A へ正式決定として昇格していなかった。実質的には全ての後続決定
（V字プロセス設計・push権限・トレーサビリティ機構等）が「器＝GitHub Issue」を暗黙の前提として進んでおり、
形式的な確定だけが欠けていた状態（Opus 5.5 サブエージェントの内部矛盾点検では検出されなかった種類の
「決め忘れ」——矛盾ではなく単純な未起票）。

**オーナー逐語**

> u-6はGitHub Issue継続でよい。

---

<a id="fbk-20260924-起票先はラベルで機械的に問い合わせる"></a>

### 起票先はラベルで機械的に問い合わせる

| | |
|---|---|
| **id** | `FBK-20260924-起票先はラベルで機械的に問い合わせる` |
| **決定日時** | 2026-09-24 |
| **状態** | **生存** |
| **置換** | なし（U-7 は本セッションで一度も §A に起票されず、§D に置かれたまま棚上げされていた論点の初回確定） |
| **関連** | [決定台帳の正本をfeedback-ledgerとする](#fbk-20260921-決定台帳の正本をfeedback-ledgerとする) の「`audit/` は手書きをやめる」を GitHub Issue 一覧にも同じ考え方で適用。[器はGitHub Issue継続で確定する](#fbk-20260924-器はgithub-issue継続で確定する) と同じ「GitHub を発生源にする」原則 |
| **実行** | **本エントリで即時実行**（ラベル作成・既存対象13件への付与・台帳冒頭への導線追記を同一ターンで実施） |

**決定内容**

> U-7「本分析結果の起票方針（#445 本文へ集約するか、個別起票するか）」は、運用上はすでに**個別起票**
> （#537/#538/#539/#540/#541/#542/#543 等）で確定していたが、**別セッションが本監査の関連Issueを
> 一括で見つける入り口が無い**という残課題があった。
>
> 入り口は**共有ラベル `audit:2026-09-pipeline-redesign` による GitHub 側のクエリ**とする
> （`gh issue list --label "audit:2026-09-pipeline-redesign" --state all`）。台帳への手書き一覧
> （メタ追跡Issueへの手書き含む）は不採用——すでに「`audit/` は手書きをやめ台帳からの導出ビューにする」
> と決めた方針と同じ理由で、クエリで代替できる一覧を手書きすると二重管理・ドリフト源になる。
>
> 初回付与対象（13件）：#371・#375・#409・#445・#513・#533・#537・#538・#539・#540・#541・#542・#543。
> 今後この監査に関連するIssueを起票・発見したときは、同じラベルを付与する。

**経緯（周辺文脈）**

U-12（#540本文の反映漏れ）の点検で「決めたが反映を忘れる」実例が見つかったのと同じ構造の問題——
**決定の置き場（台帳）と実行の置き場（GitHub Issue群）が分かれているため、後から追跡する手段が
無いと反映漏れ・見落としが起きる。** U-6と同様、これも「矛盾」ではなく「機構が無い」ことによる
未起票状態だった。

**オーナー逐語**

> u-7は結局何処見ればいいかの入り口がいるのでは？何を入り口にするのが適切か？
> 別セッションに引き継ぎできるように入り口は作らないといけない。
>
> （3選択肢提示後）u-7は推奨案でよい。

**AI の原案と、オーナーが変えた点**

3選択肢（A：GitHub共有ラベル／B：メタ追跡Issue手書き／C：台帳冒頭ナビゲーションのみ）の提示・
比較・推奨（A＋Cの併用）はAI。オーナーが推奨どおり承認した。

---

## §B 訂正・置換された決定事項（**無効**）

> ここに載っているものは**すべて無効**である。参照して行動してはならない。
> 残すのは、なぜその結論に至り、なぜ誤りだったかを後から再構成するためである。

<a id="fbk-20260923-tracking_issueとreasonの二択のみとする"></a>

### tracking_issueとreasonの二択のみとする（**無効**）

| | |
|---|---|
| **id** | `FBK-20260923-tracking_issueとreasonの二択のみとする` |
| **記録日時** | 2026-09-23 |
| **状態** | **置換済み・無効** |
| **置換先** | **[tracking_statusにpendingを正当な状態とする](#fbk-20260923-tracking_statusにpendingを正当な状態とする) へ置換** |
| **由来** | AI（Claude Code）の設計 |

**無効になった内容**

> ADR の `decision` テーブルに `tracking_issue`（Issue番号）または `no_tracking_reason`（追跡不要の理由）の
> いずれかを必須とし、両方空なら書き込みを拒否する。

**なぜ誤りだったか**

**二値の設計に、第三の正当な状態（「Issueは要ると決めたが、まだ起票していない」）を保存する場所が
無かった。** オーナーの最初の質問はまさにこの状態の表現方法を聞いていたが、AI は「対策の実装状況」の
質問として誤読し、1往復のやり取りを無駄にした。この穴自体、本セッション中に実際に2件（D-001・
マージ権限決定）の Issue 化漏れとして顕在化した。

**訂正の契機（オーナー逐語）**

> 話が通じねぇなぁ。
> 対策はこうしますって決めたんだろ？
> で、Issue切る予定(まだ切ってない)はどう表現されて、どう刈り取るのか質問してんだけど伝わってる？

**実施者** Claude Code (AI)

---

<a id="fbk-20260922-.ai-adr-データ-＋adr-コード-を新設する"></a>

### .ai/adr/（データ）＋adr/（コード）を新設する（**無効**）

| | |
|---|---|
| **id** | `FBK-20260922-.ai/adr/（データ）＋adr/（コード）を新設する` |
| **記録日時** | 2026-09-22 |
| **状態** | **置換済み・無効** |
| **置換先** | **[ADRはコード共有・データは道具ごとに分離する](#fbk-20260923-adrはコード共有・データは道具ごとに分離する) へ置換** |
| **由来** | AI（Claude Code）の提案 |

**無効になった内容**

> harness 用 ADR を `adr/`（コード）＋`.ai/adr/`（データ）として新設し、harness 全体の設計判断を1つの
> データ置き場にまとめる。

**なぜ誤りだったか**

**無関係な発生源（karte/gitgate/feedback_ledger 等）の設計判断を1箇所に混ぜた。** #537 で直したばかりの
SSoT/PR1（1台帳1目的）を、AI 自身が「harness は1プロジェクト」という誤った粒度で早速繰り返していた。

**訂正の契機（オーナー逐語）**

> .ai/adr/（データ）＋adr/（コード）を新設するの意味がわからん。どういうこと？なぜデータとコードで分ける必要がある？karteはカルテ用、gitgateはgitgate用のADRフォルダを付ければいいでしょ？なぜ違うもののADRを混ぜる？

**実施者** Claude Code (AI)

---

<a id="fbk-20260922-harness側テストエビはdoc_systemの点検実施記録型を写す"></a>

### harness側テストエビはdoc_systemの点検実施記録型を写す（**無効**）

| | |
|---|---|
| **id** | `FBK-20260922-harness側テストエビはdoc_systemの点検実施記録型を写す` |
| **記録日時** | 2026-09-22 |
| **状態** | **置換済み・無効** |
| **置換先** | **[harnessのテストエビはtest-strategyを手本にする](#fbk-20260923-harnessのテストエビはtest-strategyを手本にする) へ置換** |
| **由来** | AI（Claude Code）の評価 |

**無効になった内容**

> harness のテストエビは、doc-system-v2 の `04-verification/verify/`（検証手法・実施日・対象範囲・結果・
> 発生した指摘の記録型）を手本にする。

**なぜ誤りだったか**

**移行途中の暫定状態を設計意図と誤認した。** doc-system-v2 の検証系ノードは TD/TC/TR に分離する設計の
はずで、`verify/` にまとまっているのは移行未完了のため。TD/TC/TR は作成タイミングが本質的に異なり、
1レコードに混ぜることはできない。

**訂正の契機（オーナー逐語）**

> あれ？doc_systemって検証系ノードをそんな雑に作ってたっけ？td/tc/trにするって設計してたはず。今たまたまverifyノードにまとめられてるだけで、直してる途中の認識。作るタイミングが違うから一つに混ぜることは不可。

**実施者** Claude Code (AI)

---

<a id="fbk-20260922-pr-reviewerのmerge権限を剥がし主文脈が機構的ゲート付きで実行する"></a>

### pr-reviewerのmerge権限を剥がし主文脈が機構的ゲート付きで実行する（**無効**）

| | |
|---|---|
| **id** | `FBK-20260922-pr-reviewerのmerge権限を剥がし主文脈が機構的ゲート付きで実行する` |
| **記録日時** | 2026-09-22 |
| **状態** | **置換済み・無効** |
| **置換先** | **[マージ権限をAIから剥がしsettings.jsonへ移す](#fbk-20260923-マージ権限をaiから剥がしsettings.jsonへ移す) へ置換** |
| **由来** | AI（Claude Code）の提案 |

**無効になった内容**

> `pr-reviewer` の merge 権限を剥がし、主文脈が `pr_merge_gate` の機構的なクリーン状態検証を条件に
> merge を実行する。

**なぜ誤りだったか**

**主文脈という別の AI ロールへ merge 権限を横移動しただけで、「AI が merge 権限を持つ」という構造自体は
残った。** また既存の `pr_merge_gate/classifier.py` を存続させる前提だったが、この分類器自体が
本セッション中に複数回誤爆しており、温存に値しなかった。

**訂正の契機（オーナー逐語）**

> マージ権限はそもそもAIに渡さないほうがいいのでは？そしたらsetting.jsonで設定できるようになるから自弁してバグ祭りになってる現状から脱却できるのでは？なんかセキュリティ周りは自弁するのは地雷だと思ってる。

**実施者** Claude Code (AI)

---

<a id="fbk-20260922-④の原則は実測で足切りする"></a>

### ④の原則は実測で足切りする（**無効**）

| | |
|---|---|
| **id** | `FBK-20260922-④の原則は実測で足切りする` |
| **記録日時** | 2026-09-22 |
| **状態** | **置換済み・無効** |
| **置換先** | **[4基本原則の採否は実測で足切りしない](#fbk-20260922-4基本原則の採否は実測で足切りしない) へ置換** |
| **由来** | AI（Claude Code）の評価 |

**無効になった内容**

> census 274件のキーワード再集計で件数の付いた原則だけを採用する。SOLID の O/L/I/D・KISS・型安全性・
> 冪等性・可観測性は「このリポジトリで根拠が薄い」として不採用とする。

**なぜ誤りだったか**

**「このリポジトリでまだ事故が起きていない」ことを「リスクがない」ことの根拠にした。** 確立された
ソフトウェア工学の原則は、自分のデータで痛い目を見る前に防ぐために存在する。274件という低いNの
自リポジトリのインシデントで教科書の採否を決めるのは倒錯している。

**訂正の契機（オーナー逐語）**

> まず、リポジトリの実測を導入是非の根拠にするのは論外だ。ソフトウェアエンジニアリングの目線で見ると、素人がたまたま見つけたことがある罠だけを根拠に教科書は要らないと言ってるに等しい。糞すぎる。

**実施者** Claude Code (AI)

---

<a id="fbk-20260922-④は保守性中心の11原則で足りる"></a>

### ④は保守性中心の11原則で足りる（**無効**）

| | |
|---|---|
| **id** | `FBK-20260922-④は保守性中心の11原則で足りる` |
| **記録日時** | 2026-09-22 |
| **状態** | **置換済み・無効** |
| **置換先** | **[原則は流入対策と流出対策の両輪で設計する](#fbk-20260922-原則は流入対策と流出対策の両輪で設計する) ／ [④の原則セットはISO25010の8特性で充足確認する](#fbk-20260922-④の原則セットはiso25010の8特性で充足確認する) へ置換** |
| **由来** | AI（Claude Code）の評価 |

**無効になった内容**

> SOLID・DRY・YAGNI・SSoT・不変条件型検証・fail-safe defaults・最小権限・テスト容易性の11項目で
> ④の原則セットとして十分。

**なぜ誤りだったか**

**2つの理由で不十分だった。** (1) ほぼ全てが流出対策（検出）に属し、流入対策（そもそも不具合を
書けなくする）が抜けていた——C-2 の目玉所見「不変条件型 > 列挙型」ですら検出方式2つの比較に
すぎない。(2) ISO 25010 の8品質特性で照合すると、まともに被覆していたのは保守性1つだけで、
機能適合性・信頼性・セキュリティが欠けていた。

**訂正の契機（オーナー逐語）**

> 次に、不具合の対策や品質担保の考え方が甘すぎる。流入の原因と流出の原因の両方に手立てを打たないのは、テストでバグを捕まえるから大丈夫と言ってるに等しい。テストで捕まえるのは当たり前で、そもそも不具合を入れないようにすべき。

> 方向性はいいけど、充足するの？

**実施者** Claude Code (AI)

---

<a id="fbk-20260922-機能適合性はac充足の正確性で足りる"></a>

### 機能適合性はAC充足の正確性で足りる（**無効**）

| | |
|---|---|
| **id** | `FBK-20260922-機能適合性はac充足の正確性で足りる` |
| **記録日時** | 2026-09-22 |
| **状態** | **置換済み・無効** |
| **置換先** | **[AC妥当性確認はplan-reviewerを強化して担う](#fbk-20260922-ac妥当性確認はplan-reviewerを強化して担う) へ置換** |
| **由来** | AI（Claude Code）の評価 |

**無効になった内容**

> ④に「機能適合性」（AC充足の正確性・完全性）を独立した原則として追加する。

**なぜ誤りだったか**

**検証（実装がACを満たすか）と妥当性確認（ACそのものが正しいか）を混同した。** AC自体が誤って
いれば、それを完璧に満たしても無意味——このIssue #371自身の AC に数値目標が1つも無いことが
実例になった。この問題は④（コード構築の原則）ではなく①（#371のプロセス設計）の管轄である。

**訂正の契機（オーナー逐語）**

> いいけどAC充足以前にAC設計が糞なら意味無いけどどう対策するの？

**実施者** Claude Code (AI)

---

<a id="fbk-20260921-qの下限は現在値0.37とする"></a>

### Qの下限は現在値0.37とする（**無効**）

| | |
|---|---|
| **id** | `FBK-20260921-qの下限は現在値0.37とする` |
| **記録日時** | 2026-09-21 |
| **状態** | **置換済み・無効** |
| **置換先** | **[Qの下限は流出欠陥率1%とする](#fbk-20260921-qの下限は流出欠陥率1%25とする) へ置換** |
| **由来** | AI（Claude Code）の推奨。オーナー決定には至らなかった |

**無効になった内容**

> Q の下限は現在値 0.37（`defect_metrics` の派生 Issue / merged PR）とし、これを悪化させない。
> 基線 0.68 ではなく現在値を採るのは、既に改善した水準を手放す施策を許さないため。

**なぜ誤りだったか**

転用先（車載 ECU）の要求水準を見ていない提案だった。**「現在の実力から出発する」という発想自体が、
制約（Q の下限は外部要件で決まる）と目的関数（C/D の最小化）を取り違えている。**
[QCDはQを制約としCとDを最小化する](#fbk-20260921-qcdはqを制約としcとdを最小化する) で「Q は制約である」と定式化した直後に、その制約を内部の実績から決めようとしていた。

**訂正の契機（オーナー逐語）**

> Qの下限は未達だぞ？車載舐めんな。下限1%だ。これでも流出タイミング次第ではまだ高く付くわ。

**実施者** Claude Code (AI)

---

<a id="fbk-20260921-issue371と409と513を非推奨とする"></a>

### Issue371と409と513を非推奨とする（**無効**）

| | |
|---|---|
| **id** | `FBK-20260921-issue371と409と513を非推奨とする` |
| **記録日時** | 2026-09-21 |
| **状態** | **置換済み・無効** |
| **置換先** | **[Issue371の作成工程分離は必須とする](#fbk-20260921-issue371の作成工程分離は必須とする)** ／ **[Issue513のescalate機械強制は必須とする](#fbk-20260921-issue513のescalate機械強制は必須とする)** へ置換 |
| **由来** | AI（Claude Code）の分析 |

**無効になった内容**

> 再発21件のうち67%がゲート機構由来である以上、検出強化（ゲート追加）は Q 下限 1% に原理的に届かない。
> #371（3ロール追加）・#409（close gate）・#513（escalate 強制）はすべて方向A に属するため非推奨。
> コストが2倍になり D（回転数）が半減する代償に見合わない。

**なぜ誤りだったか**

**「ゲートを足すな」と「工程を定義しろ」を同じ箱に入れていた。**

- **#371 は検出器の追加ではなく工程の定義である。** investigation / planning / plan_review は
  V字の左側（要求分析・アーキテクチャ設計）にあたる。点検時点のプロセスには左側が存在せず、
  **左側が無いから右側（検証）をゲートで盛るしかなくなっていた。**
  「再発の67%がゲート由来」という実測は、**左側の欠如が右側の肥大として現れた結果**と読むのが正しく、
  #371 はその根本に当たる。
- **#513 は新しい判定面を作らない。** 既に出ている `escalate: yes` を無視できなくするだけで、
  `escalate: no` なら何も起きない。誤検出が構造的に発生しない。列挙型ゲートとは別クラスである。

**なお #409 については、オーナーの明示判断はまだ無い**（U-10）。本項の無効化は #371 と #513 に限る。

**訂正の契機（オーナー逐語）**

> 検出強化を避けるべきはそのとおりだがら例示の371,513はやるぞ？A-SPICEベースで考えたら今のプロセスなんか遊びと変わらんし、品質低いのも当然だ。

**実施者** Claude Code (AI)

---

<a id="fbk-20260921-issue452を不変条件型の手本とする"></a>

### Issue452を不変条件型の手本とする（**無効**）

| | |
|---|---|
| **id** | `FBK-20260921-issue452を不変条件型の手本とする` |
| **記録日時** | 2026-09-21 |
| **状態** | **置換済み・無効** |
| **置換先** | **[Issue452を不変条件型の手本にしない](#fbk-20260921-issue452を不変条件型の手本にしない) へ置換** |
| **由来** | AI（Claude Code）の評価 |

**無効になった内容**

> #452（Codex の OS 隔離）は方向Bの唯一の実装例。列挙をやめてプロセス境界で解いている。
> 最も価値が高い可能性がある。従来は「#371 を塞ぐ重い epic」として厄介者扱いされていたが、
> 評価を上げるべき。

**なぜ誤りだったか**

**着想と実行を分けて評価しなかった。** 「列挙をやめてプロセス境界で解く」という着想は妥当でも、
起票 2026-08-26 から4週間で P4（implementer / fixer の実起動検証）を一度も通せておらず、
F-452-31〜36 が open のままである。**実行可能性を確認せずに手本として推した。**

**訂正の契機（オーナー逐語）**

> 452は出来もしないことやろうとして4週間トークン溶かしてるから手本にするな。なんなら見切りつけてCodex使うのやめようかと思ってるくらい。。。

**実施者** Claude Code (AI)

---

<a id="fbk-20260921-決定台帳のsource.issueをoptional化する"></a>

### 決定台帳のsource.issueをoptional化する（**無効**）

| | |
|---|---|
| **id** | `FBK-20260921-決定台帳のsource.issueをoptional化する` |
| **記録日時** | 2026-09-21 |
| **状態** | **置換済み・無効** |
| **置換先** | **[決定台帳の正本をfeedback-ledgerとする](#fbk-20260921-決定台帳の正本をfeedback-ledgerとする) へ置換** |
| **由来** | AI（Claude Code）の起票案。オーナーが gap 1 を訂正 |

**無効になった内容**

> 決定台帳の正本は既存の `feedback_ledger` とし、2つ目の台帳を作らない。埋めるギャップは4点：
> 1. **`source.issue` を optional 化する**（複数 Issue 横断の方針決定を入れられるようにする）
> 2. `DECISION_POINTS` に方針決定系の語彙を追加する
> 3. `DIVERGENCES` に「AI 案をそのまま承認した」を表す語彙を追加する
> 4. 生存／訂正に分けて出力する `render` verb を追加する

**なぜ誤りだったか（ギャップ1のみ）**

**`source.issue` を optional 化すると、「紐づけをサボったエントリ」が検査を通る。**
複数 Issue 横断の決定を入れたいという要求に対し、**制約を緩める方向（null 許容）で応えていた**。
正しくは**表現力を上げる方向（複数許容）**で応えるべきで、そうすれば `required_nonempty=True` を
維持したまま紐づけの強制を落とさずに済む。

**ギャップ2〜4 と「新台帳を作らない」という中核の決定は誤っていない**（[決定台帳の正本をfeedback-ledgerとする](#fbk-20260921-決定台帳の正本をfeedback-ledgerとする) へそのまま引き継ぐ）。
エントリ単位で置換する台帳の性質上、一部の訂正でもエントリ全体を置き換える。

**訂正の契機（オーナー逐語）**

> feedback_ledger/schema.py：source.issue を optional 化
> については、どっちかって言うと複数を設定可にすべきでは？
> Nullを許容するよりは複数を許容する方が適切だと思う。
> 他はいいと思う

**実施者** Claude Code (AI)

---

<a id="fbk-20260922-検証設計と本体を同一ロールが書く"></a>

### 検証設計と本体を同一ロールが書く（**無効**）

| | |
|---|---|
| **id** | `FBK-20260922-検証設計と本体を同一ロールが書く` |
| **記録日時** | 2026-09-22 |
| **状態** | **置換済み・無効** |
| **置換先** | **[ロールは分離済みだが成果物名は未根拠](#fbk-20260923-ロールは分離済みだが成果物名は未根拠) へ置換** |
| **由来** | AI（Claude Code）の起票案 |

**無効になった内容**

| # | 工程 | 成果物 | レビュー方法 | 担当ロール |
|---|---|---|---|---|
| 1a | 要件定義+アーキ設計：検証設計を先に作る | AC（受け入れ基準）＋結合検査設計 | 1b とまとめて `plan-reviewer` が検証 | `change-planner`（既存） |
| 1b | 要件定義+アーキ設計：本体 | スコープ/OOS・責務分割・IF・invariants・failure_behavior・ADR | `plan-reviewer` が 1a↔1b の整合＋既存決定との矛盾を検証 | `change-planner`（既存）→`plan-reviewer`（既存） |
| 2a | 実装：テスト設計を先に作る | 1a の AC を具体化したテストケース（Red） | 2b とまとめて `pr-reviewer` が検証 | `issue-implementer`（役割を拡張） |
| 2b | 実装：本体 | 実装コード（Green）＋必要なら軽量 ADR | `pr-reviewer`（既存） | `issue-implementer`（既存） |
| 3a | V字右側：テストエビ | TD/TC/TR（review_system 側は既存 `test-strategy` を流用。harness 側は要検討） | 既存の成績書運用 | 既存資産の流用 |
| 3b | V字右側：バグ修正ループ | `karte` の診断→是正 | `issue-fixer`→`pr-reviewer` 再レビュー（既存） | 既存資産の流用 |

**なぜ誤りだったか**

**1a/1b をともに `change-planner`、2a/2b をともに `issue-implementer` としており、
「検証設計を書く者と本体を書く者は別コンテキストが必須」という決定
（[1a/2aと1b/2bはロール分離を必須とする](#fbk-20260922-1a-2aと1b-2bはロール分離を必須とする)）
と自己矛盾していた。** 加えて成果物名（結合検査設計／テストケース／TD/TC/TR）の対応関係が未整理で、
1b の成果物名（スコープ/OOS・責務分割・IF・invariants・failure_behavior）にも根拠を示していなかった
——ただしこの2点は次の
[ロールは分離済みだが成果物名は未根拠](#fbk-20260923-ロールは分離済みだが成果物名は未根拠)
でも未解決のまま残った。

**訂正の契機（オーナー逐語）**

> 「工程別成果物とレビュー一覧」は未確定のまま状況とか本文を更新するほうがええのでは？もっと詰めるべきと思ってるけど。

**実施者** Claude Code (AI)

---

<a id="fbk-20260923-ロールは分離済みだが成果物名は未根拠"></a>

### ロールは分離済みだが成果物名は未根拠（**無効**）

| | |
|---|---|
| **id** | `FBK-20260923-ロールは分離済みだが成果物名は未根拠` |
| **記録日時** | 2026-09-23 |
| **状態** | **置換済み・無効** |
| **置換先** | **[検証エビはTD-TC-TRの単一鎖とする](#fbk-20260923-検証エビはtd-tc-trの単一鎖とする) へ置換** |
| **由来** | AI（Claude Code）の訂正 |

**無効になった内容**

1a 担当ロールを `ac-designer`（新設）、2a 担当ロールを `test-writer`（新設）へ訂正し、
[権限設計は工程表の各ロールへ役割必須性で接続する](#fbk-20260922-権限設計は工程表の各ロールへ役割必須性で接続する)
の適用表と整合させた。他の列（成果物・レビュー方法）は前バージョンのまま——結合検査設計／テストケース
（Red）／TD/TC/TR という3つの別名、スコープ/OOS・責務分割・IF・invariants・failure_behavior という
根拠不明の成果物名列挙は残っていた。

**なぜ誤りだったか**

**役割の自己矛盾だけを直し、成果物名の問題には手を付けていなかった。** オーナーが「工程別成果物って
名前が独り歩きしてるものばかりでは？」と再指摘し、これらが #371 の既存 `ChangePlan` スキーマや
フラクタル構造（検証設計→本体の順）への根拠づけを欠いた単語の羅列だったことが判明した——結合検査設計
（1a）／テストケース（2a）／TD/TC/TR（3a）は同じ検証エビデンス系列を3箇所で別名を使って書いていただけ、
スコープ/OOS・責務分割・IF・invariants・failure_behavior は #371 の `ChangePlan` 実フィールドを
無根拠に言い換えたものだった。

**訂正の契機（オーナー逐語）**

> 工程一覧が先かな。
> 工程別成果物って名前が独り歩きしてるものばかりでは？もっと詰めないでいいの？

**実施者** Claude Code (AI)

---

<a id="fbk-20260923-検証エビはtd-tc-trの単一鎖とする"></a>

### 検証エビはTD-TC-TRの単一鎖とする（**無効**）

| | |
|---|---|
| **id** | `FBK-20260923-検証エビはtd-tc-trの単一鎖とする` |
| **記録日時** | 2026-09-23 |
| **状態** | **置換済み・無効** |
| **置換先** | **[工程ごとの成果物とロールを表で確定する](#fbk-20260923-工程ごとの成果物とロールを表で確定する) へ置換** |
| **由来** | AI（Claude Code）の訂正 |

**無効になった内容**

| # | 工程 | 成果物 | レビュー方法 | 担当ロール |
|---|---|---|---|---|
| 1a | 要件定義+アーキ設計：検証設計を先に作る | AC（受け入れ基準）＋TD（結合検査の設計） | 1b とまとめて `plan-reviewer` が検証 | `ac-designer`（新設・1a専任） |
| 1b | 要件定義+アーキ設計：本体 | `ChangePlan` の残りフィールド（`selected_solution`＝スコープ＋責務分割を含む解決方針・`OOS`・`interfaces`・`invariants`・`failure_behavior`・`work_units DAG`）＋ ADR（`phase=要件定義+アーキ設計`） | `plan-reviewer` が 1a↔1b の整合＋既存決定との矛盾を検証 | `change-planner`（既存）→`plan-reviewer`（既存） |
| 2a | 実装：テスト設計を先に作る | TC（1a の TD を具体化したテストケース。Red 状態の実コードとして存在） | 2b とまとめて `pr-reviewer` が検証 | `test-writer`（新設・2a専任） |
| 2b | 実装：本体 | 実装コード（Green）＋必要なら軽量 ADR（`phase=実装`） | `pr-reviewer`（既存） | `issue-implementer`（既存） |
| 3a | V字右側：テストエビ | TR（2a の TC を実行した結果） | 既存の成績書運用 | 既存資産の流用 |
| 3b | V字右側：バグ修正ループ | `karte` の診断→是正 | `issue-fixer`→`pr-reviewer` 再レビュー（既存） | 既存資産の流用 |

**なぜ誤りだったか**

**2つの誤りが残っていた。** (a) 1a（アーキ設計＝結合検査レベル）と 2a（実装＝単体検査レベル）という
**異なる検査レベル**を「TD→TC→TR」という1本の鎖に畳んでいた——review_system 既存 `test-strategy` の
実際の ID 規則（`TD-<area>-<nnn>`）には検査レベルの区別が元から無く、それをそのまま踏襲してしまった。
(b) 別コンテキストの別 dispatch であるはずの 1a と 1b が、`ChangePlan` という**同じオブジェクトを
co-edit** する設計になっていた——`.claude/rules/05-skills-agents.md`「戻り値のハンドオフ規約」
（呼び出し先が新規に書き、呼び出し元が回収する）と整合しない。

**訂正の契機（オーナー逐語）**

> ①結合検査と単体検査は違うだろ？なんでTD/TC/TRに畳むの?分かりづらいならプリフィックス付けたら？
> あと必要に応じてシステム検査レベルもすべきでは？
>
> ②作る工程が違うのに同じオブジェクト(？)クラス(？)使い回すのは何で？

**実施者** Claude Code (AI)

---

<a id="fbk-20260923-工程ごとの成果物とロールを表で確定する"></a>

### 工程ごとの成果物とロールを表で確定する（**無効**）

| | |
|---|---|
| **id** | `FBK-20260923-工程ごとの成果物とロールを表で確定する` |
| **記録日時** | 2026-09-23 |
| **状態** | **置換済み・無効** |
| **置換先** | **[1a1bと2a2bのレビューを分離し無条件化する](#fbk-20260924-1a1bと2a2bのレビューを分離し無条件化する) へ置換** |
| **由来** | Opus 5.5 サブエージェントによる決定台帳の内部矛盾点検（オーナー指示・2026-09-24） |

**無効になった内容**

| # | 工程 | 成果物 | レビュー方法 | 担当ロール |
|---|---|---|---|---|
| 1a | 要件定義+アーキ設計：検証設計を先に作る | 新設 `VerificationDesign`：AC（受け入れ基準。`ST-TD` 相当）＋ `IT-TD`（結合検査設計） | 1b とまとめて `plan-reviewer` が検証（trace の起点として使う） | `ac-designer`（新設・1a専任） |
| 1b | 要件定義+アーキ設計：本体 | `ChangePlan`（`selected_solution`・`OOS`・`interfaces`・`invariants`・`failure_behavior`・`work_units DAG`）＋ ADR（`phase=要件定義+アーキ設計`。`input_refs` で 1a の `VerificationDesign` を参照） | `plan-reviewer` が 1a↔1b の整合＋既存決定との矛盾を検証（**risk gate 該当時**） | `change-planner`（既存）→`plan-reviewer`（既存） |
| 2a | 実装：テスト設計を先に作る | `UT-TC`（1a の `IT-TD` の対象範囲内で、単体レベルのテストケース。Red 状態の実コードとして存在） | **2b とまとめて** `pr-reviewer` が検証 | `test-writer`（新設・2a専任） |
| 2b | 実装：本体 | 実装コード（Green）＋必要なら軽量 ADR（`phase=実装`） | `pr-reviewer`（既存） | `issue-implementer`（既存） |
| 3a | V字右側：テストエビ | `ST-TR`／`IT-TR`／`UT-TR`（各レベルの TC 実行結果） | 既存の成績書運用 | 既存資産の流用 |
| 3b | V字右側：バグ修正ループ | `karte` の診断→是正 | `issue-fixer`→`pr-reviewer` 再レビュー（既存） | 既存資産の流用 |

上記のうち IT 水準の review_system 側実装状況（形式的には無いが `test_apply.py` 等一部が実質的に
結合レベルを含む・#543 起票済み）と、各行の受け渡し方式（`gitgate new-branch`→`adopt-branch` 連鎖）は
本訂正の対象外で、そのまま v5（現行§A）へ引き継がれている。

**本エントリ（v4）自身の成立過程**（役割矛盾の是正→TD-TC-TR重複の解消→検査レベルの分離・
`VerificationDesign`の分離、の3段階の訂正を経て確定）は §B の3エントリ
（[検証設計と本体を同一ロールが書く](#fbk-20260922-検証設計と本体を同一ロールが書く)→
[ロールは分離済みだが成果物名は未根拠](#fbk-20260923-ロールは分離済みだが成果物名は未根拠)→
本エントリ）に記録済み。

**なぜ誤りだったか**

**2つの矛盾を、決定台帳全体をOpus 5.5サブエージェントに点検させたところ発見した。**

(a) [フェーズ内はテスト設計先行のフラクタル構造とする](#fbk-20260922-フェーズ内はテスト設計先行のフラクタル構造とする)
は「先に検証設計を作りレビューしてから本体成果物を作る」と定めていたが、本表の1a/2a行は
「1b/2bとまとめてレビュー」——検証設計は本体ができた後にしかレビューされておらず、
フラクタル原則自身が防ごうとした「AC後付け確証バイアス」（#371自身が問題視した構造）が
形を変えて残っていた。

(b) 1b行の `plan-reviewer` が「risk gate 該当時」の条件付きだった一方、
[AC妥当性確認はplan-reviewerを強化して担う](#fbk-20260922-ac妥当性確認はplan-reviewerを強化して担う)
は `plan-reviewer` をAC妥当性の唯一のチェック点としていた——risk gateに該当しないIssueは
`VerificationDesign`もACも一切レビューされずに通ってしまう設計になっていた。

**訂正の契機（オーナー逐語）**

> フラクタル原則 vs 表の1a/2aレビュー方法はa
> plan-reviewerの実施条件（risk gate該当時のみ）vs AC妥当性確認の強化はaかな。
> bは改良ネタとしていつかやれたらいいなぁ。。。くらいの感覚。

**実施者** Claude Code (AI)（点検はOpus 5.5サブエージェントへ委譲・オーナー指示）

---

<a id="fbk-20260924-1a1bと2a2bのレビューを分離し無条件化する"></a>

### 1a1bと2a2bのレビューを分離し無条件化する（**無効**）

| | |
|---|---|
| **id** | `FBK-20260924-1a1bと2a2bのレビューを分離し無条件化する` |
| **記録日時** | 2026-09-24 |
| **状態** | **置換済み・無効** |
| **置換先** | **[検証エビは全レベルでTD-TC-TRを揃える](#fbk-20260924-検証エビは全レベルでtd-tc-trを揃える) へ置換** |
| **由来** | Opus 5.5 サブエージェントによる決定台帳の内部矛盾点検（オーナー指示・2026-09-24） |

**無効になった内容**

| # | 工程 | 成果物 | レビュー方法 | 担当ロール |
|---|---|---|---|---|
| 1a | 要件定義+アーキ設計：検証設計を先に作る | 新設 `VerificationDesign`：AC（受け入れ基準。`ST-TD` 相当）＋ `IT-TD`（結合検査設計） | `plan-reviewer` が単独でレビューする（無条件）。1b の着手前に完了させる | `ac-designer`（新設・1a専任） |
| 1b | 要件定義+アーキ設計：本体 | `ChangePlan`（`selected_solution`・`OOS`・`interfaces`・`invariants`・`failure_behavior`・`work_units DAG`）＋ ADR（`phase=要件定義+アーキ設計`。`input_refs` で 1a の `VerificationDesign` を参照） | `plan-reviewer` が 1a↔1b の整合＋既存決定との矛盾を検証する（無条件） | `change-planner`（既存）→`plan-reviewer`（既存） |
| 2a | 実装：テスト設計を先に作る | **`UT-TC`のみ**（1a の `IT-TD` の対象範囲内で、単体レベルのテストケース。Red 状態の実コードとして存在） | `pr-reviewer` が単独でレビューする（無条件） | `test-writer`（新設・2a専任） |
| 2b | 実装：本体 | 実装コード（Green）＋必要なら軽量 ADR（`phase=実装`） | `pr-reviewer`（既存・無条件） | `issue-implementer`（既存） |
| 3a | V字右側：テストエビ | `ST-TR`／`IT-TR`／`UT-TR`（各レベルの TC 実行結果） | 既存の成績書運用 | 既存資産の流用 |
| 3b | V字右側：バグ修正ループ | `karte` の診断→是正 | `issue-fixer`→`pr-reviewer` 再レビュー（既存） | 既存資産の流用 |

**なぜ誤りだったか**

**TD→TC→TRの鎖が非対称だった。** 1aがST-TD/IT-TDを作り、2aがUT-TCのみを作るが、
ST-TC/IT-TC/UT-TDを作る工程がどこにも無いまま、3aでST-TR/IT-TR/UT-TRという3レベル分の
実行結果が突然生成される構造になっていた。Opus 5.5サブエージェントの点検がこの非対称を
指摘し、オーナーが「モデルの欠落だと思う」（意図的な省略ではない）と裁定した。

**訂正の契機（オーナー逐語）**

> ドリフト⑦はモデルの欠落だとおもう。

**実施者** Claude Code (AI)（点検はOpus 5.5サブエージェントへ委譲・オーナー指示）

---

<a id="fbk-20260922-権限設計は工程表の各ロールへ役割必須性で接続する"></a>

### 権限設計は工程表の各ロールへ役割必須性で接続する（**無効**）

| | |
|---|---|
| **id** | `FBK-20260922-権限設計は工程表の各ロールへ役割必須性で接続する` |
| **記録日時** | 2026-09-22 |
| **状態** | **置換済み・無効** |
| **置換先** | **[成果物ロールは各自pushし直接コミットする](#fbk-20260923-成果物ロールは各自pushし直接コミットする) へ置換** |
| **由来** | AI（Claude Code）の点検 |

**無効になった内容**

| 役割 | push | merge | 必須か | 戻すのは容易か | 結論 |
|---|---|---|---|---|---|
| `issue-investigator` | 無 | 無 | 調査は書込不要 | — | 変更なし（既に最小） |
| `change-planner`/`ac-designer`（1a/1b） | 無 | 無 | 計画は handoff artifact で足りる。git 書込は不要 | — | 変更なし（既に最小） |
| `plan-reviewer` | 無 | 無 | レビューは書込不要 | — | 変更なし（既に最小） |
| `issue-implementer`/`test-writer`（2a/2b） | 有 | 無 | push は必須——worktree の成果をレビュー可能な形にする唯一の経路 | — | push は維持。`test-writer` 自体には持たせず、実際のコミットは `issue-implementer` が担う |
| `issue-fixer`（3b） | 有 | 無 | 同上 | — | 変更なし |
| `pr-reviewer` | 無 | 有 | 必須ではない——判定を出すことと merge を実行することは別行為 | 容易——hook 側の設定変更のみで戻せる | 剥がす |

**なぜ誤りだったか**

**`ac-designer`／`change-planner`／`test-writer` を「push 不要・handoff artifact で足りる」と判定したが、
その前提（`tmp/_handoff/` 経由で成果物を渡し、`issue-implementer` が代表してまとめて commit する）
自体が誤りだった。** 成果物を作った本人が commit しないのは、既存の `issue-fixer`（`adopt-branch` で
合流し自分の変更を自分で commit+push する）の運用とも整合しない。merge 側（`pr-reviewer` の剥がし）の
判定は誤りではなく、そのまま [成果物ロールは各自pushし直接コミットする](#fbk-20260923-成果物ロールは各自pushし直接コミットする) へ引き継がれている。

**訂正の契機（オーナー逐語）**

> 各ロールにもコミット・push権限が必要では？レビュー役は兎も角、成果物作ってコミットしないのは意味不明。

**実施者** Claude Code (AI)

---

<a id="fbk-20260922-トレサビは参照方式+doc_system水準の査定を必須とする"></a>

### トレサビは参照方式+doc_system水準の査定を必須とする（**無効**）

| | |
|---|---|
| **id** | `FBK-20260922-トレサビは参照方式+doc_system水準の査定を必須とする` |
| **記録日時** | 2026-09-22 |
| **状態** | **置換済み・無効** |
| **置換先** | **[トレサビはref宣言駆動の共通アダプタで実現する](#fbk-20260923-トレサビはref宣言駆動の共通アダプタで実現する) へ置換** |
| **由来** | AI（Claude Code）の設計 |

**無効になった内容**

アダプタ実装方式は「データ源ごとに個別実装（案ii）」を推奨し、#371 の共通 envelope
（ChangePlan/VerificationDesign）は「`tmp/_handoff/` の一時ファイルで work unit 完了後に消える」
という前提で索引対象外としていた。

**なぜ誤りだったか**

**2点、根拠不足のまま結論を出していた。** (1) `feedback_ledger/schema.py` を実際に読まずに
「データ源ごとの個別実装」を推奨していた——実際には `Field.ref` という宣言がスキーマに既にあり、
フィールド名を決め打ちしない安全な汎用アダプタ（案iii）が可能だった。(2) envelope を tmp 経由の
一時ファイルとみなしたが、[成果物ロールは各自pushし直接コミットする](#fbk-20260923-成果物ロールは各自pushし直接コミットする)
の訂正により ChangePlan/VerificationDesign は最初から worktree へ直接 commit される永続artifact
であることが分かり、索引対象から外す根拠が失われた。

**訂正の契機（オーナー逐語）**

> envelope消すならトレサビどうやって取るの？

**実施者** Claude Code (AI)

---

<a id="fbk-20260921-撤退条件をa-b-cの3種で定める"></a>

### 撤退条件をA/B/Cの3種で定める（**無効**）

| | |
|---|---|
| **id** | `FBK-20260921-撤退条件をa-b-cの3種で定める` |
| **記録日時** | 2026-09-21 |
| **状態** | **置換済み・無効** |
| **置換先** | **[撤退条件Aから現行必須施策を除外する](#fbk-20260924-撤退条件aから現行必須施策を除外する) へ置換** |
| **由来** | Opus 5.5 サブエージェントによる決定台帳の内部矛盾点検（オーナー指示・2026-09-24） |

**無効になった内容**

| | 条件 | 処置 |
|---|---|---|
| A | 観測窓終了時に Q が下限（0.01）を割った | **無条件撤退**。C/D が良くても残さない（**例外なし**） |
| B | Q の改善率 < C または D の悪化率 | 撤退または縮退 |
| C | 観測窓終了時に**因果を識別できなかった**（交絡） | 撤退（学習成果を生まなかったため） |

**なぜ誤りだったか**

現在値（Q=0.37）から下限（0.01）への到達まで約29週間を要する状況で、条件Aに例外が無いと、
オーナーが既に明示的に必須と裁定した3件の施策
（[Issue371の作成工程分離は必須とする](#fbk-20260921-issue371の作成工程分離は必須とする)・
[1a/2aと1b/2bはロール分離を必須とする](#fbk-20260922-1a-2aと1b-2bはロール分離を必須とする)・
[Issue513のescalate機械強制は必須とする](#fbk-20260921-issue513のescalate機械強制は必須とする)）
までもが、条件A発動時に無条件で撤退対象になってしまう——「必須」という確定裁定を、将来条件が
自動的に上書きする矛盾を内包していた。

**訂正の契機（オーナー逐語）**

> 撤退条件A vs 必須施策については、現時点の必須事項についてのみb

**実施者** Claude Code (AI)（点検はOpus 5.5サブエージェントへ委譲・オーナー指示）

---

<a id="fbk-20260923-envelopeは対象ごとにwork_unit分置する"></a>

### envelopeは対象ごとにwork_unit分置する（**無効**）

| | |
|---|---|
| **id** | `FBK-20260923-envelopeは対象ごとにwork_unit分置する` |
| **記録日時** | 2026-09-23 |
| **状態** | **置換済み・無効** |
| **置換先** | **[枠組みフィールドは対象prefixなしで置く](#fbk-20260924-枠組みフィールドは対象prefixなしで置く) へ置換** |
| **由来** | オーナー指摘（項目6の検討） |

**無効になった内容**

（構造そのものは v2 でも維持されている——「無効」なのは**不完全だった**点のみ。全文は
[枠組みフィールドは対象prefixなしで置く](#fbk-20260924-枠組みフィールドは対象prefixなしで置く) の
「決定内容」冒頭で v1 として再掲済み）

**なぜ誤りだったか**

`work_units DAG`の分解構造はカバーしたが、`ChangePlan`/`VerificationDesign`が持つ**Issue全体スコープの
枠組みフィールド**（`selected_solution`/`OOS`/`interfaces`/`invariants`/`failure_behavior`・`ST-TD`）
への言及が無かった。対象ごとの物理分置しか無い状態でこれらをどこに書くかが未規定のまま残っていた。

**訂正の契機（オーナー逐語）**

> 項目6は選択肢3がよい

**実施者** Claude Code (AI)

---

## §C 根拠（実測データ）

### C-1 スケールの実測（2026-09-21・現物から取得）

| 対象 | 実測 |
|---|---|
| 製品 `review_system/` | 1,744 行・最終コミット 2026-06-09（104日前） |
| Issue 運用ハーネス Python（14モジュール） | **29,799 行**（製品の 17.1倍） |
| 毎ターン注入される規範（`CLAUDE.md` + `.claude/rules/*.md` + `.ai/guidance/common.md`） | **86,058 文字** |
| その配送用の写し `governance-directives.md` | 別途 19,918 文字 |
| issue-pipeline 資産一式（`.ai/` 本文 + Claude wrapper） | 122,453 文字 |
| 直近60日のコミット | 全 323 ／ `review_system/` **0** ／ ハーネス 147（45%） |
| open Issue | 75件。`area:harness` 50（67%）／ `area:review-system` 7（9%） |
| open のうち `type:tracking` | 11件（15%） |
| `.claude/hooks/` のフック本体 | 14本 |

`.claude/rules/05-skills-agents.md` 単体で 21,442 文字＝`CLAUDE.md` 本体（4,172）の 5.1倍。
内容は手順ではなく意思決定の経緯が大半を占める。#407（手順と経緯の分離）が 2026-08-30 に close した**後**の状態。

### C-2 再発欠陥の発生源（census 274件の再集計）

再発（`recurrence_of` が埋まっている）は全21件。親 Issue の root cause 内訳：

| 親の root cause | 再発数 | 系列 |
|---|---|---|
| `gate-false-positive` | 4 | #412→#428(2)、#414 |
| `gate-bypass-detection-gap` | 3 | #213→#215→#218 |
| `worktree-isolation-gap` | 2 | #350→#354 |
| `scope-bypasses-harm-gate` | 2 | #495→#503/#511 |
| `worktree-cleanup-incomplete` | 1 | #426 |
| `allowlist-coverage-gap` | 1 | #356 |
| `enumeration-approach-limit` | 1 | #222 |
| **小計：ゲート・隔離・権限まわり** | **14 / 21 ＝ 67%** | |
| `required-edge-rule-overgeneralized` | 3 | #253→#254/#255/#256（doc_system 側） |
| `time-fixture-wallclock` | 1 | #302 |
| `tools-declaration-runtime-mismatch` | 1 | #386 |
| `state-transition-ambiguity` | 1 | #423 |
| `unsafe-default-value` | 1 | #355 |

**不変条件型の機構は再発ゼロ**：

| 機構 | 判定方式 | 再発 |
|---|---|---|
| `asset_parity` | 4ツリーの presence が**一致するか** | **0件** |
| `test_governance_sync` | 正本集合の**ハッシュが一致するか** | **0件** |
| `time_fixture_lint` | 保護マーカーが**有るか無いか** | **0件** |
| `agent-command-gate` | 禁止コマンドを**列挙して照合** | 4件＋ |
| `pr_merge_gate` | merge コマンドを**分類器で判定** | 4件 |
| worktree 状態管理 | 状態遷移を**列挙** | 3件 |
| `scope`/`disposition` の契約 | 散文規律 | 2件 |

AI 自身が 2026-07-12 に一度この結論へ到達している（#220 の root cause slug ＝ `enumeration-approach-limit`
「agent-command-gate の恒等変換フィルタ列挙方式の限界（**設計判断待ち**）」）。放置され、
その後も #412 / #414 / #428 / #533 として同型が再発し続けた。

**交絡のため結論にしない数字**：`mechanized=yes` 再発 14/103 = 13.6% ／ `mechanized=no` 5/70 = 7.1%。
選択バイアス（機械化されるのは難しい問題）と観測バイアス（散文側の再発は検出されず Issue にならない。
census の `data/README.md` が「`recurrence_of` は**過小計上**」と明記）があるため、率の比較には耐えない。
**正しい読み：機械化は再発を防いでいないが、再発を可視化している。**
上の内訳（67%）は率ではなく件数の構成比なので、この交絡の影響を受けない。

**曝露量で割れない**：各機構が何回発火し、何回 deny し、何回が誤検出だったかの記録が無いため、
「ゲート系が多く作られたから多く壊れた」という説明を排除できない。`karte` が `tmp/`（版管理外）で
消えることの帰結。

### C-3 A-SPICE マッピング（対象＝ハーネス開発そのもの）

実際に開発されているのはハーネス（29,799行）であり、`review_system`（1,744行）は題材である。
したがって A-SPICE を当てる対象はハーネス開発になる。

| A-SPICE | ハーネス開発での現状 | 判定 |
|---|---|---|
| SYS.1 要求抽出 | Issue の「目的・背景」 | 構造化なし |
| **SYS.2 システム要求分析** | Scope と AC に混在。要求として分離・版管理されていない | **無し** |
| **SYS.3 システムアーキテクチャ設計** | 14フック・6台帳・3ロール・3層 allowlist の**全体構造を記述した文書が存在しない** | **無し** |
| **SWE.1 SW要求分析** | Acceptance criteria が近いが、要求として管理されていない | **無し** |
| **SWE.2 SWアーキテクチャ設計** | — | **無し**（#371 の `change-planner` が埋める） |
| SWE.3 詳細設計・実装 | `issue-implementer` | あり |
| SWE.4 ユニット検証 | `tests/unit` 38,291行 | **あり・強い** |
| SWE.5/6 統合・適格性確認 | CI ＋ `pr-reviewer` | あり（**#530 で網羅性に穴**） |
| **SUP.1 品質保証（独立した QA）** | `pr-reviewer` は検証であって QA ではない | **無し** |
| SUP.8 構成管理 | git ＋ `gitgate` | あり。**ただし `karte`/handoff は `tmp/`＝構成管理外** |
| SUP.9 問題解決管理 | Issue ＋ `karte` | あり。**ただし `karte` は close で消える** |
| **SUP.10 変更要求管理** | Issue 本文を直接書き換えて変更が入る（#370 は訂正2回＋追記4件） | **無し** |
| MAN.3 プロジェクト管理 | Projects ＋ tracking Issue 11件 | あり |
| **双方向トレーサビリティ**（全 SYS/SWE 共通の BP） | Issue → PR → commit → test の鎖が機械検証されていない | **無し** |

**存在するのは SWE.3〜6 ＋ SUP.8/9 ＋ MAN.3 だけ。** CL1（Performed）は部分的に満たしても、
**CL2（Managed：作業成果物の管理とトレーサビリティ）は明確に未達。**

**なぜそうなったかは規約に書いてある**——`.claude/rules/02-decision-process.md` の起票先判定表が
「どちらのシステムにも含有されない汎用ハーネス」を仕様グラフの外に出すと規定している。結果：

- doc_system の成果物 → VAL→SR→FR→SPEC→ORC/MOD→TD/TC/TR の完全な鎖 ＋ `dsv2` の必須辺検証（機械）
- review_system の成果物 → `docs/` の凍結セット ＋ Q#/DD#
- **Issue 運用ハーネス（29,799行・実質的な主産物）→ Issue 本文と PR だけ**

**「Issue 運用に回す」と決めた瞬間に、そのハーネスは V字の外に出る。**

### C-4 #371 のブロッカー状態（2026-09-21 実測）

```
#371 の blocked_by:  374 closed / 407 closed / 386 closed / 452 open
#452 の blocking:    [(371, 'open')]
```

#374 / #386 / #407 は 2026-08-30 までに全て close。**#452 が唯一の残存ネイティブブロッカー**。
一方 #445 本文は「#452は#374/#407をblockしない（**2026-08-29オーナー判断**）」「#452（…**#374/#407/#371をblockしない**）」
と明記している。**本文の決定が machine-readable な relation へ反映されていなかった。**
#445 の Acceptance criteria「native blocker、Project Status、本文のDAGに矛盾・循環・stale表示がない」に
#445 自身が違反していた状態。

> **処置済み**：オーナーが relation を削除した（2026-09-21）。

---

## §D 未決事項

| # | 論点 | 状態 |
|---|---|---|
| U-1 | #371 のテーラリング判定軸（[Issue371は軽い修正でテーラリング可とする](#fbk-20260921-issue371は軽い修正でテーラリング可とする) の具体化）。制約は[軽量経路は分析省略でなく分析縮小とする](#fbk-20260923-軽量経路は分析省略でなく分析縮小とする)で確定 | 未決（**優先度低**） |
| ~~U-2~~ | ~~Codex 利用を継続するか~~ | **解消 → [Codexを主文脈にせずサブ扱いとする](#fbk-20260922-codexを主文脈にせずサブ扱いとする)**（②を採用・実行は #538） |
| U-3 | 撤退条件B の具体的閾値（Q 改善率と C/D 悪化率の比） | 未決 |
| U-4 | 5項目（[必須施策を5項目に定める](#fbk-20260921-必須施策を5項目に定める)）の実施順。AI 推奨＝⑤ → ④明文化 → ② → ① → ③ | 未決 |
| ~~U-5~~ | ~~④（基本原則の徹底機構）の設計~~ | **解消 → [④の原則セットはISO25010の8特性で充足確認する](#fbk-20260922-④の原則セットはiso25010の8特性で充足確認する)**（実行は #539。gap分析・優先順位付け・機構化そのものは #539 側の以後の作業） |
| ~~U-6~~ | ~~器の最終判断（GitHub Issue 継続＋役割分離でよいか）~~ | **解消 → [器はGitHub Issue継続で確定する](#fbk-20260924-器はgithub-issue継続で確定する)**（オーナー承認・2026-09-24） |
| ~~U-7~~ | ~~本分析結果の起票方針（#445 本文へ集約するか、個別起票するか）~~ | **解消 → [起票先はラベルで機械的に問い合わせる](#fbk-20260924-起票先はラベルで機械的に問い合わせる)**（個別起票は既定路線・「入り口」問題はラベル化で解消・オーナー承認・2026-09-24） |
| U-8 | 1 PR の上限行数を置くか | 未決 |
| ~~U-9~~ | ~~決定台帳の Skill の書き込み先~~ | **解消 → [決定台帳の正本をfeedback-ledgerとする](#fbk-20260921-決定台帳の正本をfeedback-ledgerとする)** |
| U-10 | #409（AgentRun ledger）の扱い。[Issue371と409と513を非推奨とする](#fbk-20260921-issue371と409と513を非推奨とする) の無効化は #371 / #513 に限り、#409 は未判断 | **優先度を下げて保留**（他の決定に従属するため・オーナー指示・2026-09-22） |
| ~~U-11~~ | ~~`VerificationDesign`/`ChangePlan`/ADR の永続化パス設計~~ | **解消 → [枠組みフィールドは対象prefixなしで置く](#fbk-20260924-枠組みフィールドは対象prefixなしで置く)** |
| ~~U-12~~ | ~~#540 本文が本セッション後半の訂正に追従できていない~~（`ref=`宣言駆動アダプタ・`VerificationDesign`/`ChangePlan`の索引化） | **解消 → `gh issue edit 540` で本文反映済み（2026-09-24・オーナー承認）。`adopt-branch`連鎖は#371側の責務のためRelated行の参照追加のみで対応** |
| U-13 | [枠組みフィールドは対象prefixなしで置く](#fbk-20260924-枠組みフィールドは対象prefixなしで置く) のスキーマ定義（`Field`/`TableSpec`/`DocSpec`パターンでの`CHANGE_PLAN_SPEC`/`VERIFICATION_DESIGN_SPEC`宣言）・読み書きコードの置き場（本決定のスコープ外として据え置いた） | 未決 |
| U-14 | [検証エビは全レベルでTD-TC-TRを揃える](#fbk-20260924-検証エビは全レベルでtd-tc-trを揃える) 3a行の harness 側テストエビ**道具ごとの具体パス**（`<tool>/test-strategy/` 相当）が未決のまま表中に記載されている。Opus 5.5サブエージェントの点検で「§Dに行が無い」と指摘され新設 | 未決 |
| U-15 | risk gate 非該当時、`plan-reviewer`/`pr-reviewer` の無条件レビューを軽量な機械代替チェックで済ませる改良案（[検証エビは全レベルでTD-TC-TRを揃える](#fbk-20260924-検証エビは全レベルでtd-tc-trを揃える) で選択肢bとして提示され不採用。`1a/2aと1b/2bはロール分離を必須とする`エントリで一度却下した類似案との整合要確認） | 未決（**優先度低・オーナー「改良ネタとしていつかやれたら」**） |
| ~~U-16~~ | ~~施策レジストリを`audit/`に手書きで置く現行方針と「`audit/`は派生ビューにすべき」の緊張関係~~ | **解消（対立案なし）→ [決定台帳の正本をfeedback-ledgerとする](#fbk-20260921-決定台帳の正本をfeedback-ledgerとする) が既に「`audit/`は台帳からの導出ビューにし、手書きをやめる」と明記済み。#537（P1/Now）が未実装なだけで、比較すべき選択肢は無かった（2026-09-24 検証・Claude Code (AI)）** |
| ~~U-17~~ | ~~#533（classifier誤検知）がマージ権限剥離後も必要か~~ | **解消（対立案なし）→ [マージ権限をAIから剥がしsettings.jsonへ移す](#fbk-20260923-マージ権限をaiから剥がしsettings.jsonへ移す) 本文に「`pr_merge_gate/classifier.py`は不要になる」と明記済み。#542本文にも「Related: #533（本Issueで解消される見込み）」と紐付け済み。比較すべき選択肢は無かった（2026-09-24 検証・Claude Code (AI)・`gh issue view 542`で確認） |

---

## §E Jev（判定専用 AI）へ持ち越す検査項目

> [トピックの述語は機械強制しない](#fbk-20260922-トピックの述語は機械強制しない) で「今は強制しない」と
> 決めた項目の置き場。**「対応不要」ではなく、強制する機構が来るまでの保留である。**
>
> **⚠ §E-2 以降は AI の提案であり、オーナーの判断を受けていない。**

### Jev とは何か（2026-09-22 時点の調査結果）

**TypeSafe AI の「System One モデル」。文章を生成せず、型付きの判定だけを返す。** LLM ではない。
2026-09-15 に限定早期アクセス開始（DCVC リードの $40M シードと同時発表）。

| 項目 | 内容 |
|---|---|
| **入力** | `state`（文字列 / JSON / テキスト配列）＋ `questions`（型付きの問い。複数可） |
| **出力** | 型付きの値＋**較正済み確率**＋confidence。**スキーマ外の値を返せない**（型エラーが原理的に起きない） |
| **問いの型** | `Choice`（選択肢から1つ・各選択肢の確率＋confidence）／`Score`（順序付きレベルで採点・各レベルの確率＋confidence）／`Noul`（yes/no 命題の確率 0〜1） |
| **サンプリング** | **非自己回帰・並列**。全ての問いを1パスで同時評価 |
| **学習** | RLCD（Reinforcement Learning for Calibrated Decisions）。合成データのみ。アーキテクチャ・重み・論文は非公開 |
| **速度** | end-to-end 70〜500ms |
| **価格** | **入力 $0.042 / 1M tokens。出力は無料** |
| **不得意** | 文章生成・計算・日付比較 |

リクエストの形（LangChain のガイドより）：

```json
{
  "model": "jev-latest",
  "state": "<判定対象のテキストや構造化状態>",
  "questions": {
    "is_urgent": { "type": "noul", "instructions": "The message conveys urgency or time-sensitivity" }
  }
}
```

```json
{ "is_urgent": { "type": "noul", "noul": 0.999 } }
```

**性能主張には留保が要る**——40〜200倍高速・40〜400倍安価（ピーク 193.6倍 / 444.6倍）という数字は
TypeSafe 自身のモデル能力チームが作ったワークフローでの自社計測であり、同社自身が偏りの可能性と
「実世界の結果は上振れ側の数字より低いだろう」ことを認めている。**採用判断は自前の観測窓で測る**
（[施策ごとに観測窓を切る](#fbk-20260921-施策ごとに観測窓を切る)）。

### なぜこのリポジトリの課題に合うのか

§C-2 の実測では、**記録済み再発欠陥21件のうち14件（67%）がゲート・隔離・権限といった列挙型の機構を
発生源**としていた。列挙型ゲートが壊れる理由は構造的で、「危険な語のリスト」「許可コマンドのリスト」を
人が保守し続けなければならず、**リストの外側は素通りし、リストの内側の誤検知は運用を止める**。

Jev は**リストではなく命題の確率**を返すので、この保守対象そのものが消える。加えて confidence が
返るため、**低 confidence をオーナーへエスカレーションする**という第三の出口を作れる
（現在の機械ゲートは deny か allow の二択しかない）。これは [Issue513のescalate機械強制は必須とする](#fbk-20260921-issue513のescalate機械強制は必須とする)
の escalate 経路と直接つながる。

**本セッション中に実測した具体例**：`pr_merge_gate` の classifier が、read-only の
`ls` / `cat` / `python3 <script>` を含む複合コマンドに対して `CLASSIFIER_UNKNOWN` と
`POST_AUDIT_INTEGRITY_ERROR/RECLASSIFIED_NOT_MERGE` を繰り返し発報し、作業を都度迂回させた
（Issue #533 として起票済み）。**「このコマンドは merge 操作か」は文字列照合ではなく判定の問題**であり、
Jev の `Noul` がそのまま当たる形をしている。LangChain が公開した `AutoModeMiddleware` は、
まさにこのツール呼び出しリスク判定を Jev で行う実装例である。

### §E-1 決定台帳の検査（持ち越し確定）

| # | 検査したいこと | 今どうなっているか | Jev の型 |
|---|---|---|---|
| J-1 | **トピックが「何を決めたか」を述語まで書いているか** | 未検査（本決定で保留） | `Noul` |
| J-2 | **トピックと決定内容が一致しているか**（トピックが本文と違うことを言っていない） | 未検査 | `Noul` |
| J-3 | **`narrative.owner_verbatim` が本当に逐語か**（AI の解釈が混入していない） | **L4 が推測語彙の列挙で代用** | `Noul`＝**置換候補** |
| J-4 | **`narrative.inferred_reason` の `推測：` が実際に推測か** | **L5 が断定語彙の列挙で代用** | `Choice`（推測 / 断定 / 混在）＝**置換候補** |
| J-5 | **`narrative.background` が経緯として成立しているか**（決定内容の再掲で埋めていない） | 未検査 | `Score` |
| J-6 | **生存する決定どうしが実質的に矛盾していないか**（`supersedes` の張り忘れ） | 未検査。**id が日付＋内容由来なので、張り忘れても衝突せず素通りする** | `Noul`（生存ペアの総当たり） |
| J-7 | **`affected_assets` に過不足がないか** | L3 が**参照先の実在**のみ検査（在るものが正しいかは見ない） | `Noul` |

**J-6 は新 id 体系が持ち込んだ穴である。** 旧 `D-0xx`/`D-1xx` は「§B へ物理的に移す」という操作自体が
訂正の証跡だったが、新体系では `supersedes` を書かない限り古い決定が §A に残り続ける。
id 規則では原理的に検出できない。生存 n 件の総当たりは O(n²) だが、n=17・1ペア数百トークンなら
1パス数円に収まらない額にすらならない（後述のコスト試算）。

### §E-2 ハーネス全体の列挙型ゲート置換候補（**AI 提案・未決**）

§C-2 で再発欠陥の発生源と特定された機構のうち、判定の性質が Jev に合うもの。

| # | 対象 | 現在の実装 | Jev の型 |
|---|---|---|---|
| J-8 | **コマンドが merge / push 操作か**（`pr_merge_gate`・`agent-command-gate.sh`） | シェル文字列の静的照合。複合コマンド・heredoc・ループで誤判定（#533） | `Noul` |
| J-9 | **finding の `harm` が real か none か**（`pr-reviewer`） | AI の自己申告。機械検査なし | `Choice` |
| J-10 | **finding が `scope: in` か `out` か**（Issue #495） | AI の自己申告。機械検査なし | `Noul` |
| J-11 | **規範の写し（`governance-directives.md`）が正本と意味的に一致するか** | `check-governance-drift.sh` は**ハッシュ一致のみ**＝意味のズレは見ない | `Noul` |

**J-11 は不変条件型の検査が既に効いている領域なので、優先度は低い**（§C-2 でハッシュ一致型は再発ゼロ）。
逆に **J-8 は再発欠陥の最大の発生源そのものなので、費用対効果が最も高い**と見ている。

### 採用前に片付ける必要がある前提

1. **課金の認可が要る**（`.claude/rules/03-operational.md`「CI/外部サービス連携のコスト方針」）。
   現在の機械検査は全て標準ライブラリのみ・オフラインで動く。Jev は外部 API であり**無課金では実現できない**。
   - **コスト試算**：台帳全21件の1パス検査で state ≈ 3万トークン → **$0.0013**。
     PR ごとのコマンド判定で 1万トークン → **$0.0004**。出力は無料。
     月間の実行回数を多めに見ても**月額は1ドルに届かない**見込み。
   - ただし**金額の多寡に関わらず、実装前にオーナーの明示認可が必要**（規範6）。
2. **CI からネットワークと API キーが要る。** GitHub Actions secrets への登録と、
   **キーが無い環境での挙動**（fail-open か fail-close か）を決める必要がある。
   現在の検査は全てオフラインで完結しているので、これは設計上の性質変化にあたる。
3. **確率をどう判定に落とすか。** Jev は真偽ではなく確率と confidence を返すので閾値が要る。
   列挙型リストの保守は消えるが、**閾値というパラメータが1つ増える**——ただし保守対象は
   「育ち続けるリスト」から「1つのスカラ」に変わるので、§C-2 の再発パターンには乗りにくい。
   低 confidence は deny/allow のどちらでもなく**オーナーへエスカレーション**する設計を推奨する。
4. **限定早期アクセスである。** 2026-09-15 開始で、可用性・レート制限・価格の安定性はまだ実績が無い。
   **撤退条件を持たせる**（[施策ごとに観測窓を切る](#fbk-20260921-施策ごとに観測窓を切る)）。
5. **判定の証跡を残す。** 確率値と confidence をカルテ／台帳に記録しないと、
   「なぜ止まったか／なぜ通ったか」が後から再構成できず、
   [撤退条件Aから現行必須施策を除外する](#fbk-20260924-撤退条件aから現行必須施策を除外する) の条件C（因果が特定できないなら撤退）に抵触する。

### 出典

- [Introducing System One Models & Jev — TypeSafe AI](https://typesafe.ai/blog/introducing-system-one-models-and-jev)
- [Building a Harness with Jev — LangChain](https://www.langchain.com/blog/building-a-harness-with-jev)
- [Jev (AI model) — Wikipedia](https://en.wikipedia.org/wiki/Jev_(AI_model))
- [A new kind of AI model from a ChatGPT inventor is thrilling developers — TechCrunch](https://techcrunch.com/2026/09/18/a-new-kind-of-ai-model-from-a-chatgpt-inventor-is-thrilling-developers/)
